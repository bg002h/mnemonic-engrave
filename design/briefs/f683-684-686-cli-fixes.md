# Brief — small CLI fixes: F-683 (ms), F-684 + F-686 (toolkit)

Read F-683, F-684 and F-686 in `/scratch/code/shibboleth/mnemonic-engrave/design/FOLLOWUPS.md`.

## F-683 — mnemonic-secret: `--help` offers separators ms refuses

ms 0.19.1 rejects `--separator hyphen` and `--separator comma`, yet `--help`
lists them as possible values. Make what `--help` (and `gui-schema`, if it
lists values) offers equal what ms accepts, ideally by fixing the value enum at
its source so the two can't disagree. Check every ms subcommand with
`--separator`. mk 0.13.0 still accepts both; don't touch mk.

Worktree `/scratch/code/shibboleth/ms-worktrees/f683` on branch `f683-separators`
off `origin/master`. Put a CHANGELOG entry under `[Unreleased]`; **no version
bump**. The controller decides when ms releases next.

**Note:** mnemonic-gui mirrors ms's surface, and it already dropped these
values after testing the real binary. If `gui-schema` output changes, say
exactly how in the report, so the GUI's next pin bump knows.

## F-684 — toolkit CHANGELOG at v0.104.0

Shipped changes sit under `[Unreleased]`: at least the `--group-size` default
of 0, `bitcoin-core-addresses`, and the argv-secret refusal. Verify each item
under `[Unreleased]` against the `mnemonic-toolkit-v0.104.0` tag
(`git merge-base --is-ancestor <commit> mnemonic-toolkit-v0.104.0`, or run the
0.104.0 release binary) and move what shipped under `## [0.104.0]`. Leave
genuinely unreleased items where they are.

## F-686 — `mnemonic restore --help` says depth-≥2 taproot is refused

0.104.0 restores a depth-2 tap tree (exit 0; first address matches the
export). Find the claim in the help source and make it true. Check for the
same claim in the man-page source, the CLI manual and the help goldens.

Toolkit worktree `/scratch/code/shibboleth/tk-worktrees/f684-686` on branch
`f684-686` off `origin/master`, one commit per follow-up.

## Gates you must actually run

- ms: `cargo nextest run --locked --workspace`, pinned clippy `-D warnings`
  (put the pinned toolchain's bin first on PATH; the system clippy is newer),
  fmt, and run `--help` to show the new value list.
- toolkit: nextest, pinned clippy, fmt, the help/Examples goldens (regenerate
  in the same commit if they move), `docs/manual` `make audit` with `*_BIN`
  pointed at release binaries in a scratch root
  (`scripts/install.sh --no-gui --no-man --root <scratch>`), and a real
  `restore --md1` depth-2 run matching the new help text.

Don't push default branches, merge or tag. Scratch under
`/scratch/code/shibboleth/f683-scratch/`; delete with `find … -delete`.

## Deliverable

Commits ending with `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>`.
**As your final action, write your report to
`/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/f683-684-686-impl.md`**
and return a short summary plus the path.
