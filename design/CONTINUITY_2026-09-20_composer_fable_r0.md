# Continuity — composer fable review r0 (5 lenses), 2026-09-20

The operator asked for a 4-agent fable review of the SH2 Wallet Policy
composer, then a 5th lens on Liana/Core importability. All five landed, all
findings folded, every repo pushed with CI green and no bypass.

## DONE and pushed

- **Five lens reports**, agent-written, verbatim, in `design/agent-reports/composer-fable-r0-*.md`:
  funds-safety 1C/3I/2M/1N, nunchuk 0C/2I/3M/2N (libnunchuk 2.1.1 BUILT and run),
  steel-restore 0C/3I/3M/2N, device-flow 0C/2I/6M/0N, liana-core 0C/3I/4M/3N.
  Briefs in `design/agent-briefs/composer-fable-r0-*`.
- **Fold A (Rust primary)** — md-codec **0.45.0**: a policy holds AT MOST ONE
  key-less spend path, refused in `compose::validate()` (two of them always put
  two signature-free arms under one `or_i`; measured over 859 lists, 0
  counterexamples), with vector `compose_refusal_keyless_cap.json` incl. four
  `precedence_*` cases — the cap yields to `TooManySlots` and
  `LegacyWrapperShape`, whose remedies its own would not satisfy. md-cli
  **0.17.0**: host seating fills an ABSENT slot fingerprint from the seated
  card (a host-completed partial template used to render that key with NO
  origin). Whole-diff review 0C/1I/3M/3N folded, verified 7/7.
- **Fold B (fork)** — 12 items: the Critical (key-less cap ported), same KEY at
  two slots refused by material not by string, consent names k-of-n on a locked
  multi path, §8a says a key-less path makes the WHOLE wallet un-importable,
  §8f no longer claims Nunchuk imports NUMS, §8w mixed lock bases, the key-order
  picker opens on the setting in force, Back on the passphrase keyboard re-asks,
  the restore document derives addresses for all 22 keyed shapes, four Minors,
  the emptied path, and a testnet `tpub` refused at BOTH doors. Review
  0C/2I/5M/4N → r1 fold → verification 10/11 + residue closed.
- **Lens 5 fold** — one consent notice **"OUTSIDE LIANA'S MODEL"** naming the
  first of nine classes in Liana 8.0's own order; verified 55/56 shapes fire
  iff Liana refused. Spec **§8x** mirrors it.
- **Host-first** `me sysw pack` refuses a testnet key (`1cbecbfd`); the shared
  `record_class_vectors.json` is 82 rows, sha256 `c5f72145…`.
- **Spec** §4e (second key-less path), §5c (Nunchuk refuses raw NUMS — measured
  0 of 7), §6a (mainnet version rule), §8a/§8f/§8m/§8w/§8x, §13 items 2/3/4
  replaced with measurements.
- **`design/IMPORTABILITY_composer_shapes.md`** — which shapes import into which
  wallets, GENERATED by `scripts/importability-matrix.py` from
  `design/evidence/composer-fable-r0/` (Nunchuk 20/30 measured, Liana 17/56,
  Core 25 39/56, Core 31.1 54/56).
- **Releases**: toolkit **0.103.2** (all five archives; 0.103.0/0.103.1 shipped
  no x86_64 musl — `man-pages.yml` now reads md-codec's pin ONCE from Cargo.toml
  and both build paths share one `[source]` list). md-cli **0.17.0** released.
  `/SH2` deployed at md 0.17.0 / mnemonic 0.103.2, every link fetched 200.

## OPEN

- **F-630 (the one with correctness weight)** — md-codec 0.44.0's xpub-header
  rewrite is UNPORTED in the fork, and `TestKeyedConformanceAgreesWithRust`
  parses `.chains[].descriptor` into a field it never asserts, so a re-vendor
  imports the change in SILENCE and stays green. Fork corpus is pinned at dm
  `745e0fd0`. Port it, assert `.chains[].descriptor` in the same change, and
  carry md-codec 0.45.0's precedence cases across at the same time.
- F-624/625/626/629 Nunchuk-facing docs (F-629: the Nunchuk AND Liana operator
  runbooks are stranded in review reports; they belong in `demo/sh2/WALKS.md`).
- F-627 `h` vs `'` restore-doc checksum; F-628 lock echo grammar;
  F-631 §8x class 5/6 order has no test.
- Toolkit `design/FOLLOWUPS.md`: doc-build drift (3 undocumented flags, 18
  drifted transcripts — honest red once the pins matched install.sh),
  `manual-gui.yml`'s split-line md-cli 0.11.0 pin, restore cross-checks one
  slot per seed, inspect prints stub COUNT not values.

## Operator's, device-gated

Flash `seedhammerii-v0.0.0-bg95716e9.signed.uf2` (in the fork checkout, built
and signature-verified from fork main `95716e97`, sha256 `65c1011a…`):
`sh2-flash /scratch/code/shibboleth/seedhammer/seedhammerii-v0.0.0-bg95716e9.signed.uf2`
— attended, never unattended. Then the highest-value next move is a LIVE
journey walk of the composer with the operator (the lens set is exhausted; the
walk finds what lenses cannot).

## Repo tips (all clean, all pushed, CI green, no bypass)

| repo | tip |
| --- | --- |
| mnemonic-engrave | `49964db1` master |
| descriptor-mnemonic | `b2c5d693` main = md-codec 0.45.0 / md-cli 0.17.0 (tagged, released) |
| mnemonic-toolkit | `53457147` master = 0.103.2 (tagged, released) |
| seedhammer (fork) | `95716e97` main |

Local CLIs rebuilt and current: `md 0.17.0`, `mnemonic 0.103.2`, `ms 0.19.0`,
`mk 0.13.0`, `me 0.10.0`.

## RESUME POINT

1. **F-630, the Go-port header sync.** In the fork: add an assertion on
   `.chains[].descriptor` to `TestKeyedConformanceAgreesWithRust` FIRST and
   watch it go RED on a re-vendor (that is the gate the finding is about),
   re-vendor the `keyed_compose_*.conformance.json` corpus from
   descriptor-mnemonic `b2c5d693`, port md-codec 0.44.0's "a rendered xpub's
   header agrees with its origin" rule into `md/`, and bring md-codec 0.45.0's
   four `precedence_*` vector cases with it. Rust-primary rule: the Go port is
   downstream and never leads. Gate: `go vet` (ArtifactDir baseline only),
   `gofmt -l .` (five-file baseline), `./md/ ./sysw/ ./mk/`, the whole gui via
   `scripts/gui-shard-test.sh ./gui/ 24`, firmware size. Push via
   `scripts/push-via-staging.sh` with main FROZEN for the window.
2. Then the docs batch: F-629 (move both operator runbooks into
   `demo/sh2/WALKS.md`), F-624/626/627/628, F-631's one fixture.
3. Toolkit doc-build drift is its own cycle (regenerate transcripts, document
   the three flags) — do NOT make it green by re-pinning older tools.

**Standing:** the operator's directive for this cycle was "proceed
autonomously"; pushes/releases without asking, the flash never unattended.
