# T7b IMPLEMENTATION PLAN — R0 review (round 1) — VERBATIM agent report

**Agent:** `ad7597946fa3bd54c` (adversarial opus architect). **Fork HEAD:** `82d46b3`. **Plan commit:** `f583e8b`. **Date:** 2026-06-19.
**Verdict:** GREEN (0C/0I). The round-0 fold (test-scaffolding rebuild) cleanly resolved both Importants + 3 Minors with no drift to verified-correct content. 2 residual non-blocking Minors. Cleared for single-implementer TDD.

---

# T7b IMPLEMENTATION PLAN — R0 review (round 1)
**Reviewer:** opus architect (adversarial)  **Fork HEAD:** 82d46b3  **Plan commit:** f583e8b  **Verdict:** GREEN (0C/0I)

## Round-0 resolution check
- **I-1 (Task 4) — RESOLVED.** `TestChildSeedWarningAbort` (plan §Task-4, lines 610-637) is now (a) wrapped in `synctest.Test(t, func(t *testing.T){...})` (line 611), (b) keeps the frame handle `frame, quit := runUI(...)` (line 615, NOT `_`), (c) renders the warning via `pumpUntil(frame, "Child Seed", 16)` (line 622) THEN `click(&ctx.Router, Button1)` (line 625) THEN pumps `for i:=0; i<16 && !done; frame()` (lines 627-629) until the goroutine returns, (d) asserts `if !done` (line 631) AND `if got {...}` (line 633) NON-vacuously. It can no longer pass vacuously: if the warning were never reached, `pumpUntil` returns `!ok` and `t.Fatalf` fires (line 623); the `done` guard proves the goroutine ran `childSeedWarning` to completion. Verified against source: `ConfirmWarningScreen.Layout` returns `ConfirmNo` when `cancelBtn` (Button1) `.Clicked` (gui.go:320-323), so Button1 drives the abort path. `pumpUntil` (slip39_polish_test.go:329) and the precedent `TestDescriptorAddressFlowBackExits` (address_polish_test.go:77-92) confirmed.
- **I-1 (Task 5) — RESOLVED.** `TestBip85DeriveFlow_ScrubsBothMnemonics` (lines 742-823) now mirrors `singlesig_flow_test.go:117-154`: `synctest.Test` wrapper (743), `frame, quit := runUI(...)` keeps the handle (753), installs `bip85SeedHook` capture (745) with `defer ...=nil` teardown (746) — identical to `singleSigSeedHook` usage. It drives the full flow: 12-WORDS via bare `click(Button3)` at default index 0 (763, matches `seedEntryFlow`'s `[]int{12,24}` choice 0 = 12, derive_xpub.go:89), `driveWords(abandonAboutPhrase())` (765), passphrase Skip (767-771), `chooseEntry` ×2 for the param pickers (774-775), warning hold-confirm (777-783), engrave connect/hold/complete (785-798), `click(Button3)` to dismiss → `Engrave` returns true (799), `synctest.Wait()` (800), drain `for i:=0; i<32 && !done; frame()` (803-805). Asserts `if !done` (806), `if master==nil || child==nil` (809 — now reachable because the flow runs to `child = c` then calls the hook synchronously), and the intact zeroed-slice scrub checks for BOTH master (812-816) and child (817-821). The "hook never observed both" guard is genuinely reachable. Engrave-completion drive verified byte-equivalent to `TestEngraveScreen` (gui_test.go:242-275); `newTestEngraveScreen` ultimately calls the SAME `NewEngraveScreen(ctx, plate)` the flow uses (gui_test.go:153-156, gui.go:2492), so the connect/hold/complete dance is plate-agnostic.
- **m1 — RESOLVED.** `chooseWords`/`clickChoose` are no longer defined or called anywhere; they appear ONLY in negative notes ("No new `chooseWords`/`clickChoose` wrappers are needed", line 826; "no invented", line 1336). Task 5 uses the shipped `chooseEntry(frame, &ctx.Router, down)` (derive_xpub_test.go:21, confirmed signature + frame-pumping body).
- **m2 — RESOLVED.** The struck `defer bip85SeedHook(...)` block is GONE. The production call is a single synchronous `if bip85SeedHook != nil { bip85SeedHook(master, child) }` right after `child = c` (lines 900-907), with exactly ONE top-level scrub `defer` registered first (lines 872-879, LIFO → runs last). The only hook-related `defer` is the test-side teardown `defer func(){ bip85SeedHook=nil }()` (line 746), identical to singlesig_flow_test.go:121. Mirrors `singleSigSeedHook` (singlesig.go:36-45) exactly.
- **m3 — RESOLVED.** Step 6.6 / file-list (lines 968, 1184-1192) now states only `TestEngraveSingleSigLeftWrap` repoints for singlesig. Verified against source: in `singlesig_program_test.go`, `TestEngraveSingleSigLeftWrap` asserts `"Multisig"` at line 80 (BREAKS → repoint), while `TestEngraveSingleSigProgramNavigable` stops at Multisig position 4 (line 59, stays green). `bundle_program_test.go`/`derive_xpub_program_test.go` are comment-only.

## New findings (this round)
## Critical
None.
## Important
None.
## Minor
- (m4, non-blocking) Step 6.6 (line 1192) cites approximate comment line numbers ("lines ~30-33", "lines ~44-47") for the comment-only repoints in derive_xpub/bundle program tests. Source confirms the comments are at derive_xpub_program_test.go:30-33 and bundle_program_test.go:44-47 — accurate; the "~" is just hedging. No action required.
- (m5, non-blocking) The Task-5 button-state subtlety (lines 828-830) is correctly identified and the shipped click sequence (785) does NOT pre-emptively insert the conditional release `click(&ctx.Router, Button3)` — it leaves it as a documented conditional fallback. This is the correct disposition for a TDD plan: the load-bearing scrub assertion is pinned, and the exact click count to reach the engrave `engraveIdle` step is a mechanical red→green alignment the implementer resolves. I independently traced the router mechanics (`EventRouter.Next` consumes only head-of-queue matching events; `Reset` discards non-matching head events but keeps matching ones; the warning's `press(Button3)` is fully consumed by the discarded warning screen's `confirmBtn`; the engrave screen's `selectBtn` is a FRESH Clickable with `Pressed=false`) and found NO leftover Button3 event in the queue when the engrave screen opens — so the documented fallback is a safety note, not a latent hang. Not a blocker.

## Verified-correct (incl. confirmation the fold left goldens/derive/fp/picker/lockstep unchanged)
- **Goldens UNCHANGED.** All pinned values byte-identical to the round-0-verified set: master fp `73c5da0a`; 12'/0' ent `ac98dac5d4f4ebad6056682ac95eb9ad` → `prosper short ramp…fold` fp `02e8bff2`; 18'/0' fp `3bb5fd0c`; 24'/0' fp `c2f2dd51`; idx1 `sing slogan bar…desert`; idx9 `earth ice square…bubble`; canonical `6250b68d…` → `girl mad pet…nose`. (Did NOT re-run the probe per scope; confirmed the strings are untouched in the plan.)
- **Derive algorithm UNCHANGED.** Fully-hardened path `[]uint32{bip85.PathRoot, 39+h, 0+h, words+h, index+h}` (lines 291-297); `entLen := (words*11 - words/3) / 8` (line 321); LEADING `hmacOut[:entLen]` (line 326). Signatures re-confirmed live: `bip85.Entropy` panics if `len!=32`, `PathRoot=83696968+0x80000000` (bip85.go:11,16-18); `bip39.New` bounds `16≤len≤32, len%4==0` (bip39.go:228-233) — the entLen guard (322-325) keeps it un-panicable.
- **Child-fp UNCHANGED.** `masterFingerprintFor(child, &chaincfg.MainNetParams, "")` (line 437), two-value err propagated; gui.go:485 signature confirmed; pinned `0x02e8bff2 != master 0x73c5da0a`.
- **Picker bounds UNCHANGED.** `bip85WordChoices=[12 18 24]`, `bip85IndexChoices=[0..9]`, app fixed BIP-39 (lines 534-535).
- **8 lockstep sites UNCHANGED + ACCURATE @ 82d46b3.** enum gui.go:147-154 (qaProgram :153); dispatch gui.go:1492-1514; left-wrap :1640-1643; right-wrap :1644-1652 (`>engraveMultisig`); title :1666-1678; `npage:=int(engraveMultisig)+1` :1852; `layoutMainPlates` case + mandatory `panic("invalid page")` :1860-1867; `npages` :1871. All keyed on `engraveMultisig`; plan repoints all to `bip85Derive` inserted before `qaProgram`. The 2 new nav-tests pump `frame()` after each `click`.
- **All cited test-harness helpers/signatures confirmed live:** `synctest.Test`/`synctest.Wait` (testing/synctest); `runUI` returns `(frame func()(string,bool), close func())` (gui_test.go:467); `pumpUntil` (slip39_polish_test.go:329); `frameUntil` (address_polish_test.go:31); `driveWords` (seedxor_polish_test.go:24); `abandonAboutPhrase` (derive_xpub_test.go:14); `chooseEntry(frame,r,down)` (derive_xpub_test.go:21); `click`/`press(r,...Button)` (event_test.go:42,57); `confirmDelay = 1*time.Second` (gui.go:269); `ConfirmWarningScreen.Layout → ConfirmNo` on Button1 (gui.go:318-323); engrave dance `newEngraver`/`p.engraver`/`<-e.closes`/`p.wakeups` (gui_test.go:459,242-275); `newTestEngraveScreen`→`NewEngraveScreen` (gui_test.go:116-156, gui.go:2492); `wipeBytes`/`showError` (slip39_polish.go:330,22); `engraveSeed`/`masterFingerprintFor`/`seedEntryFlow`/`ChoiceScreen.Choose` all present at cited lines.
- **`childSeedWarning` impl** (lines 656-676) is a byte-faithful copy of `stubZeroWarning` (derive_xpub.go:237-256). The Task-4/Task-5 import lists name everything used: `testing/synctest`, `time`, `confirmDelay`, `press`, `chooseEntry`, `newEngraver`, `pumpUntil`.
- **Spec coverage I-1..I-8 fully mapped** (Self-Review §1) — intact after the rewrite.

## Bottom line
**GREEN — 0 Critical, 0 Important.** The fold cleanly resolved both round-0 Importants and all three Minors without touching any verified-correct content. Confirmed by source diff-of-intent: the goldens, derive algorithm, child-fp call, picker bounds, and all 8 lockstep sites are byte-unchanged; only the two UI-harness tests (Tasks 4/5) and the m1/m2/m3 prose were rewritten. Both rebuilt tests are now genuinely functional and non-vacuous. Every cited helper, signature, line number, and precedent was verified against the live fork at HEAD 82d46b3. The two residual minors (m4 approximate comment line refs, m5 documented conditional button-release fallback) are polish and do not block. **This clears T7b for implementation.**
