# Push report — 2026-08-15c

## Pre-flight verification

```
$ git log --oneline origin/master..HEAD
09cdde8 reports: persist the S2-green push report verbatim
```

Exactly one unpushed commit, as expected.

```
$ git status --porcelain
```
(no output — exit 0, clean tree)

## Commit pushed

- `09cdde8` — `reports: persist the S2-green push report verbatim`
- Full SHA: `09cdde8d55ddb443ae507a4e737d19d9eff661d8`

## Procedure executed

### 1. Stage the SHA

```
$ git push origin master:refs/heads/ci/staging
remote:
remote: Create a pull request for 'ci/staging' on GitHub by visiting:
remote:      https://github.com/bg002h/mnemonic-engrave/pull/new/ci/staging
remote:
To github.com:bg002h/mnemonic-engrave.git
 * [new branch]      master -> ci/staging
```

### 2. Locate and watch the run

`gh run list --limit 5 --repo bg002h/mnemonic-engrave` showed the triggered run:

```
in_progress   reports: persist the S2-green push report verbatim   release   ci/staging   push   31898578116   7s   2026-08-15T17:32:04Z
```

Watched via `gh run watch 31898578116 --repo bg002h/mnemonic-engrave --exit-status` to completion, then confirmed via `gh run view`:

```
$ gh run view 31898578116 --repo bg002h/mnemonic-engrave --json status,conclusion,url,headSha
{"conclusion":"success","headSha":"09cdde8d55ddb443ae507a4e737d19d9eff661d8","status":"completed","url":"https://github.com/bg002h/mnemonic-engrave/actions/runs/31898578116"}
```

Confirmed the specific required check job:

```
$ gh run view 31898578116 --repo bg002h/mnemonic-engrave --json jobs -q '.jobs[] | select(.name=="test (rust + go)") | {name, status, conclusion}'
{"conclusion":"success","name":"test (rust + go)","status":"completed"}
```

**CI run URL:** https://github.com/bg002h/mnemonic-engrave/actions/runs/31898578116
**Conclusion:** success (whole run and the `test (rust + go)` job specifically)

### 3. Push master (the check now belongs to this SHA)

```
$ git push origin master
To github.com:bg002h/mnemonic-engrave.git
   769ebeb..09cdde8  master -> master
```

Exit code: 0. **No "Bypassed rule violations" message appeared** — the required `test (rust + go)` context was satisfied, not bypassed.

### 4. Clean up staging branch

```
$ git push origin --delete ci/staging
To github.com:bg002h/mnemonic-engrave.git
 - [deleted]         ci/staging
```

## Final verification

```
$ git status --porcelain
```
(no output — exit 0, clean)

```
$ git log --oneline origin/master..HEAD
```
(no output — exit 0, nothing ahead)

```
$ git rev-parse origin/master
09cdde8d55ddb443ae507a4e737d19d9eff661d8
$ git rev-parse master
09cdde8d55ddb443ae507a4e737d19d9eff661d8
```

Local and remote `master` are identical. `ci/staging` deleted.

## Result

**SUCCESS.** Commit `09cdde8` is on `origin/master`, backed by a passing `test (rust + go)` check earned via the `ci/staging` staging procedure. No bypass occurred.
