# Composer S3 plan -- Task A10 fold report

**Artifact:** `design/IMPLEMENTATION_PLAN_composer_S3_fork_gui.md`, Task A10 only
(plan lines 4796-4908 after the fold; 4796-4862 before). Repo tip at start:
`9081222`. **Nothing outside Task A10 was edited**, and no commit was made.

Input: `fold-brief-s3-a10.md` (controller) + `s3-a10-fold-notes.md` items 1-4,
which derive from `design/agent-reports/composer-S0b-plan-R0-r0-fidelity.md`
(I-1, I-2, I-4).

`git diff -U0` hunk ranges, all inside the task: 4796, 4798, 4802-4806,
4805-4812, 4807-4815, 4810-4818, 4812-4830, 4815-4835, 4818-4842, 4822-4846,
4824-4853, 4828-4857, 4832-4861, 4838-4868, 4845-4879, 4851-4885, 4853-4889,
4858-4895.

---

## Item 4 -- precondition names S0b, and the pending fact has an owner

**Old (heading + first paragraph):**

> ### Task A10: the five presets (§4d) -- BLOCKED on F-453, and the only task that is
>
> **PRECONDITION, hard.** `design/FOLLOWUPS.md` F-453 (`composer-preset-vectors-missing`) owns the Rust half to this stage: `md compose --preset <name>` plus one exported vector per archetype in descriptor-mnemonic, FIRST. At `66bdf2f4`, `md compose --help` shows `--wrapper`, `--path`, `--experimental`, `--json` and no `--preset`, and the corpus carries no preset entry. [...] Until F-453's Rust half has shipped, this task does not start [...]

**New:** the heading is `-- BLOCKED on F-453's Rust half (plan S0b), and the only
task that is`, and the paragraph now names the mini-plan by path
(`design/IMPLEMENTATION_PLAN_composer_S0b_presets.md`, R0 GREEN), its branch
(`composer-s0b`), its state when written (under whole-diff review, NOT on
descriptor-mnemonic `main`), and makes the block condition "until S0b has merged
to descriptor-mnemonic `main`" rather than the vaguer "until F-453's Rust half
has shipped". The `66bdf2f4` evidence sentence is kept and re-framed as what
`main` shows today.

A new paragraph and blockquote were added for the one genuinely pending fact --
no placeholder, an owner and a time instead:

> **The one fact this task cannot carry yet is S0b's merge commit, and it is not a placeholder for the implementer to fill.** The controller records it on the line below, on the day S0b merges to descriptor-mnemonic `main`. The implementer READS it and the precondition step below stops if the checkout is not at it; until that line names a SHA, this task is not dispatchable.
>
> > **S0b merge commit on descriptor-mnemonic `main`:** not yet landed. Recorded here by the controller when S0b merges.

The vendoring step was rewritten to build `md` by path from descriptor-mnemonic
`main` at that commit, because a bare `md` on `PATH` may be a stale install:

```bash
git -C /scratch/code/shibboleth/descriptor-mnemonic rev-parse main
cargo build -q --manifest-path /scratch/code/shibboleth/descriptor-mnemonic/Cargo.toml -p md-cli
/scratch/code/shibboleth/descriptor-mnemonic/target/debug/md compose --help 2>&1 \
  | grep -q -- --preset && echo PRESET-PRESENT || echo PRESET-ABSENT
ls /scratch/code/shibboleth/descriptor-mnemonic/crates/md-codec/tests/vectors/ \
  | grep -c '^keyed_compose_preset_'
```

**One deliberate departure from the old text, stated because it changes an
Expected line.** The old probe was `md compose --help 2>&1 | grep -c -- --preset`
with `Expected: 1`. That expectation is wrong once S0b lands: `--preset` appears
on more than one help line, because the `--path` option's own doc comment names
it as mutually exclusive (`crates/md-cli/src/main.rs:289` in the S0b worktree,
"exclusive with --preset."). A count of 1 would fail on a correct build. The
probe is now presence (`PRESET-PRESENT` / `PRESET-ABSENT`) and the reason is in
the Expected line. Package/binary names verified in the S0b worktree:
package `md-cli`, binary `md`.

## Item 3 -- fork pin-test ownership (the "no edit needed" claim was wrong)

**Old (Files bullet):**

> - Modify: `md/testdata/compose_vectors.provenance.json` (re-pinned by `scripts/vendor-compose-vectors.sh`, which already globs `compose_*`; the preset vectors are exported under that prefix so no script change is needed -- CONFIRM that when F-453 lands, and if they are exported under `preset_*` instead, widen the script's `grep -E` and the pin test's `isComposeVectorFile` together)

**New:** the provenance bullet keeps only "regenerated by
`scripts/vendor-compose-vectors.sh`, never edited by hand", and two Modify
bullets were added:

> - Modify: `md/compose_vectors_pin_test.go` -- **this task OWNS the pin-test edit; an earlier draft of it said no such edit was needed, and that was wrong.** Two hand-maintained constants move together: `composeVectorNames` (`md/compose_vectors_pin_test.go:39`) holds 26 names and gains the six `keyed_compose_preset_*` names, 26 -> 32; the file-count assertion with its arithmetic comment (`md/compose_vectors_pin_test.go:82-84`, "22 keyed vectors carry five files, 4 unkeyed carry four: 126") becomes 28 keyed x 5 + 4 unkeyed x 4 = **156**. `isComposeVectorFile` (`md/compose_vectors_pin_test.go:57`) needs NO edit -- it already accepts anything prefixed `keyed_compose_`.
> - Modify: `scripts/vendor-compose-vectors.sh` -- one line only: the generated pin's own `_comment` at `scripts/vendor-compose-vectors.sh:29` names `126` in prose and would otherwise ship a stale instruction into every regenerated pin. **The script's SELECTION needs no change** (next paragraph).

The CONFIRM clause is resolved in its own paragraph:
`scripts/vendor-compose-vectors.sh:16` selects `^(keyed_)?compose_`, the exports
are `keyed_compose_preset_<archetype>`, so selection and `isComposeVectorFile`
both stand. The `preset_*` fallback is dropped. The vendored-file bullet now
names the real files:
`md/testdata/vectors/keyed_compose_preset_<archetype>.{bytes.hex,conformance.json,descriptor.json,phrase.txt,template}`,
six archetypes in snake_case, 30 files -- replacing the old, wrong
`md/testdata/vectors/preset_*.{bytes.hex,phrase.txt,descriptor.json,template}`.

**Two things the brief did not name, found while machine-checking and folded in
(both inside A10's own file set):**

1. `scripts/vendor-compose-vectors.sh:29` -- the script writes the pin's
   `_comment`, which says "if the file count is not 126". The brief said the
   script needs no change; that is true of the SELECTION (line 16), and the
   report states it that way, but the `_comment` literal is a second `126` that
   goes stale on the same day. A10 now owns it, and it is in the commit step's
   `git add`.
2. `TestEveryKeyedComposeVectorHasAConformanceRecord`
   (`md/compose_vectors_pin_test.go`) requires a `.conformance.json` per keyed
   vector. Checked: S0b exports one per preset, so this passes untouched -- and
   it is the reason each vendored vector is five files, not four. Stated in the
   plan so a reader does not have to re-derive it.
   `md/conformance_keyed_test.go` globs rather than counting, so it needs
   nothing either (checked: no hardcoded count in that file).

Counts stated in the plan, all measured, not inferred:
pin today `files: 126, vectors: 26` (read out of
`md/testdata/compose_vectors.provenance.json`); fork `md/testdata/vectors`
holds 277 files today, 110 of them `keyed_compose*`; the S0b worktree's
`crates/md-codec/tests/vectors/` holds 287 files, 30 of them
`keyed_compose_preset_*`. After vendoring: 156 files / 32 vectors in the pin,
307 files in the directory. The first re-run's failure message quoted in the
plan is the real one from `md/compose_vectors_pin_test.go`: `pin says 32
vectors, this test knows 26`.

## Item 2 -- wrapper coverage: offering is not pinning

**Old (vector test bullet):**

> - `TestComposerPresetsReproduceTheirVendoredVectors`: for each of the six presets `composerPresets(md.ComposeWsh)` returns, `md.Compose(p.list)` then `Chunks()` equals the vendored `md/testdata/vectors/preset_<name>.phrase.txt` chunk for chunk. [...]

**New:** §4d's OFFERING sentence is kept verbatim in substance and marked
unchanged by S0b ("all six under `wsh` and `tr`; under `sh`/`sh(wsh)`, plain
k-of-n alone"). A new paragraph plus a three-row table splits the twelve offered
(wrapper, preset) pairs into six pinned and six unpinned:

| (wrapper, preset) pairs | how the test checks them |
| --- | --- |
| `wsh` x plain-multisig, simple-timelocked-inheritance, tiered-recovery, hashlock-gated, decaying-multisig | PINNED: chunks compared against the vendored vector |
| `tr` x kofn-recovery | PINNED: chunks compared against the vendored vector |
| `tr` x the other five, and `wsh` x kofn-recovery | STRUCTURAL only, and named UNPINNED in the test itself |

followed by the rule the notes asked for, in the plan's voice: a `tr` vector's
chunks are never compared against a `wsh` preset's, because "the wrapper is half
of an assertion's identity here, and a table iterated at one wrapper against
vectors exported at another is a test that passes by comparing the wrong things".

The test specification is now concrete on all three things the brief asked for:

- **How many sub-tests, named how.** Six under
  `TestComposerPresetsReproduceTheirVendoredVectors`, named `<wrapper>/<preset>`
  and enumerated (`wsh/plain-multisig`, `wsh/simple-timelocked-inheritance`,
  `wsh/tiered-recovery`, `wsh/hashlock-gated`, `wsh/decaying-multisig`,
  `tr/kofn-recovery`); six under a new
  `TestComposerPresetsWithoutAVectorAreStructurallyValid`, also enumerated. The
  run step's Expected line moved from "two top-level PASS with six sub-tests" to
  "three top-level PASS", six sub-tests under each of the first two.
- **What a missing vector prints.**
  `t.Fatalf("INCONCLUSIVE: no vendored vector at %s -- re-run scripts/vendor-compose-vectors.sh (F-453/S0b)", path)`
  -- never a skip, never a pass.
- **What the structural test may and may not claim.** It asserts only
  `md.ValidatePathList` acceptance and a non-empty `md.Compose(...).Chunks()`,
  and its doc comment says the pairs are unpinned and why, "so a green run is
  never read as pinning twelve shapes"; a pair moves into the pinned test when a
  vector for it exists.

The vector path was also corrected to the form a `gui` package test actually
needs: `../md/testdata/vectors/...` (precedent:
`gui/template_engrave_test.go:207`). The old text omitted the `../`.

## Item 1 -- defaults are S0b's decision, not an inherited accident

**Old:** the rule existed only as "**Do not invent a shape here**" with no
statement of where the numbers come from or that anyone chose them.

**New (added after that rule, which is kept):**

> **The vectors' parameters ARE the device's offered defaults -- S0b decided that, so this table is not inheriting a test corpus's accident.** The ruling: the smallest legal shape of each archetype is the honest starter on a device whose only editor is a scroll wheel, and a wider tier is one edit away while a shape the operator did not intend is not. So the defaults are the vector parameters verbatim -- `plain-multisig` 2-of-3; `simple-timelocked-inheritance` `older(26280)`; `kofn-recovery` 2-of-3 with `older(26280)`; `tiered-recovery` 2-of-2 then 1-of-2 with `older(26280)`; `hashlock-gated` `older(26280)` and the digest read off `keyed_compose_preset_hashlock_gated.template`, never typed from memory; `decaying-multisig` 2-of-2 at `older(13140)`, then 1-of-1 at `older(26280)`, then one key at `after(1000000)`.

Every parameter above was read from the S0b MANIFEST rows
(`crates/md-codec/tests/compose_support.rs`, the preset block) and cross-checked
against the six `.template` files in the S0b worktree. The hashlock digest is
deliberately NOT reproduced in the plan as a literal: the plan points at the
`.template` file instead, per the brief's "never from memory". (For this
report's record only, the row's `HH` is
`a8a8a8a8a8a8a8a8a8a8a8a8a8a8a8a8a8a8a8a8a8a8a8a8a8a8a8a8a8a8a8a8`, i.e.
`H = [0xa8; 32]` at `compose_support.rs:27-28`, and it appears verbatim in
`keyed_compose_preset_hashlock_gated.template`.)

A second paragraph was added so the one-table-two-wrappers claim is not a guess:

> **The two wrappers share one table because the origins are already wrapper-derived.** `md.DefaultOrigin(w, account)` (`md/compose.go:288`) is `m/48'/0'/<account>'/<script-type>'`, and `ComposeWrapper.ScriptType()` (`md/compose.go:42`) supplies 2 for `wsh` and 3 for `tr` -- which is exactly the difference between the five `wsh` templates' `48'/0'/<i>'/2'` and the `tr` template's `48'/0'/<i>'/3'`. So each archetype is constructed once, at the wrapper it is asked for, and the unpinned pairs get the wrapper's own origins rather than a `wsh` shape wearing a `tr` label. Do not hardcode either origin.

This closes a trap the narrowing would otherwise have opened: with five vectors
at `wsh` and one at `tr`, the obvious way to fill the unpinned pairs is to copy
the `wsh` path list, which would be WRONG -- the exported templates differ in
the BIP-48 script-type component, and `md.DefaultOrigin` already handles it.

## Commit step

`git add` gained `md/compose_vectors_pin_test.go` and
`scripts/vendor-compose-vectors.sh`. The commit message's subject changed from
"the five archetype presets" to "the six archetype presets" (the task offers six:
five toolkit archetypes plus plain k-of-n), and a paragraph was added recording
the 6-pinned / 6-unpinned split and the 126 -> 156 / 26 -> 32 corpus move.

**Not changed, deliberately:** the task's own HEADING still reads "the five
presets (§4d)". "The five toolkit archetypes plus plain k-of-n" is the plan's
established phrasing (it appears in the body and at plan line 39), the heading is
referenced elsewhere only as "Task A10", and renaming it is outside the four
folded items.

---

## Machine checks (run from `/scratch/code/shibboleth/mnemonic-engrave`, after the fold)

```
$ ./scripts/plan-glyph-check.sh design/IMPLEMENTATION_PLAN_composer_S3_fork_gui.md
─── operator strings scanned: 306 ; undrawable: 0            exit=0

$ ./scripts/plan-table-check.sh design/IMPLEMENTATION_PLAN_composer_S3_fork_gui.md
─── table rows checked: 129 ; malformed: 0                   exit=0

$ ./scripts/plan-stepref-check.sh design/IMPLEMENTATION_PLAN_composer_S3_fork_gui.md
─── step numbers in prose: 0  (the TABLE is exempt; it IS the ordering)   exit=0

$ CITE_FORK_ROOT=/scratch/code/shibboleth/seedhammer \
    ./scripts/plan-cite-check.sh design/IMPLEMENTATION_PLAN_composer_S3_fork_gui.md
─── citations resolved: 249 / 249 ; dangling: 0 ; ambiguous: 0            exit=0
```

Every citation this fold ADDED was read back off the gate's own output rather
than trusted:

```
ok  md/compose.go:288                    // DefaultOrigin is §4f's m/48'/0'/<account>'/<script-type>'.
ok  md/compose.go:42                     func (w ComposeWrapper) ScriptType() uint32 {
ok  md/compose_vectors_pin_test.go:39    var composeVectorNames = []string{
ok  md/compose_vectors_pin_test.go:57    func isComposeVectorFile(name string) bool {
ok  md/compose_vectors_pin_test.go:82    // 22 keyed vectors carry five files, 4 unkeyed carry four: 126.
ok  md/compose_vectors_pin_test.go:84    t.Fatalf("pin lists %d files, want 126", len(p.Files))
ok  scripts/vendor-compose-vectors.sh:16 mapfile -t files < <(cd "$VEC" && ls | grep -E '^(keyed_)?compose_' | sort)
ok  scripts/vendor-compose-vectors.sh:29 "if the file count is not 126, or if the vector names drift from its list.",
```

That read-back caught one of my own errors before it shipped: I first wrote
`md/compose.go:41` for `ScriptType()`, which the gate resolved to the line ABOVE
it (a comment) and still printed `ok`. Corrected to `:42`. This is exactly the
gate's documented blind spot -- a stale-but-plausible line reads identically to a
correct one -- and it is only visible by reading the printed line, not the exit
code.

No `.jsonl` file was read. No commit was made. No file other than
`design/IMPLEMENTATION_PLAN_composer_S3_fork_gui.md` (and this report) was
touched.

## Unresolved / for the controller

1. **The S0b merge commit is still a hole in A10, by design.** The plan says the
   controller writes it into the blockquote when S0b merges to
   descriptor-mnemonic `main`, and that the task is not dispatchable until then.
   Nothing else in A10 is pending.
2. **Two counts in A10 are predictions, not measurements**, and are labelled as
   Expected lines rather than facts: `vendored 156 files, 32 vectors` from the
   script, and `md/testdata/vectors` at 307 files. They follow arithmetically
   from measured inputs (126 + 30, 26 + 6, 277 + 30) and from the script's own
   `print("vendored %d files, %d vectors, primary %s")`, but they cannot be
   executed until S0b lands and the script is re-run. If S0b's export set changes
   before it merges, these three numbers and the `156` in two files move together.
3. **A10's heading still says "the five presets"** while the task offers six.
   Left alone as out of scope (see above); flagging it in case the controller
   wants it in a separate, non-fold edit.
