# T6b SPEC — R0 review (round 1) — VERBATIM agent report

**Agent:** `a88cba78d14313c30` (adversarial opus architect). **Fork HEAD:** `072461a`. **Spec commit:** `8f4eb76`. **Date:** 2026-06-19.
**Verdict:** GREEN (0C/0I). Persisted per the R0 gate discipline. Two new Minors (n-1, n-2) are non-blocking; folded post-GREEN as cosmetic accuracy improvements.

---

# T6b SPEC — R0 review (round 1)
**Reviewer:** opus architect (adversarial)  **Fork HEAD:** 072461a  **Spec commit:** 8f4eb76  **Verdict:** GREEN (0C/0I)

## Round-0 resolution check

- **I-1 (extractSuppliedMd1) — RESOLVED.** The spec now (a) DEFINES a net-new `extractSuppliedMd1(cards) ([]string, ok)` with explicit failure modes — exactly-one cardMD1 → return verbatim strings; 0 md1 → refuse; ≥2 md1 → refuse; any cardMK1/cardMS1 → refuse (§2 L14); (b) LISTS it in §3 NET-NEW (L28); (c) has a §5.1 acceptance test (L38); (d) adds invariant I-11 (L57) and explicitly states `singleSigReadbackCards` is NOT reused. The cited reuse problem is real and correctly avoided: `singleSigReadbackCards` (`gui/singlesig_verify.go:38`) returns false unless BOTH mk1 AND md1 are present — the opposite of a one-md1/zero-mk1 supply. Source check of what the extractor needs: `bundleGatherFlow` returns `[]bundleCard` (`gui/bundle_flow.go:95`); `bundleCard{kind,label,strings,summary}` with `bundleCardKind{cardMK1,cardMD1,cardMS1}` (`gui/bundle.go:24-38`) — all present and the chunked-md1 path collapses one csid into ONE `cardMD1` (`gui/bundle.go:217-246`), so a legitimately-chunked supply is NOT falsely rejected. (See New-Minor n-1 on the cardMS1 sub-branch.)

- **I-2 (canonical-pair compare) — RESOLVED.** §2 (L17), §6-I1 (L47) and §7 risk-1 (L60) now all state the compare as `bytes.Equal(cc[:], slot.Xpub[0:32]) && bytes.Equal(pk[:], slot.Xpub[32:65])` and explicitly call out "NEVER `==`" with the `[N]byte == []byte` does-not-compile rationale. Slice-bounds re-derived against source: `slot.Xpub` is `[65]byte` (`md/expand.go:62`), `Xpub[0:32]`=32-byte chain code, `Xpub[32:65]`=33-byte compressed pubkey (byte-split asserted at `md/expand_test.go:162-165`); `decodeXpubBytes` returns `chainCode [32]byte, compressedPubkey [33]byte` (`gui/singlesig_derive.go:99`), so `cc[:]` is 32B and `pk[:]` is 33B. Both `bytes.Equal` calls compile and compare ALL bytes incl. index 64 — all 33 pubkey bytes, exactly as I-2 demanded.

- **I-3 (multisig restore render path) — RESOLVED.** §3 NET-NEW now enumerates "the multisig restore-doc render path (`expandedToDescriptor`→`address`, NOT the single-sig `restoreDocFlow`)" (L28); §2 L21 and §5.5 (L42) describe it; I-6 (L52) states the render path is net-new and `restoreDocFlow` is not reused. Source confirms `restoreDocFlow` (`gui/singlesig_restore.go:118`) takes single-sig scalars (`xpub, masterFP, parentFP, script, path`) and builds a single-sig descriptor via `singleSigRestoreDescriptor` (`:119`) — it cannot accept an `expandedToDescriptor(tpl,keys)` multisig descriptor. Faithful-or-refuse logic correctly stated: `expandedToDescriptor` returns a nil descriptor for non-sortedmulti (`gui/md1_expand.go:36-49`), so the §2 "NO `address.*` call" on the non-bip380 branch makes a wrong-address verify structurally impossible.

- **m-1 (Network label) — RESOLVED.** §2 L18 now sets `Network: "mainnet"` and annotates it as a label only. Source: `mk.Card` HAS a `Network` field (`mk/mk.go:134`); `encodeBytecode` builds `header|stub_count|stubs|fp|path|compact73` and never appends Network (`mk/encode.go:46-95`); `bundle.Verify` compares Fingerprint/Xpub/Path/md1-string/ms1-entropy only, never Network (`bundle/verify.go:52-99`). Label-only claim confirmed.

- **m-2 (9th qaProgram ref) — RESOLVED.** §3 L28 now notes the name-based `qaProgram` ref at `gui/gui.go:1606`. Source confirms `return startScreenAction{prog: qaProgram}, true` (the FOREVERLAURA! debug entry) — name-based, no change needed on insert.

- **m-3 (layoutMainPlates panic) — RESOLVED.** §3 L28 now flags `:1856-1861` as a `panic("invalid page")` site, MANDATORY. Source confirms the switch default at `gui/gui.go:1861` panics; a navigable program missing its case panics on render. (Spec cites the block `:1856-1861`; the panic statement itself is `:1861` — accurate.)

- **m-4 (ambiguous-case wording) — RESOLVED.** §2 L17 and §5.1 (L38) now say "same seed legitimately appears at two cosigner slots under DISTINCT origin paths → first-by-index + notice." Rule is logically sound and deterministic: two slots match iff `derive(seed,origin_k)==xpub_k` for two distinct origins, so policy+stub are identical and only the mk1 Path differs; first-by-index is deterministic.

## New findings (this round)

## Critical
None.

## Important
None.

## Minor
- **n-1 (dead sub-branch / slightly over-stated prose) — `extractSuppliedMd1`'s "refuse any cardMS1" is structurally unreachable.** `bundleGatherFlow`'s accumulator never produces a `cardMS1`: an ms1 is refused at `classify`→`clsMs1Refuse`→`bundleRefusedMs1` and never appended (`gui/bundle.go:64-71,130-131`); only `offerStandaloneMD1`/`offerChunkedMD1`/`offerChunkedMK1` append cards, and they append only `cardMD1`/`cardMK1` (`gui/bundle.go:156,202,240`). So the gathered `cards` slice can contain `cardMD1` and `cardMK1` only. The `cardMK1` refusal IS reachable (a chunked mk1 over NFC accumulates as a `cardMK1`) and meaningful; the `cardMS1` refusal is defensive dead code. Harmless — keeping it is fine — but §2/§5.1/§6-I11 read as if both are live operator-hit failure modes. Suggest a one-clause note ("cardMS1 cannot arise from the gather path — ms1 is refused upstream at classify; the check is defensive"). Does not block.
- **n-2 (residual §1 ambiguity) — §1 L8 "The multisig restore + address derivation are already shipped."** Read in isolation this could imply the whole restore-doc is shipped, which contradicts the net-new render path. It is technically true as scoped (it names `expandedToDescriptor` + `address`, the *primitives*, which ARE shipped and handle sortedmulti), and §2/§3/§5.5/I-6 unambiguously flag the render-path *wiring* as net-new three times, so a careful reader is not misled. Suggest tightening §1 to "the restore *primitives* … are already shipped (the render path is net-new, R0-I3)". Does not block.

## Verified-correct (fresh checks)
- **Lockstep completeness is exhaustive.** A full grep of non-test gui code shows exactly the 8 spec'd `engraveSingleSig` lockstep sites (`gui/gui.go:151,1501-1502,1638,1645,1670,1846,1856,1865`) plus the `engraveSingleSigFlow` def (`gui/singlesig.go:10,30`, untouched) and the 3 qaProgram refs (`:152,1492,1606`). No hidden site. Both wrap bounds (`:1638`,`:1645`) AND both consts `npage`/`npages` (`:1846`,`:1865`) are `int(engraveSingleSig)+1`/`engraveSingleSig`-keyed and must all retarget to `engraveMultisig` — the spec lists all four.
- **Failure-mode severities of the lockstep are correctly classified.** layoutMainPlates default = `panic` (`:1861`, mandatory); title switch = no default → blank string (`:1663-1672`, cosmetic); dispatch switch = no default → falls to `engraveObjectFlow(…, nil)` no-op (`:1491-1515`, functional-but-not-panic). The panic fires on render the instant the carousel reaches the new program, so a missed case is loud, not silent — m-3's "mandatory" classification holds.
- **§5.2 stub assertion is achievable with shipped APIs.** `WalletPolicyIDStubChunks(strs []string) ([4]byte, error)` (`md/walletpolicyid.go:126`); a test can `mk.Decode` the produced mk1, read `card.Stubs [][4]byte`, and compare each against `WalletPolicyIDStubChunks(suppliedMd1)` via `[4]byte == [4]byte` — exactly the pattern `checkStubBinding` uses (`bundle/verify.go:108-114`). "Decoded-field assert not raw-string" is realizable.
- **Scrub spine holds on all early/late refuse paths.** `extractSuppliedMd1` + the full-policy gate refuse BEFORE the seed is typed (§2 ordering) — no secret exists yet, nothing to scrub. After typing, each `deriveAccountXpub` call scrubs its own seed/master/intermediates and serializes the xpub before zeroing the final key (`gui/derive.go:21,35,46-52`); the only cross-loop secret is the mnemonic `[]Word`, scrubbed by the flow's top-level `defer` on every exit incl. no-match/abort, mirroring `gui/singlesig.go:39-45`. The no-match refuse leaves no un-scrubbed secret.
- **Source-cited APIs all match at 072461a:** `ExpandWalletPolicyChunks(strs []string)(Template,[]ExpandedKey,error)` (`md/expand.go:102`); `ExpandedKey{Index,OriginPath,Xpub [65]byte,XpubPresent,Fingerprint}` (`:56-64`); `deriveAccountXpub(m,pp,net,path bip32.Path)` arbitrary path (`gui/derive.go:19,33`); `decodeXpubBytes` fixed-array returns + xprv refusal (`gui/singlesig_derive.go:99-122`); `address.Receive/Change(desc,index)` (`address/address.go:20-26`); `EncodeMS1(entropy []byte)` (`codex32/msencode.go:17`); `m.Valid()`/`Entropy()` panic-on-invalid (`bip39/bip39.go:107,158-161`); full-policy XpubPresent goldens (`md/expand_test.go:100-102` template-only false, `:143-166` full-policy true). No cited file:line was made wrong by the fold.
- **No invariant/gate staleness from the fold.** §7 risk-2's "I-3 invariant" = §6 I-3 (full-policy gate) and "I-11" = §6 I-11 (gather contract) — cross-references consistent, no duplication or contradiction. I-1..I-11 each map to a §5 acceptance test.

## Bottom line
GREEN — 0 Critical / 0 Important. All three round-0 Importants (I-1 net-new extractor with 0/≥2/stray-card refusals; I-2 `bytes.Equal` over the full 32+33 bytes; I-3 net-new multisig restore render path) are genuinely resolved, not hand-waved, and verified against the real 072461a source; the four Minors are folded; the lockstep enumeration is exhaustive and severity-correct; the security spine (canonical-pair cross-match, refuse-on-zero, typed-only seed, per-leg scrub across the loop and on early/late refuse, verbatim engrave, watch-only-aware bundle.Verify, faithful-or-refuse restore) is sound. The two new findings (n-1 unreachable cardMS1 sub-branch, n-2 residual §1 phrasing) are cosmetic and explicitly non-blocking. This spec is ready to advance to the implementation-plan phase (which carries its own R0 gate).
