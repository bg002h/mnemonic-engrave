# Architect blueprint (#10 md encoder, agent aaa2d2ace4061f23b, 2026-06-19) — faithful md1 ENCODER + chunked + me-bundle strategy

Source-verified. **Rust** = `descriptor-mnemonic/crates/md-codec` v0.36.0 + `me-cli` v0.3.0. **Go** = `seedhammer` @ `e4ca173`, module `seedhammer.com`.

**False-consensus guard (verified):** the csid is NOT a direct hash→20-bit projection. Path: `SHA-256(payload)` → **first 16 bytes** (`Md1EncodingId`, identity.rs:39-45) → **top 20 bits MSB-first** (`derive_chunk_set_id`, chunk.rs:175-179). Go `mk top20()` (mk/encode.go:316-319) is the same arithmetic but model the 16-byte id as the intermediate (forward-compat).

## 1. md ENCODER port strategy
Faithful inverse of shipped `md/md.go` decoder, mirroring Rust `encode_payload` (encode.rs:65-92): canonicalize-on-clone → validators → Header(5b)→PathDecl→UseSitePath→write_node→TlvSection → `(bytes, total_bits)`. Go has `bitReader` but **NO `bitWriter`** (md/bits.go reader-only) → first net-new artifact = MSB-first `bitWriter` (byte-exact port of bitstream.rs:11-84) + `reEmitBits` (bitstream.rs:220-230). Throwaway `testBitWriter` (md_test.go:86-117) proves the algorithm — promote it.

**Fidelity traps (cite Rust):**
- (A) MSB-first packing, last byte zero-padded on LOW bits (bitstream.rs:29-69,81-83). Golden `wpkh_basic.bytes.hex=2002001800`.
- (B) Header 5 bits, version in LOW nibble: `(divergent<<4)|(version&0xF)`, v0.30 version=4 (header.rs:27,30-33); common byte 0x20. DO NOT confuse with chunk header (version TOP 4 bits, §2) — source of `WireVersionMismatch{got:2}`.
- (C) `kiw = 32 - leading_zeros(n-1)`, clamped 0 at n∈{0,1} (encode.rs:37-41; Go md.go:842). n=1⟹kiw=0⟹key-arg fields emit ZERO bits (tree.rs:336-346). A stale +1 desyncs every key index.
- (D) Tag 6-bit primary; 0x3F reserved ext (unused v0.30) (tag.rs:140-146; codes tag.rs:99-137 ↔ Go md.go:38-75).
- (E) Node bodies (write_node, tree.rs:79-176), inverse of readNodeDepth (md.go:329-489): KeyArg index@kiw; Children recurse no count; Variable(Thresh) (k-1)5b+(len-1)5b+children, enforce k,n∈1..=32 & k≤n (tree.rs:90-114); MultiKeys (k-1)5b+(len-1)5b+RAW kiw indices not nodes (tree.rs:115-139); Tr is_nums 1b, if !nums key_index@kiw, has_tree 1b, optional subtree — NUMS suppresses kiw entirely (tree.rs:140-159, normalize key_index=0 under NUMS); Timelock 32b; Hash256 32B; Hash160 20B; Empty nil.
- (F) Origin path (origin_path.rs): 4-bit depth max15, per-comp hardened1b+LP4varint; PathDecl (n-1)5b + Shared/Divergent, n∈1..=32. Pins: BIP84=26b, +n-prefix=31b.
- (G) Use-site path (use_site_path.rs:80-96): has_multipath 1b; if set (alt_count-2)3b range2..=9, alts; wildcard_hardened 1b. `<0;1>/*`=16b. Read `hasMultipath` flag, NOT len>0 (md.go:264-268).
- (H) LP4-ext varint (varint.rs:15-42): [L:4][payload:L] L≤14; L=15 escape→[15][low:14][high:L_high], max 29b. **Lengths in BITS not bytes** (identity.rs:503-513 guards this). Go readVarint md.go:141-166.
- (I) TLV (tlv.rs:86-208): per-entry sub-bitstreams → emitted SORTED by tag asc (tlv.rs:200) as [tag:5][varint(bit_len)][reEmitBits(payload,bit_len)]. 4 sparse TLVs (USE_SITE_PATH_OVERRIDES=0x00,FINGERPRINTS=0x01,PUBKEYS=0x02,ORIGIN_PATH_OVERRIDES=0x03). idx strictly ascending (OverrideOrderViolation); empty Some(vec![]) rejected (EmptyTlvEntry); reEmitBits uses tracked bit_len NOT padded bytes; unknown-TLV round-trip re-emit buffered unknowns sorted-by-tag (Go retains tlvUnknown{tag,payload,bitLen} md.go:516-520; re-emit via reEmitBits NOT verbatim — pack md.go:623-654).

Go structure: `md/encode.go` with `encodePayload(*descriptor)([]byte,int,error)` + sub-writers inverting each read*; `md/bits.go` gains bitWriter+reEmitBits; decoder types reused as encoder input.

## 2. Identity + chunked WRITE
`computeEncodingID(d)[16]byte` = SHA-256(encodePayload bytes)[0..16] (identity.rs:39-45) — transitively canonicalizes (trap C in §8). `deriveChunkSetID` = `((b0<<12)|(b1<<4)|(b2>>4))&0xFFFFF` (chunk.rs:175-179); pin AB CD EF→0xABCDE.
ChunkHeader 37b (chunk.rs:32-57): `[version:4][chunked=1:1][csid:20][count-1:6][index:6]` — **version TOP 4 bits**. Guards count∈1..=64, index<count, csid<2^20; version=4.
Split (chunk.rs:235-290): encodePayload→id→csid; threshold SINGLE_STRING_PAYLOAD_BIT_LIMIT=320 (=64*5); chunks_needed=ceil(bytes*8/320), >64→ChunkCountExceedsMax; bytes_per_chunk=ceil(len/count) byte-aligned; per chunk 37b header+bytes@8b; wrap codex32::wrap_payload(bytes,bitcount).
Single vs chunked = TWO entry points (encode.rs:136-139 single via wrap_payload; split always chunked-format). T6 needs both.
codex32 wrap (codex32.rs:67-83): bits_to_symbols (5b, final left-justified low-zero-pad) → regular BCH (13 syms) over hrp_expand("md") → "md"+"1"+data+checksum. Go has MDDataSymbols (decode) but **NO MD checksum-symbol generator** → net-new `codex32.MDChecksumSymbols` (regular only) analogue of shipped MKChecksumSymbols (codex32/mkencode.go:18-55) using mdRegularTargetHi/Lo + mdmkPolymodInitLo (codex32/mdmk.go:39,55-56) + `assembleMD1` mirroring assembleMK1 (mk/encode.go:297-312), regular-only HRP "md1". ValidMD (codex32/mdmk.go:124-127) is the round-trip gate.

## 3. Chunked READ (#10 first half)
Mirror reassemble (chunk.rs:305-389): unwrap per chunk→(bytes, 5×symcount bits); ChunkHeader::read 37b; payload_byte_count=(bits-37)/8; consistency (version/csid/count all-equal→ChunkSetInconsistent 343-350); completeness (len==count→ChunkSetIncomplete; sort; 0..count-1 no gap→ChunkIndexGap 351-367); concat; decode_payload(full,len*8) (TLV rollback ≤7 trailing zero bits tolerates byte-vs-symbol slack, chunk.rs:376 / md.go:549-554); **integrity: computeEncodingID(decoded)→deriveChunkSetID→compare header csid→ChunkSetIdMismatch (378-386)** — why #10 READ needs the full encoder.
Generalize mk1Gatherer→md1Gatherer: net-new public `md.ParseChunkHeader(s)(ChunkHeader,error)` mirroring mk.Header shape (mk/mk.go:48-56); md1Gatherer/md1GatherFlow near-clones of mk ones (gui/mk1_inspect.go:48-83,156-256) calling md.Reassemble; recommend factoring a shared generic gatherer (polish, not fidelity).
**WireVersionMismatch{got:2} quirk — MUST replicate the bit-0 probe:** inspect `syms[0]&1` (chunked flag) FIRST; only call ParseChunkHeader when set (bundle.rs:138-151; Go md.go:1207). Do NOT use ChunkHeader::read-then-catch as primary discriminator.

## 4. Wallet-policy xpub-expansion (#10 second half)
Rust decodes Pubkeys TLV → (@N,[u8;65]) = 32B chaincode||33B compressed pubkey (tlv.rs:30-32,416-429); exposes expand_per_at_n→ExpandedKey (canonicalize.rs:338-470). **Firmware must do the projection itself** onto in-tree `*bip380.Descriptor`. Go decoder elides the secp256k1 point check (md.go:1071-1077 validateXpubBytes is no-op, deferred to #10) → #10 adds PublicKey::from_slice([32..65]) on-curve check (validate.rs:216).
Approach: decode → require is_wallet_policy (pubkeys present&non-empty, encode.rs:50-52); per @N reconstruct xpub from [u8;65]+origin/fp into bip380.Key (carries Network/MasterFingerprint/DerivationPath/Children/KeyData/ChainCode/ParentFingerprint + ExtendedKey(), bip380/bip380.go:28-109); origin from originPathStringFor (md.go:1336-1354); use-site `<0;1>/*`→RangeDerivation+WildcardDerivation. **Project bip380-expressible subset ONLY**: singlesig (wpkh/pkh/tr-keyonly/sh-wpkh) + wsh(sortedmulti) + sh(wsh(sortedmulti)). bip380 has NO unsorted multi/multi_a/sortedmulti_a/taptree/arbitrary miniscript (bip380.go:298-352). Aligns with decoder Renderable classifier (md.go:1237-1293; tr+scripttree non-renderable 1244-1256). Feed projected desc → existing `verifyAddressFlow(ctx,th,desc)` (gui/verify_address.go:22).
**Trap:** md1 multi(unsorted) decodes PolicyMulti (md.go:1283-1293) but has NO faithful bip380 projection → display read-only, EXCLUDE from address-verify (else verify against wrong sorted address). Lock in R0.

## 5. Device-side me bundle faithfulness (later T5)
Refuse ms1 first before validating any (bundle.rs:186-192, exit-3 analogue = on-screen "type ms1 on device never NFC"); per-string PRISTINE non-correcting validation (ValidMD; mk1 reject corrections); group by csid deterministic (bundle.rs:201-223, mismatched csid→separate sets not merged); per-set via md.Reassemble / mk.Decode (243-294); exit-code taxonomy as on-screen outcomes (36-43): integrity SetVerified/BchOnly/Na. T5 orchestrates #10's primitives; only strategy locked now.

## 6. SCOPE BOUNDARY — recommendation: #10 builds COMPLETE encoder + identity + full chunk write/read + xpub-expansion NOW
#10 ships: md/bits.go (bitWriter+reEmitBits); md/encode.go (encodePayload + sub-writers + encode validators + encodeMD1String single + ParseChunkHeader + ChunkHeader.write + split/Encode chunked-write + Reassemble chunked-read+integrity + computeEncodingID + deriveChunkSetID); codex32/mdencode.go (MDChecksumSymbols + assembleMD1); xpub-expansion→bip380 projection + secp256k1 check.
Argument: (1) no shortcut — reassembly integrity IS encodePayload→hash→csid (chunk.rs:378-386), so READ half already needs full encodePayload; (2) chunk WRITE path is tiny incremental (~95 LOC) once encodePayload+identity exist — deferring = re-open + re-gate, MORE throwaway; (3) T6 (md1-from-seed) needs full write path anyway; (4) each cycle gate-able: #10 acceptance = byte-exact .bytes.hex parity + Reassemble round-trip + xpub-expansion address-verify; T5=orchestration; T6=seed→split→engrave no new codec.
OUT of #10: T5 bundle UX, T6 seed glue, BCH error-CORRECTION (decode_with_correction chunk.rs:502-623; device engraves verbatim, gather uses pure verify — shipped codex32 verify-only by design).

## 7. TEST / GOLDEN parity
Constellation ships byte-exact cross-lang goldens (strongest oracle): tests/vectors/<name>.bytes.hex (exact encodePayload out, e.g. wpkh_basic=2002001800, wsh_with_fingerprints=204200182182142f09bd5b7ddfcafebabe), .phrase.txt (full md1 string), .descriptor.json (AST to build input). 10-entry MANIFEST + wsh_multi_chunked(force_chunked) covers single+chunked+fingerprints+divergent+all renderable.
T2c decoder-parity precedent: Go tests use verbatim .phrase.txt literals, assert human Template + errors.Is by category, never Rust-string-equality (md_test.go:20-63,82-84).
**Acceptance gate for #10 TDD plan:** (1 PRIMARY) byte-exact encoder parity — per MANIFEST vector build descriptor from .descriptor.json, encodePayload, assert hex(bytes)==.bytes.hex AND bitLen; (2) full-string parity encodeMD1String==.phrase.txt (catches MDChecksumSymbols drift; ValidMD independent BCH); (3) round-trip decode→encode→decode — md1 CAN assert byte-equality on .bytes.hex (goldens SHA-derived canonical, unlike mk1 arbitrary-csid mk/encode_test.go:202-207); (4) chunked round-trip split→each ValidMD→Reassemble ok + csid match; drop→ChunkSetIncomplete; corrupt csid→ChunkSetIdMismatch (port bundle.rs:547-585 chunked_md1_vector, 6-key wsh-sortedmulti 15-deep ≥4 chunks); (5) identity pins deriveChunkSetID→0xABCDE, computeEncodingID determinism+path-sensitivity (identity.rs:301-322); (6) xpub-expansion projected desc → same receive address verifyAddressFlow validates, singlesig + wsh(sortedmulti).
Vendor tests/vectors/*.{bytes.hex,phrase.txt,descriptor.json} subset into md/testdata/ (hermetic). Live Rust↔Go harness NOT needed (frozen files cheaper/stabler); optional CI regen-and-diff for upstream drift.

## 8. RISKS (for R0 to lock)
1. **(CRITICAL) canonicalize must run inside Go encoder or identity diverges.** encode_payload canonicalizes placeholder indices on a clone first (encode.rs:66-67), permuting tree indices+divergent paths+ALL per-@N TLV maps atomically (canonicalize.rs:1-29,168-). csid over canonical bytes. **Go decoder NEVER ported canonicalize** (rejects non-canonical via validatePlaceholderUsage md.go:904-921). Encoder is first Go code needing canonicalize_placeholder_indices (~150 LOC port of canonicalize.rs:168- incl remap_indices/walk_collect_first). Skip→non-canonical input→wrong csid→cross-tool ChunkSetIdMismatch. READ path input always canonical (near-no-op) but T6 author-built AST can be non-canonical → mandatory in #10 since T6 write path in #10.
2. **(CRITICAL) MSB-first bitWriter + non-byte-aligned reEmitBits.** Every §1 trap routes through it. testBitWriter (md_test.go:96-117) proves algorithm; .bytes.hex goldens prove integration. Lock writer golden tests (port bitstream.rs:236-407) BEFORE structural encoder.
3. (IMPORTANT) Header(5b low-nibble version) vs ChunkHeader(37b top-nibble version) — WireVersionMismatch{got:2}. Single/chunked discriminator MUST be syms[0]&1, never ChunkHeader::read (bundle.rs:138-151, md.go:1207).
4. (IMPORTANT) varint lengths in BITS not bytes; TLV bit_len ≠ padded byte length (identity.rs:503-513).
5. (IMPORTANT) is_nums suppresses kiw; MultiKeys raw indices not nodes; n=1⟹kiw=0⟹zero-bit key args. All pinned by tree.rs bit-cost tests — port them.
6. (IMPORTANT) xpub-expansion projectable subset narrower than decoder renderable set. bip380 no unsorted multi/multi_a/taptree. Address-verify only singlesig+sortedmulti; else display-only.
7. (MODERATE) MDChecksumSymbols POLYMOD_INIT=0x23181b3 not codex32's 1 (codex32/mdmk.go:11-13,39; MKChecksumSymbols documents trap mkencode.go:11-17). ValidMD guards.
8. (MODERATE) unknown-TLV re-emit via reEmitBits(payload,bitLen) not padded bytes (tlv.rs:197-199; pack md.go:623-654).

**Sizing ≈ 950 LOC Go (excl tests):** bitWriter+reEmit ~70; encodePayload+writers+validators ~350; canonicalize ~150; identity+ChunkHeader+split+Reassemble ~180; MDChecksumSymbols+assembleMD1 ~50; xpub-expansion+secp256k1 ~150. + comparable golden-driven test body.
**Reuse as-is:** decoder+AST+validators (md/md.go), bitReader, MDDataSymbols/ValidMD, bip380.{Descriptor,Key,Derivation}+verifyAddressFlow, mk1Gatherer GUI template, md1DisplayFlow, bip32/hdkeychain/btcec. **Net-new:** bitWriter, md/encode.go, canonicalize port, MDChecksumSymbols/assembleMD1, ParseChunkHeader/Reassemble, xpub-expansion projection, md1Gatherer. **Precedent: T4 mk encoder (mk/encode.go) — same recipe; md1 differs only by bit-packing (needs bitWriter) + regular-BCH-only.**

R0 focus: confirm (a) canonicalize in-scope for #10; (b) byte-exact .bytes.hex parity = primary gate; (c) xpub-expansion projectable-subset boundary; (d) bit-0 discriminator everywhere (no ChunkHeader::read probe as primary).
