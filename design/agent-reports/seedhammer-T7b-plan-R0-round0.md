# T7b IMPLEMENTATION PLAN — R0 review (round 0) — VERBATIM agent report

**Agent:** `a501af366ac976682` (adversarial opus architect; RAN a golden re-derivation probe in a throwaway worktree). **Fork HEAD:** `82d46b3`. **Plan commit:** `7ee0b62`. **Date:** 2026-06-19.
**Verdict:** NOT GREEN (0C / 2I / 3M). The 2 Importants are test-scaffolding defects (not crypto/flow); the goldens + flow logic are verified-correct. Persisted before folding per the R0 gate discipline.

---

# T7b IMPLEMENTATION PLAN — R0 review (round 0)
**Reviewer:** opus architect (adversarial)  **Fork HEAD:** 82d46b3  **Plan commit:** 7ee0b62  **Verdict:** NOT GREEN (2 Important; 0 Critical; 3 Minor)

## Golden re-derivation (MANDATE #1) — RAN the probe
Wrote an independent probe (`gui/r0probe_test.go`) in a detached throwaway worktree off `main`, re-creating the plan's exact algorithm (build master → fully-hardened `m/83696968'/39'/0'/{words}'/{index}'` walk → `ECPrivKey()` → `bip85.Entropy(priv)` → LEADING `entLen=(words*11-words/3)/8` bytes → `bip39.New`), and computed fps via the in-tree `masterFingerprintFor`. Actual output:

```
R0 abandon master fp           = 73c5da0a (knownMasterFP=73c5da0a)
R0 abandon 12'/0' entLen=16 ent=ac98dac5d4f4ebad6056682ac95eb9ad
R0 abandon 12'/0' child  = "prosper short ramp prepare exchange stove life snack client enough purpose fold"  fp=02e8bff2  valid=true words=12
R0 abandon 18'/0' entLen=24 ent=fc039f51d67ed7dfd01552f27de28887cf3e58655153e44b
R0 abandon 18'/0' child  = "winter brother stamp provide uniform useful doctor prevent venue upper peasant auto view club next clerk tone fox"  fp=3bb5fd0c  valid=true words=18
R0 abandon 24'/0' entLen=32 ent=d5a9cb46670566c4246b6e7af22e1dfc3668744ed831afea7ce2beea44e34e23
R0 abandon 24'/0' child  = "stick exact spice sock filter ginger museum horse kit multiply manual wear grief demand derive alert quiz fault december lava picture immune decade jaguar"  fp=c2f2dd51  valid=true words=24
R0 abandon 12'/1' child  = "sing slogan bar group gauge sphere rescue fossil loyal vital model desert"
R0 abandon 12'/9' child  = "earth ice square lottery area detail test spike innocent right matter bubble"
R0 canonical master fp         = 627ef3a6
R0 canonical 12'/0' ent(16)    = 6250b68daf746d12a24d58b4787a714b
R0 canonical 12'/0' child      = "girl mad pet galaxy egg matter matrix prison refuse sense ordinary nose"  fp=595037d0
```

**EVERY pinned golden MATCHES the plan exactly:** abandon master fp `73c5da0a` (= in-tree `knownMasterFP`); 12'/0' ent `ac98dac5…`, child `prosper short ramp…fold`, fp `02e8bff2`; 18'/0' `winter brother…fox` fp `3bb5fd0c`; 24'/0' `stick exact…jaguar` fp `c2f2dd51`; idx 1 `sing slogan bar…desert`, idx 9 `earth ice square…bubble`; canonical vector master fp `627ef3a6`, ent `6250b68d…`, child `girl mad pet…nose` fp `595037d0` — byte-identical to the published BIP-85 spec vector. The goldens are **VERIFIED**, not trust-me. Probe + worktree removed; main tree clean.

## Flagged risks (MANDATE #2)
**§5.4 / Plate.Spline opacity — input-level assertion is FAITHFUL (verified-correct).** `type Curve = iter.Seq[Knot]` (`bspline/bspline.go:22`) — `Plate.Spline` is a function, genuinely un-inspectable for words; its only consumer is `newEngraverJob(ctx.Platform, plate.Spline, 0)` (`gui.go:2495`). No public reader for the engraved mnemonic on a `Plate`. Asserting on engrave INPUTS is the only seam and faithfully satisfies §5.4-I-A: `engraveSeed` stamps `MasterFingerprint: mfp` (`gui.go:475`), `engraveBip85Child` returns exactly that `mfp`; the Task-2 test asserts `gotFP == childOwnFP && gotFP != masterFP && gotFP == 0x02e8bff2`. The wrong-identifier guard is genuinely caught — not a weakening. The child mnemonic passed to `engraveSeed` is the same object derived/pinned by Task-1, so words are transitively covered.

**Task-5 defer-ordering scrub trap — the FLOW logic is correct; the precedent is real; but the TEST that proves it is broken (see Important #1).** The shipped precedent is faithful: `singleSigSeedHook` is called synchronously right after `seedEntryFlow` returns (`singlesig.go:36-38`), then the scrub `defer` is registered (`:41-45`); the test (`singlesig_flow_test.go:117-154`) holds the captured slice header and reads contents AFTER the flow returns and the defer ran. The plan's design (call `bip85SeedHook(master, child)` synchronously after `child = c`, single top-level scrub defer — lines 871-881) mirrors this exactly. I traced EVERY exit of `bip85DeriveFlow`: master-entry-cancel returns before the defer is registered (master nil — nothing to scrub); after registration the defer scrubs BOTH on param-cancel / derive-error / warning-abort / engrave-abort / engrave-error / engrave-success; child is additionally scrubbed in-loop on every abort path. **No path leaves master or child unscrubbed.** The flow logic is sound; only the TEST cannot drive the flow to observe it.

## Critical
None. (The goldens — the highest-risk item — are all verified-correct.)

## Important
- **I-1 — The two UI-harness tests (Task 4 `TestChildSeedWarningAbort`, Task 5 `TestBip85DeriveFlow_ScrubsBothMnemonics`) are non-functional as written: they omit all `frame()` pumps (Task 5 even discards the `frame` handle with `_`), so they will not drive the flow.** `runUI` uses `iter.Pull` (`gui_test.go:467-478`): the flow goroutine only advances when `frame()` is pulled; a bare `click()` merely queues a router event. Every shipped flow/abort test interleaves `frame()` after each `click` and pumps until the goroutine returns before asserting — e.g. `TestDescriptorAddressFlowBackExits` (`address_polish_test.go:77-92`, loops `frame()` until `!ok`) and `TestEngraveSingleSigFlowSeedScrubbed` (`singlesig_flow_test.go:139-141`, `for i:=0;i<32 && !done; frame()`). As written: **Task 4** (`_, quit := runUI(...); click(Button1); quit(); if got {...}`) — the flow blocks on its first `ctx.Frame` yield, never processes Button1, so `childSeedWarning` never returns; `got` stays zero `false` and the assertion passes *vacuously* (does NOT test abort — false-green). **Task 5** (`_, quit := runUI(...)`, no frame pumps) — the flow is stuck inside `seedEntryFlow`'s first `Choose()` yield; never reaches `child = c` to call the hook, so `master`/`child` stay nil → `t.Fatal("hook never observed both mnemonics")` and the test **FAILS** (the plan's Step 5.4 claims PASS); also `_, quit := runUI(...)` discards the `frame` function the test needs. Fix before GREEN: both tests must (a) keep the `frame` handle (`frame, quit := runUI(...)`), (b) interleave `frame()` after every `click`/`driveWords` (use `pumpUntil`/`frameUntil` to reach UI states + drain until the flow returns), and (c) wrap the body in `synctest.Test(t, func(t *testing.T){...})` (`testing/synctest`; every seed-typing flow test does: `singlesig_flow_test.go:118`, `derive_xpub_test.go:118`). The plan flags this risk in prose (lines 768, 886, 1276) and says "do NOT weaken the scrub assertion; align the click sequence" — but it ships concrete test code with the wrong structure labelled "Expected: PASS." Per writing-plans, shipping non-functional test code claimed to pass is Important: the implementer following verbatim gets vacuous-green (Task 4) and a hard-fail (Task 5). The scrub *assertion* (zeroed slices) is correct and must be preserved; only the driving scaffold needs rebuilding against the cited precedents.

- **I-2 — Task 5 references three helpers without confirming they exist; one (`pumpUntil`) is the actual fix, two (`chooseWords`/`clickChoose`) are invented, and `synctest` is unmentioned.** The plan defines `clickChoose`/`chooseWords` as new wrappers (lines 750-765) but the seed-entry-then-warning sequence cannot work without `frame()` interleaving and `pumpUntil` (`slip39_polish_test.go:329`) / `frameUntil` (`address_polish_test.go:31`), which the plan never names. `driveWords` (`seedxor_polish_test.go:24`) and `abandonAboutPhrase` (`derive_xpub_test.go:14`) DO exist as cited, and `click(r *EventRouter, ...)` (`event_test.go:42`) is cited correctly. The missing `synctest` wrapper + frame-pump helpers mean the Task-5 "Interfaces/helpers" list is incomplete for a working test. Folding I-1 (rebuild driving against `singlesig_flow_test.go` / `derive_xpub_test.go` verbatim, including `synctest`) resolves I-2.

## Minor
- **m1 — `chooseWords` helper is dead/misleading.** The plan's `chooseWords` (lines 759-765) only supports `n==12` and calls `clickChoose` once with no `frame()`; in a corrected (frame-pumped) test it should follow the `chooseEntry(frame, &ctx.Router, down)` precedent (`derive_xpub_test.go:21`). Cosmetic given I-1's rebuild subsumes it.
- **m2 — Step 5.3 carries contradictory hook code then a prose correction.** Lines 814-817 show a `defer bip85SeedHook(...)` block, then lines 871-879 say "that is WRONG ... drop the second defer and call the hook synchronously." Collapse to the single correct synchronous-call version. Non-blocking.
- **m3 — Step 6.6 over-states the singlesig repoint scope (harmless).** The plan lists `gui/singlesig_program_test.go` among files to repoint; only `TestEngraveSingleSigLeftWrap` (`:80`, `"Multisig"`→`"BIP-85"`) breaks — `TestEngraveSingleSigProgramNavigable` stops at the 4th Right (Multisig, still position 4) and stays green. Step 6.6 already targets only `LeftWrap` for singlesig; just a loose file-list.

## Verified-correct
- **Derive helper (Task 1) algorithm — byte-exact to biptool + canonical BIP-85.** Path fully hardened; LEADING `entLen` bytes confirmed against biptool `main.go:188-189`; `entLen=(words*11-words/3)/8` → 16/24/32; `ECPrivKey() (*PrivateKey, error)` error propagated, never `.Serialize()` on nil. No off-by-one, no non-hardened element, no mis-sliced HMAC. `Derive` returns fresh independent buffers (`extendedkey.go:228-321`), so in-loop `k.Zero()` after `Derive` doesn't corrupt the child; master/intermediates/leaf all zeroed; `priv`+`hmacOut` `wipeBytes`-deferred. `entLen` guard makes `bip39.New` (bounds `16≤len≤32, len%4==0`) un-panicable.
- **Child fp (Task 2, I-A).** `masterFingerprintFor(child, &chaincfg.MainNetParams, "")` `(uint32, error)` (`gui.go:485`), err propagated; passed straight to `engraveSeed` as `mfp` (`:475`); `backupWalletFlow`'s passphrase-fp picker skipped. Test well-formed (pure-function, no harness); child fp `02e8bff2` ≠ master `73c5da0a` (probe-verified).
- **Picker bounds (Task 3, I-2).** `bip85WordChoices=[12 18 24]`, `bip85IndexChoices=[0..9]`, app fixed BIP-39. `TestBip85ParamBounds` pins the sets and derives a valid child for all 30 pairs. Well-formed (no harness).
- **Scrub spine (I-3).** Two secrets + privkey + HMAC; typed-only master via `seedEntryFlow` (`derive_xpub.go:82`, count `[]int{12,24}` `:89`, no `gui/scan.go`). Flow-level logic correct on every exit (traced above).
- **Channel (I-4).** Child engraved only via `engraveSeed`→`backup.EngraveSeed` (steel). Task-7 grep is a real runnable check; no extended key in the artifact.
- **Lockstep (I-7) — all 8 sites accurate @ 82d46b3, all keyed on `engraveMultisig`.** enum `:147-154` (qaProgram `:153`); dispatch `:1492-1514`; left-wrap `:1640-1643`; right-wrap `:1644-1652` (`>engraveMultisig` `:1649`); title `:1668-1678`; `npage:=int(engraveMultisig)+1` `:1852`; `layoutMainPlates` case + MANDATORY `panic` `:1860-1867`; `npages` `:1871`. The 2 new nav-tests (`bip85_program_test.go`) correctly pump `frame()` after each `click` (well-formed). Prior-program repoint correct: `TestEngraveMultisigProgramNavigable`/`LeftWrap` + `TestEngraveSingleSigLeftWrap` break and are repointed; bundle/derive_xpub are comment-only. `qaProgram` reachable only via the special action `:1610` — stays non-navigable. `TestAllocs` (`gui_test.go:93`) unaffected.
- **Warning (Task 4 impl).** `childSeedWarning` is a faithful copy of `stubZeroWarning` (`derive_xpub.go:237-256`). The *implementation* is correct; only its test (I-1) is broken.
- **Spec coverage.** Every §6 invariant I-1..I-8 and §5.1–§5.8 acceptance item maps to a task; the only gaps are the two non-functional tests (I-1) — coverage intent complete, test mechanics wrong.
- **All cited APIs/signatures confirmed** (bip85/bip39/hdkeychain/engraveSeed/Plate/masterFingerprintFor/ChoiceScreen.Choose/NewEngraveScreen/wipeBytes/showError/passphraseFlow/op.*/the test helpers `runUI`/`uiContains`/`click(*EventRouter,...)`/`driveWords`/`abandonAboutPhrase`).

## Bottom line
**NOT GREEN — 0 Critical, 2 Important, 3 Minor.** MANDATE #1 fully discharged: every pinned golden is byte-identical to live Go output, and the derive/fp/picker/lockstep/scrub-flow logic is verified-correct; the §5.4 input-assertion is faithful and the scrub-flow has no unscrubbed exit. The two blockers are confined to test *scaffolding*: the Task-4/Task-5 UI-harness tests omit `frame()` pumping (Task 5 discards the `frame` handle) and the `synctest` wrapper every shipped seed-typing flow test uses, so Task 4 passes vacuously and Task 5 hard-fails — contradicting their "Expected: PASS." Fold: rebuild both tests' driving against the cited precedents (`singlesig_flow_test.go:117-154`, `address_polish_test.go:77-92`, `derive_xpub_test.go:113-165`), keeping the scrub assertion intact and naming `synctest`/`pumpUntil`/`frameUntil`; collapse Step 5.3's struck-then-corrected hook code (m2); tidy `chooseWords` (m1); loosen the m3 file-list note. Re-dispatch R0 after the fold.
