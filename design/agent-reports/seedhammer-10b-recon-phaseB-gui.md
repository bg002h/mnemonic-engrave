# RECON (#10b Phase B, agent a39ee510efc355ad0, 2026-06-19) — md1 gather + wallet-policy xpub-expansion → display + verify (source-verified vs fork `3a55ae5`)

Recon only. All citations `file:line` at fork HEAD `3a55ae5` (#10a merged) + Rust md-codec `c85cd49`.

## 1. mk1 gather UX to clone (`gui/mk1_inspect.go`)
`mk1Gatherer` pure accumulator (`:48-83`): `{set map[int]string; total int; setID uint32; primed bool}`; `offer(s) gatherStatus` (`:55-73`) calls `mk.ParseHeader`, primes on first chunk, rejects `gatherForeign` if `!Chunked||ChunkSetID!=setID||TotalChunks!=total` (`:65-67`), `gatherDup` (`:68-70`); `complete()`=`primed&&len(set)==total` (`:75`); `collected()` (`:77-83`). `gatherStatus` enum `:37-44`.
`mk1GatherFlow(ctx,th,first)(mk.Card,bool)` (`:156-256`): primes from already-scanned chunk (`:157-161`); own scanner goroutine guarded `if r:=ctx.Platform.NFCReader();r!=nil` (`:163`) w/ closer/closed/defer idiom (`:164-208`); frame loop type-asserts `scan.Object.(mdmkText)` (`:218`), dispatches on offer status (`:219-231`), on added+complete→`decodeGathered` (`:222-224`); UX `"Captured %d of %d."`+`"Scan the next chunk."` (`:235`), title `"Inspect key"` (`:247`). `decodeGathered`→`mk.Decode(g.collected())` (`:258-265`).
**CLONE (not generic):** `md.ParseChunkHeader` (`md/chunk.go:174-189`) returns `md.ChunkHeader{Chunked,ChunkSetID,TotalChunks,ChunkIndex}` (`md/chunk.go:38-44`) — FIELD-NAME-COMPATIBLE with mk's, so `offer` ports verbatim (swap parser). md1-specific: `ParseChunkHeader` does bit-0 single/chunked discriminator first (`md/chunk.go:182-185`) → single md1 returns `{Chunked:false}` no-error; keep the `!h.Chunked`→foreign guard. Precedent for parallel clones: `md1DisplayFlow` "Mirrors mk1DisplayFlow" (`gui/md1_inspect.go:72-133`).

## 2. Current md1 flow + refusal point
`mdmkFlow(ctx,th,s)` at `gui/gui.go:1939`; md1 inspect branch `:1979-1987`: `md.Decode(str)` → `err==nil`→`md1DisplayFlow`; `errors.Is(err,md.ErrChunkedUnsupported)`→`showError(...,"Multi-part descriptor — not yet supported.")` **(#10b ROUTE POINT, `:1984-1985`)**; default→"Can't decode". `md.Decode` detects chunked via bit-0 → `ErrChunkedUnsupported` (`md/md.go:1207-1209`); routing condition already wired, only the action changes → call `md1GatherFlow(ctx,th,str)`.
`md1Summary` (`gui/md1_inspect.go:53-70`): renderable→`"Type: "+scriptName+" "+policyLine`; per-key `"@%d %s %s %s"`=Index,Fingerprint(or "—"),OriginPath,UseSite. **Current display shows NO xpub** — template metadata only; #10b adds descriptor string + address-verify on top via DescriptorScreen/verifyAddressFlow, not just md1DisplayFlow.

## 3. md decoder wallet-policy data (`md/md.go`)
Decoded types UNEXPORTED: `descriptor{n,pathDecl,useSite,tree,tlv}` (`:815-821`); `node{tag,body}` (`:134-137`). Per-@N TLV: `idxPub{idx uint8; xpub [65]byte}` (`:508`, `readPubkeys` `:743-778`) — **[65]B = 32B chainCode‖33B compressed pubkey** (Rust `derive.rs:43-44`, `canonicalize.rs:347-348`); `idxFP{idx,fp[4]byte}` (`:504`), `idxOrigin` (`:512`), `idxUseSite` (`:500`); `tlvSection{...pubkeys []idxPub; pubPresent bool;...}` (`:522-530`).
`originPathStringFor(d,idx)` (`:1336-1354`): override>divergent[idx]>shared; returns STRING (#10b needs structured components → reach into `d.tlv`/`d.pathDecl`).
**`validateXpubBytes` NO-OP** (`:1071-1077`, body `_=errInvalidXpubBytes;return nil`; secp256k1 deferred to #10). Rust `validate.rs:215-227` does `PublicKey::from_slice(&xpub[32..65])`→`InvalidXpubBytes{idx}`. `errInvalidXpubBytes` already exists (`:898`).
`Renderable` (`summarize` `:1295-1318`, `classifyPolicy` `:1237-1281`): `tr` renderable ONLY key-path-only (`!isNums&&tree==nil` `:1253-1255`; tr+tree→Complex); wsh/sh via `multiPolicy` accepts `tagMulti`→PolicyMulti AND `tagSortedMulti`→PolicySortedMulti (`:1283-1293`) — **both renderable but `multi`(unsorted) NOT bip380-expressible** (§4). `PolicyKind`/`ScriptKind` `:1159-1177`.
Public: `md.Template{N,Root,Policy,K,M,Keys []KeyOrigin,Renderable}` (`:1190-1197`); `md.KeyOrigin{Index,Fingerprint,OriginPath,UseSite string}` (`:1182-1187`) — **string-only, NO xpub bytes / structured origin / numeric fp.**
**MISSING (NET-NEW):** (1) per-@N xpub bytes accessor; (2) `md.Reassemble` returns unexported `*descriptor` (`md/chunk.go:196`) — unusable from `gui` → add exported `md.DecodeChunks([]string)(Template,…)` + exported expansion accessor; (3) `md.Decode` returns only Template (no xpub).

## 4. bip380 projection target (`bip380/bip380.go`)
`Key{Network *chaincfg.Params; MasterFingerprint uint32; DerivationPath bip32.Path; Children []Derivation; KeyData []byte; ChainCode []byte; ParentFingerprint uint32}` (`:28-36`). `ExtendedKey()` (`:97-109`): `hdkeychain.NewExtendedKey(Network.HDPublicKeyID, KeyData, ChainCode, parentFP, depth=len(DerivationPath), childNum=last-path-elem, isPrivate=false)` — **depth/childnum from DerivationPath**; but address derivation (`derivePubKey`) uses only KeyData+ChainCode+Children (depth/childnum/parentfp only affect the serialized string).
`Descriptor{Title,Script,Threshold,Type MultisigType,Keys []Key}` (`:20-26`); `Derivation{Type,Index,Hardened,End}` (`:38-46`), types Child/Wildcard/Range (`:50-54`); `Script` Unknown/P2SH/P2SH_P2WSH/P2SH_P2WPKH/P2PKH/P2WSH/P2WPKH/P2TR (`:58-67`); `MultisigType` Singlesig/SortedMulti (`:92-95`).
**Expressible:** singlesig wpkh/pkh/tr-keyonly/sh-wpkh (`Singlesig()` `:116-118`); multisig ONLY `SortedMulti` (`Parse` accepts only "sortedmulti" `:333-337`). **NOT:** unsorted multi, multi_a, tr-taptree. Address layer narrower: `addressAt` (`address/address.go:96-158`) SortedMulti for P2SH/P2WSH/P2SH_P2WSH + Singlesig for P2PKH/P2WPKH/P2SH_P2WPKH/P2TR; else `errUnsupported`.
**NO builder** — only `Parse(string)`; assemble struct literal directly (pattern `gui/gui_test.go:117-145`).

## 5. address-verify entry (`gui/verify_address.go`)
`verifyAddressFlow(ctx,th,desc *bip380.Descriptor)` (`:22-38`) — takes desc directly; Scan/Type→`runVerify` (`:173-213`) calls `address.Find(desc,candidate,20)` once (`:179`). `descriptorFlow(ctx,th,desc)` (`gui/gui.go:2058-2072`)→`DescriptorScreen{Descriptor:desc}` loop `ds.Confirm` (`:2354-2424`); `supported:=address.Supported(desc)` hoisted out of frame loop (`:2372`, alloc-gate); menu→`descriptorAddressFlow` (`gui/address_polish.go:26`) or `verifyAddressFlow` (`:2385`). **#10b: build `*bip380.Descriptor`→hand to descriptorFlow/DescriptorScreen (full UX) or verifyAddressFlow (verify-only) — same entry T1/T3 used.**

## 6. secp256k1 on-curve check
In-tree: `btcec/v2.ParsePubKey` (go.mod:7) + `decred/dcrec/secp256k1/v4.ParsePubKey` (go.mod:11, used by `address`/`bip32`). `md` imports NEITHER today. Rust `validate.rs:215-227` = `PublicKey::from_slice(xpub[32:65])`. **Plan locks owner:** put it in `md.validateXpubBytes` (matches Rust placement; `md` gains secp256k1 dep) → `errInvalidXpubBytes` (`md/md.go:898`). 32B chaincode prefix intentionally unvalidated (Rust `validate.rs:203-206`).

## 7. xpub-expansion semantics (Rust `canonicalize.rs`+`derive.rs`)
`ExpandedKey{idx,origin_path,use_site_path,fingerprint Option<[4]byte>,xpub Option<[65]byte>}` (`canonicalize.rs:337-350`). `expand_per_at_n(d)` (`:420-474`) one per @N in 0..n: origin = override>shared/divergent[idx] (`:437-443`); MissingExplicitOrigin gate (`:450-455`); use-site = override>baseline (`:458-460`); fingerprint/xpub = TLV[idx] else None (`:462-463`).
Xpub reconstruct (`derive.rs:49-64`): chain_code=bytes[0:32]; public_key=`from_slice(bytes[32:65])` (**= on-curve check**); metadata PLACEHOLDERS network=Main, depth=0, parentfp=0, childnum=0 ("not used by derive_pub; only chain_code+public_key in CKDpub"). **Go bip380.Key:** ChainCode=xpub[0:32], KeyData=xpub[32:65]; for address derivation only KeyData+ChainCode+Children matter (depth/childnum placeholder OK); BUT if serialized xpub string displayed, depth=len(DerivationPath)/childNum=last(path) from ORIGIN (not TLV) → set Key.DerivationPath from origin, Key.MasterFingerprint from fp. Use-site→Children (`to_miniscript.rs:116-131`): `<a;b>`→[RangeDerivation{a,b},WildcardDerivation]; bare `*`→wildcard. `address.derivePubKey` defaults `<0;1>/*` when Children empty (`address/address.go:176-188`), only supports `End==Index+1` (`:196-198`) — std `<0;1>/*` fits. Hardened wildcard/multipath → Rust `HardenedPublicDerivation` (`derive.rs:98-110`); Go `derivePubKey` fails late → add early guard.
**RISKS:** (a) byte split [0:32]chaincode/[32:65]pubkey (off-by-one swaps); (b) DerivationPath from origin (serialize depth/childnum); (c) Children from use-site; (d) **network NOT in TLV — Rust hardcodes Main; plan must lock mainnet vs picker.**

## 8. GUI program/lockstep
**NO new program** — enum `{backupWallet,engraveXpub,qaProgram}` (`gui/gui.go:147-151`); md1 inspect reached via `engraveObjectFlow case mdmkText→mdmkFlow` (`:1885-1886,1939`), NOT a StartScreen program. #10b extends `mdmkFlow` in place (the `ErrChunkedUnsupported` arm `:1984-1985`). 6/8-site lockstep N/A.
**TestAllocs:** `BenchmarkAllocs` (`gui/gui_test.go:50-91`) exercises ONLY `StartScreen.Flow` + `DescriptorScreen.Confirm` — gather/display NOT alloc-gated. BUT if #10b routes into `DescriptorScreen` (alloc-gated; hoists `address.Supported` `:2369-2372`), the reassemble→validate→build-descriptor MUST complete in the gather-completion handler, NEVER per-frame.

## Classification
**REUSABLE as-is:** `md.ParseChunkHeader`; `md1DisplayFlow`/`md1Summary`/`scriptName`/`policyLine`/`chunkString`; `verifyAddressFlow`/`runVerify`/scan/type; `descriptorFlow`/`DescriptorScreen`/`descriptorAddressFlow`; `address.Find/Receive/Change/Supported/derivePubKey`; scanner-goroutine shell; secp256k1 `ParsePubKey`.
**CLONE-with-tweaks:** `md1Gatherer`←`mk1Gatherer` (swap parser, keep logic); `md1GatherFlow`←`mk1GatherFlow` (swap types, "descriptor" copy, completion→reassemble+expand+build).
**NET-NEW:** (1) exported `md.DecodeChunks` + per-@N expansion accessor surfacing (idx, structured origin, use-site, fp[4], xpub[65]); (2) real secp256k1 check in `validateXpubBytes`; (3) programmatic `*bip380.Descriptor` assembly (map ChainCode=xpub[0:32]/KeyData=xpub[32:65]/DerivationPath←origin/MasterFingerprint←fp/Children←use-site; Script/Type from Template.Root/Policy; Network); (4) `mdmkFlow` routing change.
**RISKS to lock:** (1) Renderable≠bip380-expressible (unsorted multi/multi_a/taptree → display-only, NO verify/build); (2) template-only vs xpub-bearing (no pubkeys TLV → no expansion, template-only); (3) **network not on wire (mainnet vs picker — DECISION)**; (4) xpub byte-split + origin-sourced DerivationPath; (5) hardened wildcard/multipath early reject; (6) reassemble/expand before DescriptorScreen (alloc); (7) csid-mismatch error UX distinct from decode failure.
