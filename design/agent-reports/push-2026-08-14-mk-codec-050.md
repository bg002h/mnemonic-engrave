# Push report — mk-codec 0.5.0 pin unblock — 2026-08-14

Scope: push already-committed local work in `mnemonic-key` and `mnemonic-toolkit` to
their remotes. No commits authored, amended, rebased, or force-pushed.
`seedhammer` was not touched (out of scope per brief).

## 1. `mnemonic-key` — PUSHED, urgent pin now reachable

- Branch: `main`
- Pre-push `git status --porcelain`: empty (clean tree)
- Pre-push `git log --oneline -1`: `a38a908 feat(mk-codec 0.5.0)!: derive chunk_set_id from the payload, not entropy`
- Pre-push `git log --oneline -1 origin/main`: `018aca0 docs(followups): companion — sibling-gui-schema-v5-default-value-emission (GUI batch)`
- Push command: `git push origin main`
- Exact output:
  ```
  remote: Bypassed rule violations for refs/heads/main:
  remote:
  remote: - Required status check "build (stable on ubuntu-latest)" is expected.
  remote:
  To github.com:bg002h/mnemonic-key.git
     018aca0..a38a908  main -> main
  ```
- Post-push `git log --oneline -1 origin/main` (via `git fetch origin main` then `git log`): `a38a908 feat(mk-codec 0.5.0)!: derive chunk_set_id from the payload, not entropy`
- Full SHA on remote: `a38a908e143c2c4bd6405997d62385b3df01615f` — **matches the `oracle/pins.json` pin exactly.** The pin is no longer unreachable.
- Test suite: **skipped**. `cargo test` on this workspace was judged not "cheap" (full crate test run, not a sub-2-minute check already wired for this purpose) — not run, not reported as passing.

### Anomaly — branch-protection bypass (not requested, not invented)

The push was **not rejected**; it went through but GitHub reported it as a
**bypass** of the required status check `build (stable on ubuntu-latest)`
(the branch-protection rule on `main` lists that single required context).
Checked via `gh api repos/bg002h/mnemonic-key/branches/main/protection`:
`enforce_admins` is `false`, so the account pushing (repo admin) is exempt
from the required-check gate — the bypass happened automatically as a
consequence of `git push origin main`, not from any override flag I passed.
I did **not** invent a `ci/staging` ritual for this repo: `mnemonic-key/CLAUDE.md`
was checked first and documents no such convention (that ritual is specific
to `mnemonic-engrave`'s own CLAUDE.md). Per the brief, "do not bypass a
rejected push" — this wasn't a rejection I could route around; it was a
successful push that GitHub itself flagged as a bypass after the fact.

Post-push, `gh run list --branch main --limit 3` shows CI now running
retroactively against `a38a908`: `CI` and `fuzz-smoke` were `in_progress`,
`vendor-freshness` had already completed `success`. The required check
(`build (stable on ubuntu-latest)`, part of the `CI` workflow) simply never
gated this push before landing — it's running now, after the fact. Flagging
this plainly for you to decide whether `mnemonic-key` should adopt the same
`ci/staging`-ref ritual `mnemonic-engrave` uses, and whether to watch the
in-flight run for a red result on `a38a908`.

## 2. `mnemonic-toolkit` — PUSHED, but NOT to `master` — flag for a decision

- Branch: **`followup/p2wsh-binding-oracle`** (not `main`/`master` — the brief
  named only the commit, not a branch, for this repo)
- Pre-push `git status --porcelain`: 38 untracked (`??`) entries, **zero**
  modified/staged tracked files. Untracked set includes the 9
  pre-existing `cycle-prep-recon-*.md` root files called out in the brief
  (left alone, not added) **plus 29 more untracked files not mentioned in
  the brief** — `design/*.md` (SPEC/PLAN docs), ~19 files under
  `design/agent-reports/`, and 4 under `docs/manual-gui/design/agent-reports/`.
  All untracked, all left untouched — none staged or committed by this agent.
- Pre-push `git log --oneline -1`: `6e511fa8 docs(manual): mirror \`mk encode --chunk-set-id\` (mk-cli 0.13.0)`
- This branch had **no upstream configured** (`fatal: no upstream configured
  for branch 'followup/p2wsh-binding-oracle'`).
- `6e511fa8`'s parent is `c14b1e21`, which **is** `origin/master`'s tip
  (verified: `git merge-base --is-ancestor origin/master 6e511fa8` → ancestor,
  i.e. a clean fast-forward is possible onto `master`) — but I did **not**
  push it to `master`. Reasoning: the brief specified a branch (`main`) for
  `mnemonic-key` but only a commit for `mnemonic-toolkit`; the commit sits on
  a named topic branch, not `master`; `mnemonic-toolkit/CLAUDE.md` documents
  no direct-push-to-`master` or `ci/staging` ritual; and the brief explicitly
  says not to invent one. Pushing the topic branch under its own name is the
  narrowest, most reversible interpretation of "push already-committed work
  to its remote" — it does not touch the protected branch.
- Push command: `git push -u origin followup/p2wsh-binding-oracle`
- Exact output:
  ```
  remote:
  remote: Create a pull request for 'followup/p2wsh-binding-oracle' on GitHub by visiting:
  remote:      https://github.com/bg002h/mnemonic-toolkit/pull/new/followup/p2wsh-binding-oracle
  remote:
  To github.com:bg002h/mnemonic-toolkit.git
   * [new branch]        followup/p2wsh-binding-oracle -> followup/p2wsh-binding-oracle
  branch 'followup/p2wsh-binding-oracle' set up to track 'origin/followup/p2wsh-binding-oracle'.
  ```
  Clean push, no bypass/protection message (this branch has no protection
  rule — verified only `master` has a branch-protection rule via
  `gh api repos/bg002h/mnemonic-toolkit/branches/master/protection`, and
  `gh api repos/bg002h/mnemonic-toolkit/rulesets` returned `[]`, so no
  wildcard rule applies either).
- Post-push `git log --oneline -1 origin/followup/p2wsh-binding-oracle` (via
  `git fetch origin followup/p2wsh-binding-oracle`): `6e511fa8 docs(manual):
  mirror \`mk encode --chunk-set-id\` (mk-cli 0.13.0)` — remote branch created
  and moved as expected, not "up-to-date" no-op.
- `master` on `mnemonic-toolkit` remains at `c14b1e21` (`mnemonic-toolkit-v0.97.0`)
  on the remote, unchanged. **`6e511fa8` is not yet on `master`** — it is
  reachable on `origin/followup/p2wsh-binding-oracle` and fast-forwardable
  onto `master` (its own PR link is printed above), but merging/landing it on
  `master` was left for you or a follow-up PR, not done by me.
- Test suite: **skipped**, same rationale as above (large multi-crate
  workspace, not a pre-wired sub-2-minute check).

## Summary table

| repo | target pushed | remote SHA now | matches expectation | anomaly |
|---|---|---|---|---|
| mnemonic-key | `main` | `a38a908e143c2c4bd6405997d62385b3df01615f` | yes — matches the pin exactly | branch-protection required check bypassed (admin exemption, not requested) |
| mnemonic-toolkit | `followup/p2wsh-binding-oracle` (new branch) | `6e511fa8...` | commit is on the remote and reachable | **not landed on `master`** — needs a decision: merge via PR, or direct fast-forward push to `master` if you want that ritual authorized |
