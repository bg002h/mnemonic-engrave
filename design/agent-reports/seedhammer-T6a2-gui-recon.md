# RECON (T6a-2 GUI, agent ab0487816c7f69912, 2026-06-19) — GUI touchpoints, source-verified at fork HEAD `bfff857`

Recon only. T6a-1 headless core + T5 `engraveBundle` both merged. Every fact personally re-read against `bfff857` (one Explore sub-run fabricated topic-3 line numbers → discarded + re-verified).

## SHIPPED headless core — exact signatures
- `md.EncodeSingleSig(chainCode [32]byte, compressedPubkey [33]byte, fp [4]byte, origin []md.PathComponent, script md.ScriptKind) ([]string, error)` — `md/encode_singlesig.go:36`; `PathComponent{Hardened bool; Value uint32}` `:20`; empty origin rejected.
- `md.ScriptKind` (`md/md.go:1163-1177`): `ScriptWpkh=0,ScriptPkh=1,ScriptSh=2,ScriptWsh=3,ScriptTr=4,ScriptShWpkh=5` (ShWpkh appended; decoder summarizes sh(wpkh) wire to `Root==ScriptSh`).
- `codex32.EncodeMS1(entropy []byte) (string, error)` — `codex32/msencode.go:17` (16/20/24/28/32-byte entropy → SECRET ms1).
- `md.WalletPolicyIDStubChunks(strs []string) ([4]byte, error)` — `md/walletpolicyid.go:126` — **the GUI-usable stub source** (md1 STRINGS). NOTE `md.WalletPolicyId`/`WalletPolicyIDStub` (`:30/:103`) take an UNEXPORTED `*descriptor` → NOT callable from `gui`; use the `…Chunks` variant.
- `bundle.Verify(derived, readback bundle.Bundle) error` — `bundle/verify.go:32`; `bundle.Bundle{MS1 string; MK1 []string; MD1 []string}` `:19`. Compares mk1↔md1 stub binding (both), mk1 fp/xpub/path, md1 exact string, ms1 recovered entropy.

## Topic 1 — Program enum + lockstep (post-T5)
Enum (`gui/gui.go:145-152`): `backupWallet=0, engraveXpub=1, engraveBundle=2, qaProgram=3`. **`qaProgram` is NON-navigable** (debug NFC cmd `"FOREVERLAURA!"`→`gui.go:1602` only); navigable bound = `engraveBundle`.
Lockstep sites: enum `:147-152`; dispatch switch `:1490-1500`; left-wrap `:1632-1635` (`<0→engraveBundle`); right-wrap `:1640-1643` (`>engraveBundle→0`); title switch `:1659-1666` (no qaProgram title); `npage` const `:1840` (`int(engraveBundle)+1`); `layoutMainPlates` panic-arm `:1848-1856`; `npages` const `:1859`; nav-tests `gui/bundle_program_test.go` + `gui/derive_xpub_program_test.go`.
**T6a-2 insertion:** new program BETWEEN `engraveBundle` and `qaProgram` (e.g. `engraveBundle=2, engraveSingleSig=3, qaProgram=4`); EVERY `engraveBundle`-as-bound ref (`:1634,1641,1840,1859`) moves to the new top; add dispatch case + title case + layoutMainPlates arm; update BOTH nav-tests (they hardcode `engraveBundle` as the wrap bound). `qaProgram` stays debug-only.

## Topic 2 — Typed seed entry (typed-ONLY; the footgun)
`seedEntryFlow(ctx,th) (bip39.Mnemonic, bool)` — `gui/derive_xpub.go:82` (12/24 words via ChoiceScreen → `inputWordsFlow` word-keyboard; never scan) — directly reusable. Spine doc `derive_xpub.go:98-105`; mnemonic scrub `:113-117`. **FOOTGUN CONFIRMED:** `scanner.Scan` (`gui/scan.go:28`) parses bip39 (`scan.go:61`) AND codex32 (`:70-73`) from NFC → T6a-2 seed MUST be `seedEntryFlow` only, never scan. (Scan IS reused for reading-back mk1/md1 in verify — fine; never for the secret seed.)

## Topic 3 — Picker + path constants
`ChoiceScreen{Title,Lead string; Choices []string}` `gui/gui.go:1347`; `(*ChoiceScreen).Choose(ctx,th)(int,bool)`. `pathPickerFlow(ctx,th)(bip32.Path,*chaincfg.Params,string,bool)` `derive_xpub.go:48` (two-stage sequential script→network). `scriptTypeChoices()` `:19-28` = SIX types (44 legacy,49 nested,84 native,86 taproot,48 multisig,87 multisig); `scriptTypePurpose` `:32-42`. Paths built inline `bip32.Path{purpose|hardened, coin|hardened, 0|hardened}` (`hardened=0x80000000` local const `:30`). `md.ScriptKind`→`bip380.Script` in `scriptForTemplate` `gui/md1_expand.go:82-115` (Wpkh→P2WPKH,Pkh→P2PKH,Tr→P2TR; **NO sh-wpkh arm**, `:96-97`).
**T6a-2:** clone `scriptTypePurpose`/path-build, RESTRICT to 4 single-sig (drop 48/87), REORDER BIP-84 first; default-one-tap + "Advanced" submenu is NET-NEW but trivial (chain two `ChoiceScreen.Choose`).

## Topic 4 — 3-leg derive + POLICY-BOUND mk1 stub
`deriveAccountXpub(m,passphrase,net,path) (xpub string, masterFP uint32, err)` `gui/derive.go:19` (base58 xpub + uint32 fp; `.Neuter()` `:50`). `mk.Encode(card Card)([]string,error)` `mk/encode.go:38`; `mk.Card{Network,Path,Fingerprint string; Stubs [][4]byte; Xpub string}` `mk/mk.go:132-139`. **T4 zero-stub** `derive_xpub.go:142` `Stubs:[][4]byte{{0,0,0,0}}` → T6a-2 replaces with `[][4]byte{stub}`, `stub,_=md.WalletPolicyIDStubChunks(md1Strings)`. **DROP** `stubZeroWarning` (`derive_xpub.go:237-256` title "Unbound Key Card", called `:157-159`). xpub→(chainCode[32],compressedPubkey[33],fp[4]) decode pattern EXISTS (`mk/encode.go compactFromXpub ~:117-157` via `hdkeychain.NewKeyFromString`; `bip380/bip380.go:409-420`); uint32→[4]byte `binary.BigEndian.PutUint32` (`bip380.go:99`) — ~5 lines GUI glue. **Caveat:** `EncodeSingleSig.fp` = the account `masterFP` (uint32 from `deriveAccountXpub`), NOT `xpub.ParentFingerprint()`.

## Topic 5 — Engrave reuse + THE ms1-CARD GAP
`bundleEngrave(ctx,th,cards []bundleCard)` `gui/bundle_flow.go:327`; `bundleCard{kind bundleCardKind; label string; strings []string; summary string}` `gui/bundle.go:29-37`; `bundleCardKind` `:21-27` = **`cardMK1=0,cardMD1=1` — NO ms1 kind.** `bundleMs1ReminderText()` `:374` shown via `showError` at end `:360`.
**KEY DESIGN GAP (recon-surfaced):** T5's bundle is PUBLIC-only (ms1 refused over NFC → reminder to hand-engrave). But T6 DERIVES the ms1 on-device → it should ENGRAVE it (the device's core purpose; user-confirmed "engrave ms1, option 1"). bundleEngrave has no ms1 path. → T6a-2 resolves this (see plan): full mode engraves ms1+mk1+md1, watch-only mk1+md1 + reminder.

## Topic 6 — verify-bundle flow + THE watch-only-verify CONSTRAINT
`bundle.Verify(derived,readback)` reads ms1 entropy on BOTH bundles (`verify.go`) → **does NOT support an ms1-less (watch-only) bundle** — flag for the plan. Read-back gather (reuse): `mk1GatherFlow`→`mk1Gatherer.collected()[]string` (`gui/mk1_inspect.go:156,77`); `md1GatherFlow`→`md1Gatherer.collected()` (`gui/md1_gather.go:72,57`). ms1 HAND-TYPED: `inputCodex32Flow(ctx,th,title)(any,bool)` `gui/gui.go:725` → `codex32.DecodeMS1` `codex32/mspayload.go:34`. PASS/FAIL via `showError` or the back-able pattern in `gui/verify_address.go`.

## Topic 7 — restore doc + sh-wpkh classifier risk (RESOLVED)
`address.Receive(desc *bip380.Descriptor,i)(string,error)` `address/address.go:24`; `Change` `:20`; `bip380.Descriptor{Title;Script;Threshold;Type;Keys}` `bip380.go:20-26`; Script enum incl. P2PKH,P2SH_P2WPKH,P2WPKH,P2TR `:56-67`. **classifier BREAKS sh(wpkh):** `classifyPolicy` `md/md.go:1257-1301` → single-key sh(wpkh) falls to `PolicyComplex` `:1300`; `scriptForTemplate` has no ScriptSh+single arm (`md1_expand.go:96-97`). BUT `address.Receive/Change` do NOT need the classifier — for `Type==Singlesig` they switch on `desc.Script` and handle `P2SH_P2WPKH` directly (`address/address.go:140-170`). **RESOLUTION (Option Y): build `*bip380.Descriptor` DIRECTLY** from the derived xpub + chosen script (literal pattern `md1_expand.go:60-77`): `Type=Singlesig`, one Key{Network,MasterFingerprint,DerivationPath,Children(recv/change),KeyData=xpub[32:65],ChainCode=xpub[0:32]}, `Script`= 44→P2PKH / **49→P2SH_P2WPKH** / 84→P2WPKH / 86→P2TR — BYPASS the classifier. All 4 restore-doc addresses (incl. sh-wpkh) work.

## Topic 8 — scrub + alloc gate
Scrub: replicate `deriveXpubFlow`'s `defer` mnemonic zero-loop (`derive_xpub.go:113-117`) + scrub entropy/ms1; `bundle.Verify` already scrubs its entropy copies (`verify.go:76-81`). Alloc gate `TestAllocs` (`gui/gui_test.go:93`/`:50`) asserts 0 allocs/op over `StartScreen.Flow` + `DescriptorScreen.Confirm` → (1) new menu render must stay alloc-free (string literals fine); (2) AVOID routing the restore-doc through `DescriptorScreen` (use `address.Receive/Change` + a plain screen).

## Classification
**REUSE as-is:** seedEntryFlow, deriveAccountXpub, mk.Encode/Card, EncodeSingleSig, EncodeMS1, WalletPolicyIDStubChunks, bundle.Verify, bundleEngrave/bundleCard, ChoiceScreen, mk1/md1GatherFlow+collected(), inputCodex32Flow, DecodeMS1, address.Receive/Change, showError, the hdkeychain xpub-decode.
**CLONE-with-tweaks:** pathPickerFlow/scriptTypePurpose→single-sig-only+reorder; deriveXpubFlow card-build→bound stub (drop stubZeroWarning); md1_expand.go:60-77 literal→per-script restore-doc descriptor; bundleMs1ReminderText→watch-only variant.
**NET-NEW glue:** the new program enum member + all lockstep edits + nav-test updates; default+Advanced picker UX; the orchestrator `engraveSingleSigFlow`; the ms1 engrave card/path; assembling the two `bundle.Bundle` structs; xpub→(chainCode,pubkey,fp[4]) wrapper; the restore-doc address screen; full/watch-only branch.
**Biggest risks to lock:** (1) enum insertion between engraveBundle/qaProgram (all bounds on engraveBundle move; both nav-tests). (2) bound-stub wiring order (xpub→EncodeSingleSig→WalletPolicyIDStubChunks→mk.Encode; masterFP not ParentFingerprint; drop stubZeroWarning). (3) **ms1-card gap** — T6 engraves the derived ms1 (full mode); bundleEngrave has no ms1 kind. (4) **watch-only verify** — bundle.Verify requires ms1 on both; needs a watch-only path. (5) sh-wpkh restore-doc → build descriptor DIRECTLY (Option Y, 49→P2SH_P2WPKH). (6) 0-alloc gate — keep menu alloc-free; restore-doc NOT via DescriptorScreen.
