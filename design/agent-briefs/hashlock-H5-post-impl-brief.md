You are the INDEPENDENT adversarial execution reviewer (opus tier) for the hashlock stage H5 implementation (device polish: F-487 reconcile screen with digest, method, chars and the mismatch sentence + the write-down line; F-480 per-digest provenance; F-484 lead inside the page band; F-485 the composer-state hook, emulator glue and the three-run walk; F-488 the refusal's next step). Fork branch `hashlock-h5` (merge of h5-a, h5-b, h5-c), worktree `/scratch/code/shibboleth/.tmp/seedhammer-hashlock-h5`, tip `8e605e1`, base main `b9a9a30`; engrave records branch `h5-records` at `4e2cf01f`; toolkit branch `h5-manual` at `b48af1c1`. Plan `design/IMPLEMENTATION_PLAN_hashlock_H5_device_polish.md` (R0 GREEN `5b77367`), spec `design/SPEC_hashlock_H5_device_polish.md` (`61a47e4`), the implementers' reports `design/agent-reports/hashlock-H5-implementation-{A,B,C,D}-report.md` (their deviations are claims to verify).

ONE QUESTION: over the whole diff `git -C <fork worktree> diff b9a9a30..8e605e1`, can you construct a state, input or sequence for which the device shows a digest that differs from the stored one, loses the operator's work, names a backup wrongly (the §8h forms on mixed / all-phrase / all-payload wallets), draws copy the spec does not say, or lets a test report PASS on a defect it names -- and does every claim in the four implementation reports hold at the tips?

Read-only on every branch; commit nothing; no sub-agents; never read any `.jsonl`; no phrase or preimage bytes in logs you keep. Build and run in your OWN detached copy: `rm -rf /scratch/code/shibboleth/.tmp/h5-review && git -C /scratch/code/shibboleth/seedhammer worktree add --detach /scratch/code/shibboleth/.tmp/h5-review 8e605e1` (Go `/scratch/code/shibboleth/.toolchain/go/bin/go`; whole-gui counts via `/scratch/code/shibboleth/mnemonic-engrave/scripts/gui-shard-test.sh ./gui/ 24`; remove the worktree when done).

## Already settled -- do not re-derive
- The controller re-ran the gates at `8e605e1` (numbers in the dispatch message) and RAN the three emulator walk runs (a unmutated PASS; b HOLD-before-confirm fails the pre-hold `null` assertion; c perturbed stored hash fails stored-equals-displayed and no earlier assertion), recorded in the continuity with the failing assertion named. No compile/vet/fmt findings.
- The plan closed R0 under a per-task build gate, three lenses and a fold verification; review the CODE against the SPEC. Secret-handling never gates (report leaks as Minor). `TestEngraveScreenReleasesResumeStateOnReturn` is a known load flake (F-490).

## Construct counterexamples -- an assessment without one is Minor at most
1. **Reconcile screen** (§1): drive HOLD on the harness for both methods and the longest phrase; the frame carries `hash <first8>..<last8>`, `method: <m>   chars: <n>` and both sentences byte-for-byte; the token equals the confirm modal's and `hashlockFirst8Last8` of the STORED hash; mutate the assignment order and show which test fails.
2. **Write-down line**: byte-exact as the spec; fit headroom re-measured (107 at the longest body).
3. **Provenance** (§2): construct the mixed wallet, the all-phrase wallet, the all-payload wallet, remove-and-readd, the phrase digest re-typed as hex, two paths one digest -- quote §8h's banner each time and judge it against the spec's forms; the zero-value-state HOLD (no panic); grep for any remaining `hashByPhrase`.
4. **Lead geometry** (§3): re-run the geometry test; measure lead ink under the nav rectangles and the readout budget yourself (raster, not text).
5. **Hook and walk** (§4): the tinygo twin is empty and `composerFlow` clears it on exit (test it: after the flow returns, the hook returns nil); `shComposerPathHashes()` reachable only in the js build; the walk parses the drawn token (mutate `hashlockFirst8Last8` to return the head twice and confirm the walk's assertion would fail -- reason from the code if you cannot run playwright); the `ok`-guard catches a trailing `out.ok = out.plates === 3`.
6. **Refusal** (§5): the sentence byte-exact, "(records count from 0)" present, fit at the longest noun with a two-digit index; mutate and show the frame test fails.
7. **Tests that cannot fail**: for each test the diff adds or edits, mutate the guard it names and quote the failure; a survivor on a normative guarantee is Critical.
8. **Records**: FOLLOWUPS closure headers on `h5-records` (placeholder `<FORK_MERGE_SHA>` still present is expected; the controller fills it), the H2 spec folds byte-exact against the shipped copy, the toolkit re-quote and `make lint` OK; firmware size at `8e605e1` vs `b9a9a30` re-measured, hook share "no measurable cost".
9. **The reports**: every count matches what you measure at the tips; a false count is Critical.

## Severity
Critical: a displayed digest that differs from the stored one; lost operator work; a hash assigned before HOLD; a test that cannot fail on a normative guarantee; a false claim in a report. Important: copy that differs from the spec, a missing case, an unsound assumption, a wrong banner form, a fit or geometry regression. Minor: secret-handling, wording, records.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H5-post-impl.md` (in the MAIN checkout at that absolute path; create; must not exist): findings `### C-n / I-n / M-n / N-n -- title` with command, verbatim output and the spec line; the banner table (wallet shape / banner text / spec form / verdict); the mutation table; the deviations with a verdict each; closing counts and GREEN / NOT GREEN. Return a two-line summary plus the path.
