You are the INDEPENDENT fold-verification reviewer (round 1, targeted) for the composer Stage 4 plan: `/scratch/code/shibboleth/mnemonic-engrave/design/IMPLEMENTATION_PLAN_composer_S4_acceptance.md`. The round-0 journey lens report is `design/agent-reports/composer-S4-plan-R0-r0-journey.md` (1C/12I/5M/2N; persisted at `ac2014e`); the fold is commit `fda1d9e` (`git diff d640875..fda1d9e -- design/IMPLEMENTATION_PLAN_composer_S4_acceptance.md`; the fold commit message lists the intended change per finding). Two follow-ups were filed for M-1 and N-2 (F-462, F-463 in `design/FOLLOWUPS.md`, the commit after the fold).

## The one question
Did the fold land every C/I/M/N of the r0 report as the report's hypothesis or a stated decline (M-4 was DECIDED, not declined: the shared master is kept and stated deliberate), and did it introduce nothing new -- a wrong needle, a value that does not match the report's quoted frames, a superseded string left behind, a row whose order no longer matches the shipped code?

## Method
Read-only; no commits; no sub-agents; read no `.jsonl` file. Compare the fold diff against the report finding by finding. For every value the fold PINS, check it against the report's quoted frame or, where the report gives a command, re-run the command by path (`/scratch/code/shibboleth/descriptor-mnemonic/target/release/md`, `/scratch/code/shibboleth/mnemonic-engrave/target/debug/me`, `~/.cargo/bin/mk`, `~/.cargo/bin/ms`): the 56-character keyless string and its chunk-set-id, the two-chunk fingerprinted template, the card chunk counts. Grep the plan for the superseded forms (`md15zfd`, `46-character`, `1-of-1`, `Seat keys into this template?` in the keyed arm, `--in -`, `7 strings`, the `now:` bound line on a relative lock, the key-order question on the keyed arm) and confirm each survives only where the plan describes what was retired. Run the plan's own checks (`scripts/plan-cite-check.sh`, `plan-glyph-check.sh`, `plan-table-check.sh`, `plan-stepref-check.sh`) and confirm the counts in the fold commit message. Do NOT re-walk the shipped code: the r0 lens did; your question is whether the fold matches what it measured.

Severity: a finding not folded and not declined with a reason, a pinned value that contradicts the report's frame, or a new defect in the folded text = Important; a wording slip = Minor/Nit. Do not pad.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/composer-S4-plan-R0-r1-fold-verification.md` (create; must not exist): per r0 finding VERIFIED / PARTIAL / NOT FOLDED with the evidence line; the propagation grep results; the plan-check counts; any new finding; closing counts. Return a two-line summary plus the path.
