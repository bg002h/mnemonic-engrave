You are implementer D for hashlock stage H5: **Task 6** (records). No fork code. Engrave worktree: `git -C /scratch/code/shibboleth/mnemonic-engrave worktree add -b h5-records /scratch/code/shibboleth/me-worktrees/h5-records master`; toolkit worktree: `mkdir -p /scratch/code/shibboleth/tk-worktrees && git -C /scratch/code/shibboleth/mnemonic-toolkit worktree add -b h5-manual /scratch/code/shibboleth/tk-worktrees/h5-manual master`. Your files: engrave design/SPEC_hashlock_H2_device.md (§4.5 write-down sentence + reconcile clause; §4.7 phrase-form sentence -- exact text from the plan's Task 6), engrave design/FOLLOWUPS.md (F-487, F-480, F-484, F-485, F-488 closure HEADERS prepared with the placeholder `<FORK_MERGE_SHA>` for the commit; the controller fills it at merge), toolkit docs/manual/src/40-cli-reference/43-ms.md (re-quote the reconcile screen and the confirm modal's write-down line as the plan's Task 2 specifies, byte-exact; `make lint` in docs/manual must print `[lint] OK` -- paste its tail). Do not edit the H5 spec or plan. Commit per repo with the trailers (no `-s` needed in engrave/toolkit).
RULES (all four implementers): the plan is `design/IMPLEMENTATION_PLAN_hashlock_H5_device_polish.md` at engrave master `<PLAN_GREEN_SHA>` (STATUS R0 GREEN) implementing `design/SPEC_hashlock_H5_device_polish.md` (`e03d8e7`); execute ONLY your tasks, exactly as written, test-first (RED quoted, then GREEN, every `MUTATION:` re-run once and reverted). The plan author left a gated tree at `/scratch/code/shibboleth/.tmp/h5-gate` with every task applied -- you may READ it to resolve a block whose context is unclear, never copy files from it wholesale and never modify it. Fork worktree: `git -C /scratch/code/shibboleth/seedhammer worktree add -b <YOUR_BRANCH> /scratch/code/shibboleth/.tmp/seedhammer-<YOUR_BRANCH> main` (main is `b9a9a30`; the fork keeps worktrees OUTSIDE the repo). Go is `/scratch/code/shibboleth/.toolchain/go/bin/go`, first on PATH; whole-gui counts only via `/scratch/code/shibboleth/mnemonic-engrave/scripts/gui-shard-test.sh ./gui/ 24`. Touch ONLY the files your tasks list (other implementers work in parallel on disjoint files; a file outside your list is a merge conflict for the controller -- if a task needs one, STOP and report). Commit per task with the plan's commit messages, `git commit -s -F <file>` (fish shell), trailers `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01Fs3bg7TRfuSaFcCEkskwXA`; stage paths explicitly. Nothing pushed; no `main`/`master` commits; no sub-agents; never read any `.jsonl`; no phrase or preimage bytes in any log you keep. If a step is wrong, do the MINIMAL correct thing and record the deviation. Every count in your report comes from a run at your tip, captured to a file under `/scratch/code/shibboleth/.tmp/` and quoted from there. FINAL ACTION: write your report (path below; create; must not exist) with per-task commit SHAs, RED evidence, PASS counts, every mutation re-run with its failing line, every deviation; return a two-line summary plus the path and your branch tip.
Report: /scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H5-implementation-D-report.md
