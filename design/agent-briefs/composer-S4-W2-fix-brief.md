You are the SAME single implementer, resumed, now fixing the defect you measured and stopped on (report `composer-S4-implementation-report.md` Task 3): W-2 in `/scratch/code/shibboleth/mnemonic-engrave/design/S4_journey_walk_2026-09-02.md`. The controller has decided the fix shape; execute it, do not redesign it.

## Where -- a SEPARATE branch, so the fix can be flashed before Task 3 finishes
```
cd /scratch/code/shibboleth/seedhammer && git worktree add /scratch/code/shibboleth/wt-composer-s4b -b composer-s4b main
```
(`main` is `60bee002`.) Do NOT put this on `composer-s4-emu`; the two merge separately. Same environment as before (Go 1.26.7 by path, `CGO_ENABLED=0 GOPROXY=off GOTOOLCHAIN=local`, `-mod=readonly`, `TMPDIR=/scratch/code/shibboleth/.tmp`). Commit signed-off (`git commit -s -F <file>`), keep the trailer lines; never push, never flash.

## The fix (decided)
`gui/composer_paged.go` `composerPickScreen`: every drawn ROW gets a `Clickable` hit area, exactly as `ChoiceScreen` gives each `Choice` a `click` (`gui/gui.go`, `ChoiceScreen.Choose`: `if c.click.Clicked(ctx) { s.choice = i }`, and its `Draw` wraps each row in `op.Input(...).Clip(...)`). A tap on a row SELECTS it (moves `sel` to `start + i`); Button3 still TAKES the highlighted row; Up/Down keep working; Button2 still pages. Do not add auto-repeat, drag, or arrows: the row-tap is the sibling pattern the operator already knows from every other list on this device. The hit area is the row's drawn rectangle as `composerPageLines` lays it out (the lead header and its spacer are not rows). Keep one `Clickable` per VISIBLE row index (re-used across pages), not one per list element.

## The test (must fail first, on the TOUCH path)
A regression test on the touch harness (`gui/start_screen_touch_test.go`: `runUITouch`, `tap(r, d, pos)`) that drives the REAL flow (`composerFlow` via `walletPolicyFlow`, or `composerShapeFlow` directly -- your call, but through the screens, not by calling `composerPickScreen` with a fake list): tap the `3` row on `Path 1: how many keys?`, assert the threshold picker reads `how many must sign? 1 2 3`; then tap `2`; then, on the path list with one path present, tap `Done` and assert the walk reaches the next screen (`Sorted keys, or your order?` on a one-path list). Row coordinates come from the frame's own layout (measure them from `composerPageLines`'s output or the drawer, as the start-screen touch tests measure the arrow points), never from constants copied from the JS walks. Run it on `60bee002` FIRST and paste the failure (the take yields `n = 1`; `Done` opens the path editor) -- that is W-2 reproduced on the harness -- then make it pass. Also add the mutation line to the commit message: with the `op.Input` wrapper removed the test fails again with that message.

## Gates before the commit
`gofmt -l gui/`; `go vet ./gui/` (the two pre-existing ArtifactDir findings excepted); `go test -count=1 -run '^TestComposer' ./gui/`; the sharded gui runner `/scratch/code/shibboleth/mnemonic-engrave/scripts/gui-shard-test.sh ./gui/ 24` all ok; `CGO_ENABLED=0 go test -count=1 ./cmd/emu/`; `GOOS=js GOARCH=wasm go vet ./cmd/emu/`. Then, on the EMULATOR (build `emu.wasm` from THIS worktree, serve it on a fresh port, Playwright): repeat your 205-tap sweep's positive form -- tap the `3` row on the `n` picker and paste the frame that follows (`how many must sign? 1 2 3`), and tap `Done` with one path present and paste the frame. That is the proof the fix reaches the machine's input path, and the plan's Task 3 driver will rely on it.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/composer-S4-W2-fix-report.md` (create; must not exist): the worktree and branch, `git log --oneline main..HEAD`, the failing-then-passing test output, the mutation output, every gate's trimmed output, the emulator frames, and anything you decided or could not do. Return a two-line summary plus the path. Do NOT touch `wt-composer-s4-emu` or `wt-engrave-s4-emu` in this step; Task 3 resumes after the fix is on fork `main`. No sub-agents; read no `.jsonl` file.
