You are the INDEPENDENT verification reviewer (sonnet tier) for the F-503 implementation — "the preimage KIND under the id `hash` is inert at every length". This is the plan's §4 post-implementation check, and it is MECHANICAL: everything design-level was settled by the operator's rulings and the controller's gate (14 blocks / 0 FAIL) before the implementer started. Do not re-open the rule, do not audit anything F-503 did not touch, do not propose alternatives.

The artifacts: plan `design/IMPLEMENTATION_PLAN_hashlock_F503_hash_kind_inert.md` in mnemonic-engrave (master `<PLAN_SHA>`); the implementer's report `design/agent-reports/f503-implementation-report.md`; branches me `f503` (`<ME_TIP>`, base `<ME_BASE>`), fork `f503` (`<FORK_TIP>`, base main `3cadffa8`), ms `f503-records` (`<MS_TIP>`, base `a994a99`).

ONE QUESTION: does each branch do exactly what the plan says, does every test the diffs add actually fail on the defect it names, and is every number in the implementer's report true?

Read-only on every branch; commit nothing; no sub-agents; never read any `.jsonl`. Build and RUN in your own detached worktrees with their own target dirs (me: `git -C /scratch/code/shibboleth/mnemonic-engrave worktree add --detach /scratch/code/shibboleth/me-worktrees/f503-check <ME_TIP>`, `PATH=$HOME/.cargo/bin:$PATH TMPDIR=/scratch/code/shibboleth/.tmp CARGO_TARGET_DIR=/scratch/code/shibboleth/.tmp/f503-check-target`; fork: `git -C /scratch/code/shibboleth/seedhammer worktree add --detach /scratch/code/shibboleth/.tmp/seedhammer-f503-check <FORK_TIP>`, Go at `/scratch/code/shibboleth/.toolchain/go/bin/go` first on PATH). Mutate only there; revert after each; remove both worktrees and the target dir when done.

## Already settled — do not re-derive
- The RULE: kind 0x03 under the id `hash` is inert at EVERY length; every other id keeps H0's BIP-93-wide device behaviour (the seam row `bip93-plain-payload-0x03` must still be `device_admits: true`). Operator ruling 2026-09-06.
- The plan's blocks were built and tested by the controller before the implementer ran. Compile findings on unmodified plan text are not findings; a block the implementer CHANGED is.
- The three `history_purge` failures need `/usr/bin/zsh` and are box-local (F-500). Fork `go vet ./...` is red at baseline (F-494). Neither is a finding.
- Secret-handling never gates (operator ruling 2026-08-27): log with a reproduction, never as blocking.

## Check, in this order — every claim by running it
1. **Diff vs plan.** `git diff <base>..<tip>` on each branch, file by file, against the plan's File structure table. Any file outside the table, or any table file untouched, is Important. Quote the deviation.
2. **Every mutation the plan names, re-run by you** (Task 1 Step 5; Task 2 Step 2 and Step 3), plus these of your own: (a) in the fork, `isHashIdPreimageKind` comparing the id case-INsensitively (`strings.EqualFold`) — does anything red? (b) in the fork, `IsPreimageKind` dropping the `Unshared` test — the share row must red; (c) in me, `id_is_hash` hard-coded `true` — which test reds? For each: caught by which test with its quoted failing line, or SURVIVED. A survivor on the F-503 rule itself is Critical.
3. **The seam file is one file.** `sha256sum` both copies (me `crates/me-cli/testdata/codex32_seam_vectors.json`, fork `sysw/testdata/codex32_seam_vectors.json`) — equal to each other AND to both pinned literals (`SEAM_VECTORS_SHA256` in `crates/me-cli/tests/codex32_seam.rs`, `seamVectorsSHA256` in `sysw/codex32_seam_test.go`). Any inequality is Critical. Confirm the four new rows' `chars` fields equal the real character counts and that `bip93-plain-payload-0x03` still reads `device_admits: true`.
4. **The refusal texts, from the built binary.** Build `me` at the me tip and run `me sysw pack --pack-preimage --no-passphrase` over: the F-503 string `ms10hashsqvqqqqqqqqqqqqqqqqqqqqqqqqqqmv3lqlgkn6s5c`; the two other `hash`-id rows (74 and 77 characters, from the seam file); the wrong-id 33-byte row; the UPPERCASE plate; the well-formed plate. Quote each exit code and first stderr line. Assert: no body claims the id is not `hash` when it IS; the 1-in-256 sentence appears ONLY for a 33-byte payload; exactly one refusal per run.
5. **Device classification, from the fork tree.** A `go test`-driven check (a temporary test you delete) or an existing test: `sysw.Classify` on the same six strings — the three `hash`-id 0x03 rows are `ClassUnknown`, the kind-0x00-under-`hash` control is `ClassCodex32Secret`, the plate is `ClassPreimage`, and the H0 row `ms10testsqv0qqqqqqqqqqqqqqqqqqqqqqq8mzk8tjfdnjn5` is STILL `ClassCodex32Secret`.
6. **Suites.** me: `cargo nextest run --locked -p mnemonic-engrave --no-fail-fast` (expect 634/631/3/2 or the report's number — if they differ, say which is right), `cargo fmt --all -- --check`, clippy. fork: `go test $(go list ./... | grep -v /gui$) -count=1`, `gofmt -l codex32/ sysw/`, `scripts/gui-shard-test.sh ./gui/ 24` (expect 1289). ms: `cargo nextest run --locked` at the records tip.
7. **Records.** The ms spec sentences say what the plan's Task 3 Step 1 says; F-503's closure names the three real SHAs; the me CHANGELOG entry is under `[Unreleased]`. Every count in the implementer's report matches yours — a false count is Critical.

## Severity
Critical: the F-503 rule not implemented as ruled (any `hash`-id 0x03 length still a device seed, or any other id newly refused); a test that cannot fail on the rule; the two seam copies differing; a false count in the report. Important: a file outside the plan's table, a missing plan step, a refusal body still saying something false. Minor/Nit: wording, records.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/f503-post-impl-check.md` (create; must not exist): findings `### C-n / I-n / M-n / N-n — title` with the command and verbatim output; the mutation table; the six-string classification table (host exit + first line, device class); the suite numbers; closing counts and GREEN / NOT GREEN. Return a two-line summary plus the path.
