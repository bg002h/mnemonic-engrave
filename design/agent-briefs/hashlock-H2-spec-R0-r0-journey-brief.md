You are the INDEPENDENT journey + adversarial reviewer (opus tier) for round 0 of the R0 gate on `design/SPEC_hashlock_H2_device.md` in mnemonic-engrave (`/scratch/code/shibboleth/mnemonic-engrave`, spec at `bfd042e`). Sources: `design/BRAINSTORM_hashlock_phrase.md` (§3.7, §4.4, §4.6, §5, §7.1), `design/SPEC_wallet_policy_composer.md` (§6c, §7, §8), `mnemonic-secret/design/SPEC_ms_hashlock.md` §4.3/§7, the fork at `/scratch/code/shibboleth/seedhammer` main `c4a64fc` (read-only), and the composer's own walk scripts under `cmd/emu/walk_*.js` (how the device is actually driven).

ONE QUESTION: walk the journeys a real operator takes with this device leg and, at every step, ask what they have in hand, what the device does, and WHAT ELSE they might reasonably do — and find the moment where the spec is silent or wrong in a way that is worse than telling them nothing.

Read-only everywhere; commit nothing; no sub-agents; no scratch copies (a fidelity lens runs concurrently); read no `.jsonl`. Do NOT re-review the citations, the port's constants, or the phrase-rule text (the fidelity lens owns those).

## Journeys to walk (classify each divergence: refusal / warning / default / not our concern / documentation only; a divergence earns a spec change ONLY if the wrong outcome is worse than saying nothing)
1. **The happy path, twice.** A person building a policy with a hashlock path on the SH2: from `Which hash?` they choose `Type a hashlock phrase`, type a six-diceware-word phrase, pick Hardened, wait about 10 s, read `hash first8..last8`, CONTINUE. Then the same person on the host runs `ms hashlock` to make the plate and the `hash:` record. What do they have in hand at the end — and does anything they hold let them SPEND later? (The preimage exists on the host's plate only; the device never showed it. Is that what they expect? Does the confirm modal tell them the preimage is NOT on this device and where it must be?)
2. **The wrong order.** They typed the phrase on the device FIRST and then, weeks later, type it on the host — with a different method (they forgot), with different capitalisation, with a trailing space the host's file carried. Which of these the device could have prevented, and how does the spec's copy fare?
3. **The mismatch they never see.** They set a path's hash by phrase on the device, then also load a payload with a `hash:` record made from the same phrase — two rows now claim the same digest, or (if the method differed) two different digests. What does `Which hash?` show, and can they tell?
4. **Back, power, and the countdown.** Back during the 10 s derivation; power loss during it; Back on the confirm modal after reading the digest; Back on the method modal. For each: is the path's hash unchanged, and does the operator KNOW it is unchanged? Does re-entering `Which hash?` show the state?
5. **Reuse.** The same person builds a second policy and, naturally, types the same phrase. The device cannot know. Is the §4.5 reuse copy at the RIGHT moment (before they commit) and is it strong enough given §3.7 (a spend publishes the preimage, and anyone can then test guesses at the phrase)?
6. **The short phrase.** They type `hunter2` and pick Hardened: the 72-days modal. They pick SHA-256: the brainwallet modal. Both proceed (L12). Is the copy's number honest for 7 characters (it says "a 20-character phrase falls in about 72 days")? Is there any length at which the device should refuse rather than warn, given L12 says warn only — and if not, does the modal make the consequence concrete enough?
7. **The typed-plate temptation.** They have a preimage plate (from `ms hashlock --out`) and try to enter it on the device: at `Which hash?` → `Type a hashlock phrase` (refused as ms1-shaped — what do they do next?), at `Type 64 hex` (they cannot: the plate is 75 characters of codex32, not hex), at `Backup Wallet` → `M*1 STRING` (refused by H0). Is there ANY route that gets a preimage plate's DIGEST onto the device this cycle, and is the spec honest that there is none?
8. **The all-hashed policy.** Every path carries a hash: §8h fires. With the phrase route, does the operator understand that the phrase is what they must keep, and that losing the method line loses the funds?

## Severity
Critical: a journey that ends with funds unspendable or a digest the operator believes is right and is not, with the spec silent. Important: a moment where the operator is left worse off than by no feature (a confusing refusal, a state they cannot see, copy that misleads). Minor: documentation only.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H2-spec-R0-r0-journey.md` (create; must not exist): one section per journey with the step-by-step (in hand / does / else), each divergence classified, then findings `### C-n / I-n / M-n / N-n — title` with the spec section and a SUGGESTION; closing counts. Return a two-line summary plus the path.
