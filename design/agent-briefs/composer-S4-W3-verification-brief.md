You are the INDEPENDENT fold-verification reviewer (targeted) for one finding from the S4 journey walk on the SeedHammer II: W-3 in `/scratch/code/shibboleth/mnemonic-engrave/design/S4_journey_walk_2026-09-02.md` (the composer's paged widgets draw their longest lines under the navigation buttons: the Template-ID's last hex digit and the mk encode tails are hidden). The fix is fork branch `composer-s4c`, tip `0b49f66c16aae1a055b8403a24b242acd3548710` on `3cc71d9b` (worktree `/scratch/code/shibboleth/wt-composer-s4c`); the implementer's report is `design/agent-reports/composer-S4-W3-fix-report.md` -- re-derive, do not trust. One question: does a TAP on any visible row of `composerPickScreen` now select that row on the machine's own input path (PointerEvents), can the regression test fail, and did nothing else move?

Read-only: copy the worktree with `cp -r` to `/scratch/code/shibboleth/.s4c-verify/` for every mutation (never dirty the worktree; do not run `git checkout` in a copied worktree -- it shares the gitdir). Go: `/scratch/code/shibboleth/.toolchain/go/bin` on PATH, `CGO_ENABLED=0 GOPROXY=off GOTOOLCHAIN=local`, `-mod=readonly`, `TMPDIR=/scratch/code/shibboleth/.tmp`. Do NOT spawn sub-agents; commit nothing; read no `.jsonl` file.

## Verify
1. `git diff 3cc71d9b..0b49f66c16aae1a055b8403a24b242acd3548710 --stat` and the hunks: the change is confined to `gui/composer_paged.go` (lines wrapped AND centred inside the band left of the navigation column, sharing the W-2 hit-area bound), the capacity pins the report names (`gui/composer_measure_test.go`, spec §13 values if moved), and the new geometry test; no other production file changed. A hunk that alters the consent screen, the navigation layout, or the row hit areas beyond sharing the bound is Important.
2. The GEOMETRY test: it renders the keyed AND keyless stub screens through the real widget and asserts from the frame's own ops that no text rectangle intersects either navigation button on any page; it FAILS on `3cc71d9b` (throwaway worktree at that revision) naming the Template-ID line, passes on the fix, and fails again in a copy where the right bound is restored to the full width. Prove the mutated line RAN.
3. Every `composerPageLines` user benefits: drive, on the harness, the stub screen (keyed and keyless), the mapping review, the seating pick list with a long prompt, and `Which hash?` -- write throwaway geometry checks in the copy if the fix's test does not cover them; any screen still drawing a glyph under a button is Important.
4. The capacity pins: recompute the per-frame capacities the report states (stub screen, pick list) from the folded layout and confirm the spec §13 numbers and `composer_measure_test.go` agree with the measurement, not with each other.
5. Gates as the brief names them: `gofmt -l gui/` (the three pre-existing `transaction*` files excepted), `go vet ./gui/`, `-run '^TestComposer'`, the sharded gui runner (count = 1188 + the new tests), `go test ./cmd/emu/`, js vet, `scripts/test-32bit.sh`, the oraclelive build, and the firmware size against `1,580,580 B flash / 62,800 B RAM` at `3cc71d9b` -- report the delta.
6. The emulator proof: build `emu.wasm` from the copy, drive the keyless arm to the Template screen (Playwright; `shSysw("none")` after the boot offer; rows by `shTargets()`), screenshot it, and LOOK at the PNG: the Template-ID's 32nd hex digit and the `mk encode` line's tail must be clear of the buttons. Paste the path and `shScreen()`.

Severity: a glyph still drawn under a button on any `composerPageLines` screen, a test that cannot fail, a capacity pin that disagrees with the measurement, or a hunk outside W-3 = Important; cosmetic = Minor/Nit. Do not pad.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/composer-S4-W3-verification.md` (create; must not exist): per item VERIFIED / NOT VERIFIED with output; the size delta; closing counts. Return a two-line summary plus the path.
