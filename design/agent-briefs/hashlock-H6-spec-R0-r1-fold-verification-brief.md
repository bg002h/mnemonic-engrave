You are the INDEPENDENT fold-verification reviewer (sonnet tier, narrowly scoped) for round 1 of the R0 gate on `design/SPEC_hashlock_H6_preimage_plates.md` in mnemonic-engrave (`/scratch/code/shibboleth/mnemonic-engrave`). Round 0 ran three lenses (`design/agent-reports/hashlock-H6-spec-R0-r0-{fidelity,journey,tests}.md`); the round-0 fold is ONE commit, `<FOLD_SHA>`, over the spec at `a0f832d0`; its message and the spec's `## R0 round 0 folded here` paragraph map each Critical/Important to a change or a stated decline.

ONE QUESTION: does the fold fix every Critical and Important the three reports raised (or decline it with a true reason), with every NEW number, citation, copy string and mechanism true when YOU measure it (QR module counts with the fork's `qr.Encode` at ECC-L; plate line counts with `backup.CharsPerLine`; `assertModalBodyFits` for modal bodies; grep for citations at fork `fb0dd04`, engrave master, ms `504ff46`), and no new contradiction inside the spec or against the settled rulings recorded in `design/CONTINUITY_composer_2026-09-01.md` ("H6 BRAINSTORM" and "Group A rulings" entries)?

Read-only on every repo; execution only in your own detached fork worktree `git -C /scratch/code/shibboleth/seedhammer worktree add --detach /scratch/code/shibboleth/.tmp/h6-r1 fb0dd04` (Go `/scratch/code/shibboleth/.toolchain/go/bin/go`; remove when done); commit nothing; no sub-agents; never read any `.jsonl`.

## Verify (execute; quote)
1. Per C/I finding: the diff `git diff a0f832d0..<FOLD_SHA> -- design/SPEC_hashlock_H6_preimage_plates.md` fixes it as stated, or the decline reason is true.
2. Every number the fold added or changed, re-measured for exactly the folded text (a number quoted from a report is only as good as the body it measured).
3. Superseded phrasing grepped (a fold fails by incomplete propagation); the rulings still hold in the folded text.
4. Read the folded spec as a hostile implementer: any sentence two implementers would read differently is Important.

## Severity
A C/I not fixed and not truly declined, a new false number or citation, a new contradiction = Important. Wording = Minor/Nit. A clean round closes R0 for this spec.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H6-spec-R0-r1-fold-verification.md` (create; must not exist): a table (finding, fold change, verdict FIXED / DECLINED-OK / NOT FIXED), the executed checks with outputs, new-defect findings, closing counts, a plain GREEN / NOT GREEN. Return a two-line summary plus the path.
