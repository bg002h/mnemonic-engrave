Task 7 for the H2 implementer (same agent, same branches): burn down the H2-owned follow-up F-474 in-phase, and reconcile F-475's owning phase. Rule: a follow-up owned by a phase is burned down before that phase closes; the H2 plan scheduled neither, so the controller adds this task.

**F-474 -- `unlock-kdf-names-the-refused-record`** (engrave `design/FOLLOWUPS.md`): `seal.AdmitSection` refuses an encrypted section WHOLE when any record is not on the allow-list, and `gui/unlock_kdf.go`'s `default:` arm renders `ErrRecordNotPermitted` as "Payload unreadable." after a successful passphrase. The neighbouring arms (`ErrTooManyRecords`, `ErrCodex32TooLong`) already name what was wrong. Do the same:
1. In `seal`: make the refusal carry the record INDEX (records count from 0, as `me` says) and its CLASS (the `seal.Classify` result or the reader's kind name -- for a preimage plate, the H0 predicate `codex32.IsPreimage`), so the gui can render both. Test first in `seal`: a section carrying a preimage plate as record 1 is refused with index 1 and the preimage class; a section carrying a plain BIP-93 share (not on the allow-list) is refused with its index and class. Build the sections in Go with the fork's own seal writer/test helpers (the released `me` cannot pack a preimage plate by design -- do not try).
2. In `gui/unlock_kdf.go`: a named arm for `ErrRecordNotPermitted` whose copy says which record and what it is, in the style of the neighbours and of H0's reader copy ("This record is a hashlock preimage, not a seed. It is not engraved as one."), e.g. `Record 1 is a hashlock preimage, not a seed. This payload cannot be unlocked here.` Render through the existing modal path; add the body to the modal-fit table if that gate covers this modal (`modal_fits_test.go`). Test first on the touch harness: the unlock flow with a section that carries a preimage plate reaches that copy (RED = "Payload unreadable.").
3. `MUTATION:` comments as the plan's tests carry them; run each once.
4. Records: FOLLOWUPS F-474 CLOSED by the fork commit; **F-475: change its owning phase from H2 to H3** in the header and add one line saying the H2 plan re-scheduled it (H2 does not touch `codex32_seam_vectors.json`); fork CHANGELOG line for F-474. Commit per repo (`-s` on the fork, the trailers everywhere), then re-run the final gates (four packages; `gui` through the shard script; vet; gofmt) at the new tips and APPEND a `## Task 7` section plus the new gate tails to the implementation report; commit it on the engrave branch.

Constraints unchanged: no `main`/`master` commits, nothing pushed, no phrase/preimage bytes in logs, never read any `.jsonl`. Return two lines: outcome + both new branch tips.
