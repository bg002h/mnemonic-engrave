You are the PUSH + RELEASE agent (sonnet tier) for mnemonic-engrave at `/scratch/code/shibboleth/mnemonic-engrave` (GitHub `bg002h/mnemonic-engrave`). The release commit is master tip `f94c9034497ea35cf917f8f583d45d1389480ded` (subject begins `me 0.8.1 --`); the fable decision `design/agent-reports/decision-me-0.8.1-release-and-flash-rule.md` §"The exact sequence" and §"Abort rules (Q1)" are your script -- read them first and follow steps 2-5 exactly; step 6 (records) is the controller's.

Preconditions (stop and report on any miss): `git -C <repo> rev-parse master` = `f94c9034497ea35cf917f8f583d45d1389480ded`; `git -C <repo> status --short` empty; `git -C <repo> ls-remote origin refs/heads/ci/staging` empty; `git -C <repo> tag -l v0.8.1` empty.

Step 2: `scripts/push-via-staging.sh master` (it stages the tip on `ci/staging`, waits for the required `test (rust + go)` context, pushes master, deletes the staging ref). Run it in the FOREGROUND with a long timeout (it takes several minutes); quote its tail verbatim. Any "Bypassed rule violations" line is FAILURE: do not tag; report. Then `git fetch origin` and confirm `origin/master` = `f94c9034497ea35cf917f8f583d45d1389480ded`.
Step 3: write the tag message to a file (first line: `me v0.8.1 -- hashlock preimage plates refused by name; ms-codec 0.8`; then two lines summarising H0/H1b/F-454 from the CHANGELOG's 0.8.1 section), `git tag -a v0.8.1 f94c9034497ea35cf917f8f583d45d1389480ded -F <file>`, confirm `git cat-file -p v0.8.1 | head -2` shows object `f94c9034497ea35cf917f8f583d45d1389480ded`, `git push origin v0.8.1`.
Step 4: find the tag-event run (`gh run list --repo bg002h/mnemonic-engrave --commit f94c9034497ea35cf917f8f583d45d1389480ded --json databaseId,event,status,conclusion`), `gh run watch <id> --repo bg002h/mnemonic-engrave --exit-status`, then per-job conclusions including `assemble + sign + release`. If that job FAILS after the tag is pushed: leave the tag alone (never delete or move a pushed tag); report.
Step 5: `gh release view v0.8.1 --repo bg002h/mnemonic-engrave --json url,assets` -- expect 7 assets (4 tar.gz, windows zip, SHA256SUMS, SHA256SUMS.minisig). In a scratch dir under `/scratch/code/shibboleth/.tmp/`: download `SHA256SUMS*` and the linux-amd64 tarball; `minisign -Vm SHA256SUMS -P RWQPmgBXsuw5yi8W0SfDr8KF+IqY/Z5U2p724emSODS1UPfJBP3agbKW`; `sha256sum -c --ignore-missing SHA256SUMS`; untar; `./*/me --version` (expect `me 0.8.1`); then `printf '%s\n' ms10hashsqw46h2at4w46h2at4w46h2at4w46h2at4w46h2at4w46h2at4w46kzv2ncy60u7z9c | ./*/me sysw pack --out ./p.bin; echo exit=$?` -- expect exit=4, stderr naming "hashlock PREIMAGE plate (kind 0x03)", and no p.bin. Any miss = report NOT released even though the tag exists.

Rules: full SHAs and `--repo` on every gh call; `git -C` / absolute paths everywhere; no commits on master (the controller is frozen off master until you report the master push done -- report that milestone in your final summary); never read any `.jsonl`; do not edit FOLLOWUPS or continuity.

Report (your final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/push-me-v0.8.1-release.md` (create; must not exist; do NOT commit it -- the controller commits it) with every command, the staging script's tail, run ids, per-job conclusions, the release url, the asset list, the verification outputs. Return three lines: master-push outcome, release outcome (RELEASED / NOT RELEASED + why), the path.
