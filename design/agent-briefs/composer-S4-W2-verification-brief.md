You are the INDEPENDENT fold-verification reviewer (targeted) for one finding from the S4 journey walk on the SeedHammer II: W-2 in `/scratch/code/shibboleth/mnemonic-engrave/design/S4_journey_walk_2026-09-02.md` (the composer's pick lists have no per-row touch target, so only a page's first row is selectable on the touch-only device). The fix is fork branch `composer-s4b`, tip `<W2_FIX_SHA>` on `60bee002` (worktree `/scratch/code/shibboleth/wt-composer-s4b`); the implementer's report is `design/agent-reports/composer-S4-W2-fix-report.md` -- re-derive, do not trust. One question: does a TAP on any visible row of `composerPickScreen` now select that row on the machine's own input path (PointerEvents), can the regression test fail, and did nothing else move?

Read-only: copy the worktree with `cp -r` to `/scratch/code/shibboleth/.s4b-verify/` for every mutation (never dirty the worktree; do not run `git checkout` in a copied worktree -- it shares the gitdir). Go: `/scratch/code/shibboleth/.toolchain/go/bin` on PATH, `CGO_ENABLED=0 GOPROXY=off GOTOOLCHAIN=local`, `-mod=readonly`, `TMPDIR=/scratch/code/shibboleth/.tmp`. Do NOT spawn sub-agents; commit nothing; read no `.jsonl` file.

## Verify
1. `git diff 60bee002..<W2_FIX_SHA> --stat` and the hunks: the change is per-row `Clickable` hit areas in `gui/composer_paged.go` (one per VISIBLE row index, the row's drawn rectangle as `op.Input(...).Clip(...)`, a tap moving `sel`), on `ChoiceScreen`'s pattern; Up/Down, Button2 paging and Button3 take unchanged; no other production file changed beyond what the report declares. Any hunk that changes what a page draws, the wrap rule, or the take semantics is Important.
2. The new touch-path test drives the REAL flow through `runUITouch` + `tap` (not `composerPickScreen` with a fake list), measures row positions from the layout (not constants), and: on `60bee002` (checkout the base into a second copy, or `git stash`-free: `git worktree add` a throwaway at 60bee002) it FAILS with the W-2 shape (`n = 1`; `Done` opens the path editor); on the fix it passes; with the `op.Input` wrapper removed in the copy it fails again with the pasted message. Prove the mutated line RAN.
3. All four call sites benefit: drive, on the touch harness, at least `Path 1: how many keys?` (row 3), the `Spend paths` list (`Done` with one path present), `Which hash?` (row 2, `Type 64 hex`) and `Seat keys` (row 2) -- write throwaway tests in the copy if the fix's test does not cover them; a site where a tap still does not select is Critical.
4. Gates as the brief names them: `gofmt -l gui/`, `go vet ./gui/` (two pre-existing ArtifactDir findings excepted), `-run '^TestComposer'`, the sharded gui runner (`/scratch/code/shibboleth/mnemonic-engrave/scripts/gui-shard-test.sh ./gui/ 24`, count = 1186 + the new tests), `go test -count=1 ./cmd/emu/`, `GOOS=js GOARCH=wasm go vet ./cmd/emu/`, `scripts/test-32bit.sh`, the oraclelive build; and the firmware size (`nix develop -c tinygo build -size short -o /dev/null -target pico-plus2 -stack-size 16kb -gc precise -opt 2 -scheduler tasks ./cmd/controller` with `/nix/var/nix/profiles/default/bin` on PATH) against 1,579,940 B flash / 62,800 B RAM at `60bee002` -- report the delta.
5. The emulator proof in the report (tap the `3` row → `how many must sign? 1 2 3`; tap `Done` → the next screen): reproduce it once yourself (build `emu.wasm` from the copy, serve on a fresh port, Python Playwright, `shTap` at the row's geometry) and paste the frames.

Severity: a tap that does not select on any of the four sites, a test that cannot fail, or a hunk outside W-2 = Important (a still-untouchable site = Critical); cosmetic = Minor/Nit. Do not pad.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/composer-S4-W2-verification.md` (create; must not exist): per item VERIFIED / NOT VERIFIED with output; the size delta; closing counts. Return a two-line summary plus the path.
