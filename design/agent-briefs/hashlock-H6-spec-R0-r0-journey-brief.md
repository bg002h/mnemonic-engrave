You are the INDEPENDENT journey-walk reviewer (opus tier) for round 0 of the R0 gate on `design/SPEC_hashlock_H6_preimage_plates.md` (engrave master `a0f832d0`). The brainstorm's journey walk (`design/agent-reports/hashlock-H6-brainstorm-journey-questions.md`) produced 14 questions that are now settled; walk the SPEC, not the brainstorm.

ONE QUESTION: walk the operator through every journey the spec defines and find where the spec is SILENT -- a step whose wrong outcome is worse than telling the operator nothing. At each step: what they have in hand exactly; what the device or host does as specified; what ELSE they might do and what happens then; classify refusal / warning / default / not our concern / documentation only.

SETTLED (do not reopen): the operator's nine brainstorm decisions and the Group A rulings recorded in `design/CONTINUITY_composer_2026-09-01.md` (entries "H6 BRAINSTORM" and "H6 BRAINSTORM -- Group A rulings") and applied in the spec: forms (ms1 string, or phrase+method; QR only with the phrase form, below the text, constant time KEPT with the encoder raised); form-specific bands; mk1 stub locator; entry from the composer at HOLD and from the wallet-policy payload (preimage record or new phrase record, Rust first); hybrid architecture (composer-native + a Hashlock plates flow under the Wallet Policy door); classes secret, sealed by default with a device-cannot-unlock warning, no-seal flag for the flow; `--pack-preimage`; the H6 admission path requires the `hash` id; free text warns-and-cuts; retention accepted. Review the SPEC against those decisions, the fork at `fb0dd04`, engrave master and ms `504ff46` -- not the decisions themselves. Secret-handling never gates. Read-only on every repo (execution only in your own detached fork worktree `git -C /scratch/code/shibboleth/seedhammer worktree add --detach /scratch/code/shibboleth/.tmp/h6-lens-<key> fb0dd04`, Go `/scratch/code/shibboleth/.toolchain/go/bin/go`, removed when done); commit nothing; no sub-agents; never read any `.jsonl`. Construct counterexamples with commands and quoted output; an assessment without one is Minor at most.

Journeys: device-derived phrase -> HOLD -> Done -> review -> cut (both forms; QR on/off) -> a year later recover from the plate alone (string via ms; phrase via QR scan and the decided text); host-derived: `ms hashlock` -> `me sysw pack --pack-preimage` (with and without the no-seal flag; the warning) -> tap -> Hashlock plates flow -> cut; a payload phrase record -> lazy derive -> where the phrase shows (masked) -> cut; mistakes: wrong method for a payload phrase, a preimage matching no path, two hashlocks in one wallet, a declined plate, an abort between the preimage plate and the md1 plates (the new order), power loss mid-QR, the 1-in-256 collision string under id entr, an ms1 string typed into free text or the passphrase program (the warning), a payload with the phrase record loaded at the Password program (must be refused).

Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H6-spec-R0-r0-journey.md` (create; must not exist): per journey a step table, findings `### C-n / I-n / M-n / N-n -- title` with a SUGGESTION, closing counts. Return a two-line summary plus the path.
