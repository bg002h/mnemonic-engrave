You are the INDEPENDENT journey-walk reviewer (opus tier) for round 0 of the R0 gate on `design/IMPLEMENTATION_PLAN_hashlock_H5_device_polish.md` (engrave master `<PLAN_SHA>`), spec `design/SPEC_hashlock_H5_device_polish.md` (`<SPEC_SHA>`).

ONE QUESTION: walk the operator through the device the PLAN builds (drive the gated tree on the touch harness where you can) at the moments H5 changes -- HOLD to the reconcile screen and back; Done's banner on the mixed, the all-phrase, the removed-and-readded and the re-typed-as-hex wallets; the phrase screen's lead and readout at sh2 size; the unlock refusal -- asking at each step what they have in hand, what the device does, and what ELSE they might do. A divergence is a finding only if the wrong outcome is worse than telling the operator nothing. Also walk the CONTROLLER's three emulator runs as the plan describes them: are the mutation recipes exact, is the failing assertion named, could a run pass for the wrong reason.

Read-only on every repo (engrave `/scratch/code/shibboleth/mnemonic-engrave`; fork `/scratch/code/shibboleth/seedhammer` main `b9a9a30`); the plan author left the GATED tree at `/scratch/code/shibboleth/.tmp/h5-gate` (fork b9a9a30 + every task applied; read it, never modify it); for execution use your OWN copy `rm -rf /scratch/code/shibboleth/.tmp/h5-<key> && mkdir -p /scratch/code/shibboleth/.tmp/h5-<key> && cp -a /scratch/code/shibboleth/.tmp/h5-gate/. /scratch/code/shibboleth/.tmp/h5-<key>/` (Go `/scratch/code/shibboleth/.toolchain/go/bin/go` first on PATH; remove it when done); commit nothing; no sub-agents; never read any `.jsonl`; secret-handling never gates. Already settled: the plan author ran the build gate at every task boundary and the blocks-vs-tree checker passes (its report `design/agent-reports/hashlock-H5-plan-author-report.md`); do not report compile findings. Construct counterexamples with commands and quoted output; an assessment without one is Minor at most.

Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H5-plan-R0-r0-journey.md` (create; must not exist): per moment a step table (step / in hand / device does / what else / classification); findings `### C-n / I-n / M-n / N-n -- title` with SUGGESTION; closing counts. Return a two-line summary plus the path.
