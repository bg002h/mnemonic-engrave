You are the SAME single implementer, resumed a third time, to fold the whole-diff review of your Stage 4 work: `design/agent-reports/composer-S4-exec-review-r0.md` (opus, 0C/1I/4M/4N; persisted at mnemonic-engrave `93988cd`). Read it whole first. The controller has decided every finding; execute the decisions, do not re-litigate them. Same worktrees (`/scratch/code/shibboleth/wt-composer-s4-emu` at `86cec95`, `/scratch/code/shibboleth/wt-engrave-s4-emu` at `c6adac2`), same environment and tool paths, `TMPDIR=/scratch/code/shibboleth/.tmp`; commit per repo, signed-off, trailer lines kept; never push, never flash; no sub-agents; read no `.jsonl` file.

## Decisions
- **I-1 (FOLD, the report's hypothesis):** `drive()` returns the failure text, never a bare `None`; the `--prove-it-can-fail` branch prints PASSED only when that text contains `the device's proof does not match the host's` AND the corrupted address; any other failure exits non-zero with `NEGATIVE CONTROL INCONCLUSIVE: the walk failed before the comparison -- <text>`. Prove it both ways and paste both: (a) the report's reproduction -- corrupt `out/composer/payload.digest.txt` (`dbe9` -> `dbe0`) in a copy of `out/`, run `--prove-it-can-fail`: exit non-zero, INCONCLUSIVE, in seconds; restore; (b) the honest run: exit 0, PASSED, after the whole itinerary.
- **M-1 (FOLD):** the post-condition for the passphrase `Skip` row becomes `(any slots)`.
- **M-2 (FOLD):** keyed arm: `mustNot(v, "QR ONLY")` (add `mustNot` if it does not exist) so "TEXT ONLY alone" is asserted; keyless arm: select the `TEXT + QR` row through `shTargets()` as `chooseRow` does and assert the taken row's label, instead of confirming the default selection.
- **M-3 (FOLD):** `read_keyed()` exits non-zero unless exactly 4 addresses were read.
- **M-4 (FOLD):** default ports `8803 / 8744`; the usage line names them.
- **N-1 (FOLD):** hoist the `shTargets` guard above the call in `chooseRow` (or delete the inner copy; say which).
- **N-2 (NOT YOURS):** it corrects the controller's brief, not the diff. No action.
- **N-3 (FOLD, last):** after every other commit, regenerate `transcript_composer.txt` at the final fork tip (`FORK=<fork worktree>`), commit it; the ids, addresses and strings must be byte-identical to before (say so, with the diff limited to the rev line, paths and mtimes).
- **N-4 (FOLD):** add `{"Build a new policy", "gui/composer_door.go"}` and `{"Which script?", "gui/composer_shape.go"}` to `cmd/emu/needle_test.go`'s single-site pins on `buildFlowNeedles`' pattern, so the composer walk's terminal anchor is machine-checked; the test must pass on the tip and FAIL in a copy where a second `"Build a new policy"` literal is added to another `gui/*.go` file (paste that failure).

## Gates before the report (paste real output; record every exit code DIRECTLY, never through a pipe)
Fork: `gofmt -l cmd/` (clean), `CGO_ENABLED=0 go test -count=1 ./cmd/emu/`, `GOOS=js GOARCH=wasm go vet ./cmd/emu/`. Engrave: `capture_composer.py --arm both` exit 0; `--prove-it-can-fail` exit 0 (honest) and the INCONCLUSIVE arm non-zero; the plan's unchunked-string mutation exit non-zero naming 56 vs 47; the three shipped drivers exit 0 against the fork worktree. Note for the record: `gofmt -l gui/` lists three `transaction*` files that are unformatted at `60bee002` already and outside this diff (review N-2); do not touch them.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/composer-S4-fold-r0-report.md` (create; must not exist): per finding what changed (file:line) and the evidence; the new tips of both branches (`git log --oneline main..HEAD` / `master..HEAD`); every gate's output; anything you decided or could not do. Return a two-line summary plus the path.
