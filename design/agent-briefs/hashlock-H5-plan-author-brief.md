You are the PLAN AUTHOR (opus tier) for hashlock stage H5 (device polish) in mnemonic-engrave (`/scratch/code/shibboleth/mnemonic-engrave`). Write `design/IMPLEMENTATION_PLAN_hashlock_H5_device_polish.md` from the spec `design/SPEC_hashlock_H5_device_polish.md` (round-0 fold `d36ede5`; a sonnet r1 verification is in flight -- before you finish, `git -C <engrave> log --oneline -3 -- design/SPEC_hashlock_H5_device_polish.md` and fold any newer spec commit into the plan). The fork under change is `/scratch/code/shibboleth/seedhammer` main `b9a9a30` (READ ONLY). Precedent: `design/IMPLEMENTATION_PLAN_hashlock_H2_device.md` (its header, Global Constraints, File Structure, task shape, MUTATION comments, `## Build gate folded here`) and its checker `scripts/h2-plan-blocks-vs-tree.sh` (path-headed code blocks compared to a gated tree).

THE JOB: a plan an implementer can follow literally, task by task, test-first, whose code blocks you have ALREADY WIRED AND RUN in a scratch tree -- built and tested at EVERY TASK BOUNDARY, not once at the end (the H2 plan's gate wired all tasks at once and could not see that Task 3 used a field Task 4 added; memory lesson). Tasks, one per spec section, in dependency order: Task 1 §2 provenance (`composerNotePhraseDigest`, `composerAnyPathByPhrase`, delete `composerHashByPhraseSync` and its call sites, §8h phrase-form sentence, tests incl. the zero-value-state HOLD test and the mixed-wallet banner test); Task 2 §1 reconcile screen + confirm write-down line (+ copy-table and fit rows; the `:909` needle kept; the stale 186 comment fixed); Task 3 §3 lead in the page band + geometry gate test; Task 4 §5 refusal sentence with "(records count from 0)" + fit + `unlock_preimage_test.go`; Task 5 §4 the `!tinygo` seam (`composerStateHook`, frame_hook.go model, tinygo twin) + `cmd/emu` js glue `shComposerPathHashes()` + the walk rewrite (pre-hold null pinned to the confirm-modal frame, post-hold full digest + token + `chars`, `ok` set only at the end) + the three controller runs described (write the two mutation recipes as exact one-line edits); Task 6 records: H2 spec folds (§4.5 write-down, §4.5 reconcile clause, §4.7 phrase form) in engrave, the toolkit manual re-quote (`docs/manual/src/40-cli-reference/43-ms.md`, `make lint`), FOLLOWUPS closures (F-487, F-480, F-484, F-485, F-488) with owning-phase reconciliation, firmware size line with the hook's share asserted 0.

Gate you run yourself: `rm -rf /scratch/code/shibboleth/.tmp/h5-gate && mkdir -p /scratch/code/shibboleth/.tmp/h5-gate && (cd /scratch/code/shibboleth/seedhammer && git ls-files -z | tar --null -T - -cf - | tar -xf - -C /scratch/code/shibboleth/.tmp/h5-gate)` (fork at b9a9a30; Go `/scratch/code/shibboleth/.toolchain/go/bin/go` first on PATH). For each task in order: apply that task's blocks to the tree, run its RED (quote), implement, run GREEN, run every `MUTATION:` you wrote (quote the failure, revert), then `go test ./gui/ -run <that task's tests>` and, at the end of Tasks 1, 2 and 5, the whole `gui` package via `/scratch/code/shibboleth/mnemonic-engrave/scripts/gui-shard-test.sh ./gui/ 24`. Firmware size at the end (`export PATH=/nix/var/nix/profiles/default/bin:$PATH; cd <tree>; nix develop -c tinygo build -size short -o /dev/null -target pico-plus2 -stack-size 16kb -gc precise -opt 2 -scheduler tasks ./cmd/controller`; baseline b9a9a30 measured the same way) -- the hook's share must be 0 B (build once with the hook file and once with it deleted from the tinygo view; both numbers in the plan). Write a checker `scripts/h5-plan-blocks-vs-tree.sh` modelled on the H2 one (or generalise the H2 script to take plan + tree arguments, if it does not already -- read it) and make it PASS on every block; paste its tail into the plan's `## Build gate` paragraph together with each task boundary's test tail. The emulator walk itself is out of your scope to RUN (the controller runs it on a fresh port with playwright); do build `cmd/emu` (`./build.sh`) at the end to prove the js glue compiles.

Rules: write code from the fork's actual sources (open every file you cite; copy signatures, never guess); every `file:line` in the plan re-grepped at `b9a9a30`; no placeholders ("TBD", "similar to", "handle edge cases"); Interfaces blocks per task; explicit `git add` lists per commit step naming every file the task touches; MUTATION comments on every test; record the plan's baseline (`b9a9a30`) in the header. Edit only the new plan file, the new/generalised checker script, and your scratch tree; commit nothing; no sub-agents; never read any `.jsonl`; no phrase or preimage bytes in any log you keep. When done, leave the scratch tree in place.

Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H5-plan-author-report.md` (create; must not exist): per task the RED/GREEN/mutation tails, the boundary test tails, the size numbers, the checker output, anything in the spec you could not implement as written (say why, with the measurement). Return a two-line summary plus the report path.
