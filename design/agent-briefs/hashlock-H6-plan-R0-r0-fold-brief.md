You are the FOLD author (opus tier) for round 0 of the R0 gate on `design/IMPLEMENTATION_PLAN_hashlock_H6_preimage_plates.md` (engrave master `e6d84d9c`, working tree) and its spec `design/SPEC_hashlock_H6_preimage_plates.md` (`5bb46948`) in mnemonic-engrave (`/scratch/code/shibboleth/mnemonic-engrave`). Four reports are persisted in `design/agent-reports/`: `hashlock-H6-spec-plan-round-verification.md` (the spec fold's verdict), `hashlock-H6-plan-R0-r0-fidelity.md`, `-tests.md`, `-journey.md`. Read all four in full first. The operator's decisions and rulings (continuity entries "H6 BRAINSTORM", "Group A rulings", "C-1 RULING", "C-1 scope corrected") are FIXED.

THE MECHANISM (as in H5): the plan's code blocks are the SOURCE and the three gated scratch trees are the TREES (`.tmp/h6-ms` @ ms `504ff46`, `.tmp/h6-me` @ engrave `75f00b56`, `.tmp/h6-gate` @ fork `fb0dd04`, each with every task applied). Every code change you make in the plan you make identically in the matching tree; every test you change or add you run RED (mutated/absent) then GREEN and quote both; every `MUTATION:` you touch you re-run; at the end run each tree's suite (`cargo nextest run --locked` in the ms and engrave trees with your own `CARGO_TARGET_DIR` under `/scratch/code/shibboleth/.tmp`, `TMPDIR=/scratch/code/shibboleth/.tmp`; the fork's five packages + `cmd/emu` + the whole gui via `/scratch/code/shibboleth/mnemonic-engrave/scripts/gui-shard-test.sh ./gui/ 24` + `GOOS=js GOARCH=wasm go vet ./cmd/emu/` + `./cmd/emu/build.sh`; firmware size with nix on PATH if any non-test Go file compiled into the firmware changed), then `./scripts/h6-plan-blocks-vs-tree.sh` from the engrave checkout -- every block PASS or you are not done. Go `/scratch/code/shibboleth/.toolchain/go/bin/go`.

FOLD: every Critical and Important from the three lenses and any NOT-GREEN item from the spec verification (or decline with a true reason, reviewers prescribe, you verify); the Minors that are one-line truth fixes; where a plan change alters what the spec says, fold the SPEC too (its `## Plan-round fold` paragraph grows; its STATUS stays as the verification leaves it). Every number you write is YOUR measurement of the text as written (fit via `assertModalBodyFits`, QR via `qr.Encode`, line counts via `backup.CharsPerLine`, budgets by the fork's fuzz method at a stated sample size). No non-ASCII character inside any device copy string.

RECORDS: add `## R0 round 0 folded here` at the end of the plan (finding -> change, declines with reasons, the test/checker tails) and set the plan's STATUS to `DRAFT -- R0 round 0 folded; r1 fold verification pending`. Edit only the plan, the spec, the three trees and (if a header convention needs it) the checker; never the reports or the repo checkouts; COMMIT NOTHING; no sub-agents; never read any `.jsonl`; no phrase or preimage bytes in logs; re-grep every NEW citation you add; grep the superseded phrasing after every edit.

Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H6-plan-R0-r0-fold-report.md` (create; must not exist): per finding the change and the RED/GREEN evidence; mutation re-runs; suite and checker tails; declines with reasons. Return a two-line summary plus the path.
