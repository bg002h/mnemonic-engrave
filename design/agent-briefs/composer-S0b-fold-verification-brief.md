You are the INDEPENDENT fold-verification reviewer (targeted) for the composer S0b implementation on descriptor-mnemonic branch `composer-s0b` (worktree `/scratch/code/shibboleth/wt-composer-s0b`). One question: did the controller's fold commit `<S0B_FOLD_SHA>` (`git show <S0B_FOLD_SHA>`; diff against `87bc10ff`) fix the whole-diff review's M-1, M-2, M-3 and N-3 exactly as filed in `design/agent-reports/composer-S0b-exec-review-r0.md` (mnemonic-engrave master), can each new test fail, and did nothing else move?

Read-only; every mutation reverted; `git status --porcelain` empty at the end. `export CARGO_TARGET_DIR=/scratch/code/shibboleth/.s0b-verify-target TMPDIR=/scratch/code/shibboleth/.tmp`; never build under /tmp. Do NOT spawn sub-agents; read no `.jsonl` file.

## Verify
1. M-2: in `crates/md-cli/src/cmd/compose.rs` every preset arm calls `named_only(..)?` BEFORE `need_ofs(..)?` (six arms). Mutation: swap the two lines back in the `plain-multisig` arm -> `preset_names_an_unknown_parameter_before_counting_kofn` FAILS (quote the assertion output); revert. Then confirm the reviewer's four reproductions from M-2 now name the token (`plain-multisig,bogus=1`, `plain-multisig,=5`, `plain-multisig,2of3=x`, and `plain-multisig,2of3,bogus=1` unchanged).
2. M-3: `need_u32` refuses `older=<n>u` (any `older*` key) and `after=<t>t` with a message naming the suffix's meaning and the `--path` remedy; a non-numeric remainder (`older=xu`) keeps the generic message. Mutation: delete the `if let Some((rest, meaning)) = suffixed { ... }` block -> `preset_names_the_path_remedy_for_u_and_t_suffixes` FAILS; revert. Check `need_after_height`'s band message is unchanged (`after=500000000` reproduction from M-3).
3. M-1: `crates/md-codec/tests/compose_support.rs`'s `SINGULAR_TAGS` doc states the two grounds (one legal shape for `spine:0`; one vector by deliverable scope for `head:hashed` and the six `preset:*`), drops "by construction", and the constant's members are unchanged; `crates/md-codec/tests/compose_vectors.rs` still pins `== 1` for each.
4. N-3: `crates/md-cli/README.md` has an `md compose` row whose flags match `md compose --help` (`--wrapper`, `--path`, `--preset`, `--json`; check the wrapper spellings against `parse_wrapper` in compose.rs), and root `README.md`'s subcommand list names `compose`.
5. Nothing else moved: every hunk of the fold belongs to one of the four items or the CHANGELOG line; `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`, `RUSTDOCFLAGS='-D warnings' cargo doc --workspace --all-features --no-deps --locked`, `cargo nextest run --workspace --all-features --locked` (expect 1340 + the two new tests = 1342 passed, 3 skipped -- report the number you get), and `cargo nextest run --locked -p md-cli -E 'binary(/^cli_compose/)'` (was 31) all green.

Severity: a finding folded wrongly, a test that cannot fail, or a hunk outside the four items = Important; cosmetic = Minor/Nit. Do not pad.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/composer-S0b-exec-review-r1-fold-verification.md` (create; must not exist): per item VERIFIED / NOT VERIFIED with the command output; closing counts. Return a two-line summary plus the path.
