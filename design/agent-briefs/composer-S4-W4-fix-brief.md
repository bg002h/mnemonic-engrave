You are the SAME single implementer, resumed, to fix W-4 in `/scratch/code/shibboleth/mnemonic-engrave/design/S4_journey_walk_2026-09-02.md`: the composer's digit pad (`gui/composer_digitpad.go` `composerDigitEntry`) draws its prompt (`lead`, e.g. "How many blocks?") and its live echo/range line (e.g. "1 to 65535 blocks") over each other between the entry box and the keyboard. The operator saw it on the device on the blocks pad (via the decaying-multisig preset -> Path 1 -> Time lock -> After a wait -> Blocks) and on the date-or-height pads. Cause (read the code): each info line is clamped INDIVIDUALLY to `top.Max.Y - sz.Y`, so a second line that does not fit is pushed up onto the first. The controller has decided the fix shape; execute it.

## Where -- a FOURTH branch
```
cd /scratch/code/shibboleth/seedhammer && git worktree add /scratch/code/shibboleth/wt-composer-s4d -b composer-s4d main
```
(`main` is `6fb90cb`.) Same environment as before; commit signed-off with the trailer lines; never push, never flash; no sub-agents; read no `.jsonl` file; direct exit codes.

## The fix (decided)
Lay out the entry box, the prompt and the echo line as ONE vertical group (box, gap, prompt, gap, echo) whose total height is measured first, then centre the GROUP inside the band above the keyboard (`top`); never clamp a line on its own. If the group would not fit the band, shrink the gaps to zero before anything else, and if it still does not fit, that is a finding to report (measure: the digit keyboard's height, the band's height, the group's height at the body style, at the SH2 display size) -- do not drop or overlap a line. Keep the pad's behaviour otherwise unchanged (the cap, the validator, Back, the drained Button3, the shipped `DIGIT_KEY_PITCH` geometry the S4 driver types by -- `gui/composer_digitpad_geometry_test.go` must still pass unchanged).

## The test (must fail first)
A rasterising GEOMETRY test in `gui/` on the pattern of `gui/composer_paged_geometry_test.go`: render `composerDigitEntry` for all four pads the composer uses (`How many blocks?`, `How many days?`, `Date as YYYYMMDD`, `Block height`, with their real validators from `gui/composer_lock.go`), each with an EMPTY fragment and with a full one (`12960`, `90`, `20260901`, `905000`), and assert from the frame's ops that (a) no two text rectangles intersect, (b) every text rectangle lies inside the band between the title and the keyboard, (c) both the prompt and the echo are present. Run it on `6fb90cb` FIRST and paste the failure naming the overlapping pair; then make it pass; then paste the failure under a mutation that restores the per-line clamp.

## Gates (paste real output)
`gofmt -l gui/`; `go vet ./gui/` (two pre-existing ArtifactDir findings excepted); `go test -count=1 -run '^TestComposer' ./gui/`; the sharded gui runner (`/scratch/code/shibboleth/mnemonic-engrave/scripts/gui-shard-test.sh ./gui/ 24`); `CGO_ENABLED=0 go test -count=1 ./cmd/emu/`; `GOOS=js GOARCH=wasm go vet ./cmd/emu/`; the firmware size recipe vs `1,580,580 B flash / 62,800 B RAM`. Then the emulator proof: build `emu.wasm` from THIS worktree and drive, with Playwright and `shTargets()`, the operator's exact route (no payload: SKIP; Wallet Policy -> Build a new policy -> Taproot -> `decaying-multisig` -> Path 1 -> Time lock -> After a wait -> Blocks) and screenshot the pad EMPTY and after typing 12960; also the date pad (After a date or height -> A date) empty. LOOK at the PNGs: the prompt and the range/echo line must be two separate legible lines under the box. Paste the paths and `shScreen()`. Then run `design/journeys/capture_composer.py --arm both` against this worktree (`EMU` override) -- it types through this pad -- and paste the exit code.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/composer-S4-W4-fix-report.md` (create; must not exist): worktree, branch, `git log --oneline main..HEAD`, the failing-then-passing test output, the mutation output, every gate, the measured band/keyboard/group heights, the emulator frames, anything you decided or could not do. Return a two-line summary plus the path.
