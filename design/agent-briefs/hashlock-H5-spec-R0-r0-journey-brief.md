You are the INDEPENDENT journey-walk reviewer (opus tier) for round 0 of the R0 gate on `design/SPEC_hashlock_H5_device_polish.md` (engrave master `f6dd437`), over fork main `b9a9a30`, parent spec `design/SPEC_hashlock_H2_device.md`.

ONE QUESTION: walk the operator through the three moments this spec changes and find where the spec is SILENT -- a step whose wrong outcome is worse than telling the operator nothing. At every step: what do they have in hand, exactly; what does the device (as specified) do; what ELSE might they do, and what happens then. Classify each divergence (refusal / warning / default / not our concern / documentation only); a divergence is a finding only if the wrong outcome beats silence.

Read-only on every repo (engrave `/scratch/code/shibboleth/mnemonic-engrave`; fork `/scratch/code/shibboleth/seedhammer` main `b9a9a30` -- for execution use your OWN detached worktree `git -C /scratch/code/shibboleth/seedhammer worktree add --detach /scratch/code/shibboleth/.tmp/h5-lens-<key> b9a9a30`, Go `/scratch/code/shibboleth/.toolchain/go/bin/go` first on PATH, remove it when done); commit nothing; no sub-agents; never read any `.jsonl`; secret-handling never gates. Construct counterexamples with commands and quoted output; an assessment without one is Minor at most. Severity: Critical = a normative sentence that, implemented literally, loses operator work, assigns a hash before HOLD, shows a digest that differs from the stored one, or makes a gate unable to fail; Important = a false claim about the fork, a fit or geometry number that does not hold, a missing case, an unsound design step; Minor/Nit = wording.

Moments: (1) HOLD on the confirm modal -> the new reconcile screen (§1): they write the digest; they go to the host and run `ms hashlock` (what does it print -- read `crates/ms-cli/src/hashlock_phrase.rs` and the manual chapter `mnemonic-toolkit/docs/manual/src/40-cli-reference/43-ms.md`); the digests disagree -- what can they do on the device now (dismiss, re-enter the hash editor: does the phrase route let them retype? where is the old hash?); they dismiss without writing anything -- is the digest recoverable later? (2) Done's §8h banner under §2's per-digest provenance: the three-hashlock wallet with one phrase-set path removed and re-added from a payload row; a phrase digest re-typed as 64 hex on another path; a policy where every hash came from payload rows -- is the banner right each time and does the operator understand "phrase and its method, or the preimage plate"? (3) the unlock refusal (§5): the operator holds a payload with a preimage plate at record 1 -- the screen tells them to remove it on the host and seal again: is `me seal` the verb (check the released `me` 0.8.1 at `/scratch/code/shibboleth/mnemonic-engrave/target/release/me --help` or `me seal --help`), and does the host refuse to seal a plate anyway (H1b), so the sentence describes something the host will not let them do? Also walk §4's walk changes as the CONTROLLER (who runs it): is the hook readable while the modal is up, and does the mutated-build run described in §4.5 actually fail where the spec says.

Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H5-spec-R0-r0-journey.md` (create; must not exist): per moment a step table (step / in hand / device does / what else / classification), findings `### C-n / I-n / M-n / N-n -- title` with a SUGGESTION, closing counts. Return a two-line summary plus the path.
