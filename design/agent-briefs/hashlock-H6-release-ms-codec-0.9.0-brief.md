You are the RELEASE agent (sonnet tier) for mnemonic-secret: publish ms-codec 0.9.0 after implementer A's branch `h6-a` (Task 1 of the H6 plan) has been merged to ms master by the controller (tip given). Follow `design/RELEASE_PROCESS.md` and the H1 precedent recorded in `design/agent-reports/` (the 0.18.0 / ms-codec 0.8.0 release: `decision-crates-io-publish-ms-codec-0.8.0.md` and the push reports) exactly: `ci/repro/vendor-freshness.sh` green (re-vendor if the lock moved); push master via the ci/staging ritual with the four required contexts (`test (ubuntu-latest)`, `clippy`, `test (ms-codec)`, `clippy (ms-codec)`) satisfied, never bypassed; tag `ms-codec-v0.9.0` (or the tag form the process names) at the release commit; `cargo publish -p ms-codec --locked` from a detached worktree at the tag (dry-run first); verify `max_version` on crates.io. Write `/scratch/code/shibboleth/mnemonic-secret/design/agent-reports/release-ms-codec-0.9.0.md` with every command and output, commit only that file with the trailers; return two lines: outcome + path. Never read any `.jsonl`.
ADDENDUM (controller, after implementer A): the plan's Task 1 Step 6 has two records items A's brief did not carry and that are YOURS before the push: (item 3) a `MIGRATION.md` section for the five public items ms-codec 0.9.0 adds (`validate_phrase`, `PhraseRefusal`, `looks_like_ms1`, `qr_text`, `HASHLOCK_PHRASE_MAX_CHARS`), per the v0.1 -> v0.2 precedent; and (item 9) a `design/FOLLOWUPS.md` entry at tier `cross-repo` naming the two consumers (`me` Task 2's bump; the fork's Task 5b re-vendor + provenance pin) -- the sibling companion entries are Task 2's and Task 13's, not yours. Commit those two files as ONE records commit on master (trailers as above) BEFORE the ci/staging push, so the released commit carries them. Then: vendor-freshness, push ritual, `ms-codec-v0.9.0` annotated tag at that commit, publish, verify `max_version`.
ADDENDUM 2 (controller, after the pre-publish review): ms master now carries `h6-a` fast-forwarded (tip `1a4f4aa8`; the pre-publish opus review of the whole diff is GREEN 0C/0I, engrave `design/agent-reports/hashlock-H6-A-pre-publish-review.md`). The CHANGELOG heading is already dated (`## ms-codec [0.9.0] — 2026-09-05`); leave it. In your records commit, besides MIGRATION.md and the cross-repo FOLLOWUPS entry, file ONE more `design/FOLLOWUPS.md` entry in ms (next free F-number in that file; secret-handling class, never blocking): the review's N-3 -- `qr_text`'s no-reallocation property (`Zeroizing<String>` reserved at exact capacity) is guarded by nothing that ships; a one-line `assert_eq!(got.capacity(), got.len())` in `the_worst_case_is_194_bytes` (`crates/ms-codec/tests/hashlock_qr_text.rs`) would close it. Do NOT add the assertion yourself -- the tree under release is reviewed as-is.
