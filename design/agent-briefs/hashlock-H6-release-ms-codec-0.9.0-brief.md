You are the RELEASE agent (sonnet tier) for mnemonic-secret: publish ms-codec 0.9.0 after implementer A's branch `h6-a` (Task 1 of the H6 plan) has been merged to ms master by the controller (tip given). Follow `design/RELEASE_PROCESS.md` and the H1 precedent recorded in `design/agent-reports/` (the 0.18.0 / ms-codec 0.8.0 release: `decision-crates-io-publish-ms-codec-0.8.0.md` and the push reports) exactly: `ci/repro/vendor-freshness.sh` green (re-vendor if the lock moved); push master via the ci/staging ritual with the four required contexts (`test (ubuntu-latest)`, `clippy`, `test (ms-codec)`, `clippy (ms-codec)`) satisfied, never bypassed; tag `ms-codec-v0.9.0` (or the tag form the process names) at the release commit; `cargo publish -p ms-codec --locked` from a detached worktree at the tag (dry-run first); verify `max_version` on crates.io. Write `/scratch/code/shibboleth/mnemonic-secret/design/agent-reports/release-ms-codec-0.9.0.md` with every command and output, commit only that file with the trailers; return two lines: outcome + path. Never read any `.jsonl`.
