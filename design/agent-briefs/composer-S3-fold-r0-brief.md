FOLD brief for the composer S3 implementer, round 0 (whole-diff review `design/agent-reports/composer-S3-exec-review-r0.md`, mnemonic-engrave master 077c5f4: 1C/2I/5M/3N). Work in `/scratch/code/shibboleth/wt-composer-s3` on `composer-s3` (tip a63fd1e, which retitled the census "Plates To Cut" -- keep it). TDD per finding: failing test first, paste its failure, fix, pass, commit per finding (signed-off, plan trailer style). Go: `/scratch/code/shibboleth/.toolchain/go/bin/go`; `CGO_ENABLED=0 GOPROXY=off GOTOOLCHAIN=local`, `TMPDIR=/scratch/code/shibboleth/.tmp`.

## Controller's decisions (do these; do not redesign)
- **C-1 (must fold).** Take the report's hypothesis (a)+(b): `composerSeatFlow` RESUMES at the first slot with `src < 0` instead of re-asking every slot, and a Back at the mapping review lands on the last seated slot (the same back-step `composerSeatFlow` already does one level down) rather than restarting `composerFlow` at the shape. Any assignment that is discarded releases its source's `used`. Regression test = the report's walk, asserting the second pass's frame still names a fingerprint (`73c5da0a`), plus a unit test that `used` is released on discard. Mutation to paste: restore the re-ask-from-0 -> the walk test fails.
- **I-1 (must fold, with C-1).** The `Move up` arm discards assignments UNCONDITIONALLY (the report's third option: one line, and exactly what §8j already promised). Keep §8j as the guard text. Test: equal-key-count swap with seated slots -> `discarded == true` and `st.assigned` cleared and every source released. Mutation: remove the unconditional discard -> the test fails.
- **I-2 (must fold).** Six more rows in `TestComposerSelfCheckRefusesAFaultInjectedBuilderOutput`, one per surviving arm (lock value, digest, use-site via `composerSelfCheckFaultHook`, unseated-slot fingerprint, fingerprint presence, §4f invariant on the decoded md1), each asserting the arm's exact refusal text. Paste each arm's `if false {` mutation now FAILING the named row.
- **M-1..M-5:** fold each if it is bounded (a test that can fail for the three lock bands; the §7g/§12-9 census ceiling refusal -- IMPLEMENT it if the plan's F-457 scope says this stage owns it, else file; the three copy bodies as §8 blockquotes with correct citations; the same-xpub body into `composerCopyTable`; the `ErrComposeIndistinguishableSlots` arm). If one is not bounded, file it in `/scratch/code/shibboleth/mnemonic-engrave/design/FOLLOWUPS.md` as F-461+ (owning phase: composer S3 residue) with the report's reproduction, in its own engrave commit.
- **N-1, N-2:** fold or file the same way. **N-3:** `gofmt -l` names five files; the three transaction* files are pre-existing on main -- if the other two are this diff's, gofmt them; if not, say so.
- Every §8 string still verbatim; no pre-existing test edited except as a named old->new replacement; `go test -timeout 20m ./...` all ok, sharded gui, `-run '^TestComposer'`, 32-bit, oraclelive, js vet -- paste the output. The firmware size step still cannot run (no /nix); say so.

## Report (your final action)
Write a NEW file `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/composer-S3-fold-r0-report.md` (create; must not exist): per finding the commit, the failing-then-passing evidence, the pasted mutation output, and for anything filed the F-number; the gate output; the new tip. Return a two-line summary plus the path. Do not read any `.jsonl` file; do not spawn sub-agents; do not push or flash.
