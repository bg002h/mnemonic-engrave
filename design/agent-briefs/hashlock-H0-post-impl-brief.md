You are the INDEPENDENT adversarial execution reviewer (opus tier) for the hashlock stage H0 implementation — the reader guards that make a kind-0x03 PREIMAGE plate inert on the SeedHammer fork and on `me`. Two branches, both named `hashlock-h0`:
- mnemonic-engrave: worktree `/scratch/code/shibboleth/me-worktrees/hashlock-h0`, tip `<ENGRAVE_TIP>`, base master `e7af98a` (Task 1: the seam corpus rows, the record-corpus capture, the host pin test, the `PreimagePlate` diagnosis).
- seedhammer fork: worktree `/scratch/code/shibboleth/.tmp/seedhammer-hashlock-h0`, tip `<FORK_TIP>`, base main `839fa5aa` (Task 2: `codex32.IsPreimage`, the two classifiers, the Scan mirror, the engrave choke point, the sealed path, the tests).
The plan is `design/IMPLEMENTATION_PLAN_hashlock_H0_reader_guards.md` (R0 GREEN at `e7af98a`; read its site table and Global Constraints), the spec `mnemonic-secret/design/SPEC_ms_hashlock.md` §1, §9, §12 item 7, and the implementer's report `design/agent-reports/hashlock-H0-implementation-report.md` on the engrave branch (its deviations are claims to verify).

ONE QUESTION: over BOTH whole diffs, can you construct a string, a payload, or a sequence of screens under which the flashed device or `me` still treats a preimage plate as a seed — or under which a legitimate seed, share, or plain BIP-93 secret is now refused — and does every test the diff adds actually fail on the defect it names?

Read-only on every repo and both branches; commit nothing; no sub-agents; never read any `.jsonl`. Build and RUN in your OWN copies: engrave `git -C /scratch/code/shibboleth/mnemonic-engrave worktree add --detach /scratch/code/shibboleth/me-worktrees/h0-review <ENGRAVE_TIP>` with `PATH=$HOME/.cargo/bin:$PATH TMPDIR=/scratch/code/shibboleth/.tmp CARGO_TARGET_DIR=/scratch/code/shibboleth/.tmp/h0-review-target`, package `-p mnemonic-engrave`; fork: `rm -rf /scratch/code/shibboleth/.tmp/h0-review-fork && mkdir -p /scratch/code/shibboleth/.tmp/h0-review-fork && (cd /scratch/code/shibboleth/.tmp/seedhammer-hashlock-h0 && git ls-files -z | tar --null -T - -cf - | tar -xf - -C /scratch/code/shibboleth/.tmp/h0-review-fork)` with `/scratch/code/shibboleth/.toolchain/go/bin/go` first on PATH. Mutate only in those copies; revert (and `touch`) after each mutation; remove the worktree and target dir when done.

## Already settled — do not re-derive
- The controller re-ran the gates at both tips (numbers in the dispatch message). Do not report compile/lint/fmt findings.
- The plan's R0 closed under fidelity, tests/mutation and two fold-verification rounds; review the CODE against the SPEC and the plan's site table, not the plan.
- The three `history_purge` failures are box-local (no `/usr/bin/zsh`) and identical on master.
- Secret-handling defects never gate (operator ruling 2026-08-27): Minor with a reproduction.

## Construct counterexamples — an assessment without one is Minor at most
1. **Every door, executed in the gui test harness.** For each of: the NFC scan (`(*scanner).Scan`), the typed `M*1 STRING` path (`validateMStar` → `engraveObjectFlow`), the sealed unlock path (`seal.AdmitSection` → `unlockEngraveCodex32`), the sysw container (`sysw.Classify` and the composer door counts) — feed the 75-character plate and confirm no screen titled `Confirm Codex32 Secret`, `EngraveSeed` or `Engrave Plate` is reached and what the operator sees instead. Then `grep -n "codex32.New(\|ClassCodex32Secret\|EngraveSeedString(\|DecodeMS1(" --include=*.go -r .` and trace any site the plan's table does not list.
2. **The negative space, executed.** `IsPreimage` on: the plate; the entr-id 33-byte 0x03 shape; a plain BIP-93 16-, 32- and 33-byte secret whose seed begins 0x03; a 2-of-N share beginning 0x03; the mnem string; an UPPERCASE plate (`codex32.New` accepts either case; `ParsePrefix` reports `Unshared` for `S` too?); a plate with a corrupted checksum. Every legitimate population must still classify `ClassCodex32Secret` in both `sysw.Classify` and `seal.Classify`, and a whole sealed payload containing such a share must still admit (build one with the seal test helpers).
3. **The host.** `me sysw pack` and `me seal` on the plate (stderr names the kind, never echoes the record, exit code), on the entr-id shape, and on the two BIP-93 0x03 rows (still refused as outside the profile, message unchanged from `e7af98a`); the seam test's three shapes; the record-corpus capture (37, all four `Unknown`); the pin test under the recorded mutation. Then simulate the 0.8 bump's hazard: make `validate_record` return `Ok(RecordKind::Ms)` for any `ms10hash` string and list EVERY test that fails — the plan claims three witnesses.
4. **Tests that cannot fail.** For each test the diff adds (engrave and fork), mutate the guard it names and quote the failure; a survivor on a spec-normative guarantee is Critical.
5. **Firmware.** The report's size numbers: re-measure with `nix develop -c tinygo build -size short -o /dev/null -target pico-plus2 -stack-size 16kb -gc precise -opt 2 -scheduler tasks ./cmd/controller` in the fork copy (`export PATH=/nix/var/nix/profiles/default/bin:$PATH`); RAM must still be 62,800 and flash within a few hundred bytes of 1,582,628.
6. **Rust-primary.** Confirm no fork change decides a behaviour Rust does not already have (the plan calls this convergence: `me` at ms-codec 0.7 refuses the kind). Confirm the vendored corpus is byte-identical to engrave's and both sha literals match.

## Severity
Critical: a path that still cuts/shows/uses a preimage plate as a seed; a legitimate seed, share or BIP-93 secret refused; a test that cannot fail on a normative guarantee; a false claim in the implementation report. Important: a real defect or missing case. Minor: secret-handling, wording, records.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H0-post-impl.md` (in the MAIN checkout at that absolute path; create; must not exist): findings `### C-n / I-n / M-n / N-n — title` with the command, verbatim output and the spec or plan line violated; the door table (door, input, screen reached / outcome); the mutation table; the deviations with a verdict each; closing counts and GREEN / NOT GREEN. Return a two-line summary plus the path.
