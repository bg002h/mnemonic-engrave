You are the JOURNEY WALKER (opus tier) for the brainstorm of hashlock stage H6 -- engraving hashlock PREIMAGE plates on the SeedHammer II. Your job is to generate the questions a real operator would force, by walking the journeys the operator's decisions imply, and to return ONLY the questions those decisions leave open. Read the fixed decisions in `/scratch/code/shibboleth/mnemonic-engrave/design/agent-briefs/hashlock-H6-brainstorm-draft-brief.md` (items 1-9 under DECIDED BY THE OPERATOR; if the file is not there yet, wait a few seconds and retry) -- they are not to be reopened. Facts to check as you walk: engrave master specs (`design/SPEC_hashlock_H2_device.md`, `SPEC_hashlock_H5_device_polish.md`, `SPEC_wallet_policy_composer.md`), fork main `fb0dd04` (`gui/composer_*.go`, `backup/`, `gui/freetext_flow.go`), mnemonic-secret's `ms hashlock` (`crates/ms-cli/src/hashlock_phrase.rs`) and the toolkit manual chapter `mnemonic-toolkit/docs/manual/src/40-cli-reference/43-ms.md`.

Journeys (at every step: what they have in hand, exactly; what the device or host does under the decisions; what ELSE they might do and what happens then): (J1) device-derived: build a policy, type a phrase, HOLD, reach Done, the review step, cut the hashlock plate among the descriptor plates, then a year later recover from that plate alone -- with the plate-string form; with the phrase form and its QR (who scans it, what do they get, how do they turn it back into a digest); (J2) host-derived: `ms hashlock` on the host, `me sysw pack --pack-preimage`, tap the payload, the Hashlock plates flow, cut; then the same preimage used in a policy later; (J3) a payload with a phrase record: the 10 s derivation on the device, the phrase visible where; (J4) mistakes: the wrong method chosen for a payload phrase; a preimage record whose digest matches no path; two hashlocks in one wallet and mixed-up plates; a declined plate at review; power loss mid-bundle; the operator types the plate string into free text and meets the warning; (J5) the template id: where would it come from, and what does the operator do with it on the plate.

For each divergence classify refusal / warning / default / not our concern / documentation only; keep only the ones whose wrong outcome is worse than telling the operator nothing, and turn each into a QUESTION with 2-3 options and a one-line recommendation. Do not answer them.

Rules: read-only; commit nothing; no sub-agents; never read any `.jsonl`. Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H6-brainstorm-journey-questions.md` (create; must not exist): per journey a step table, then the QUESTIONS, most consequential first. Return a two-line summary plus the path.
