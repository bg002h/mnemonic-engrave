You are the INDEPENDENT journey-walk reviewer (R0, round 0, the only lens) for the composer's Stage 4 plan: `/scratch/code/shibboleth/mnemonic-engrave/design/IMPLEMENTATION_PLAN_composer_S4_acceptance.md` at mnemonic-engrave `d640875`. The plan's Task 3 states two itineraries (keyed arm, keyless arm) as tables of "wait for / do / assert" rows against the SHIPPED SeedHammer fork `main` `60bee002f24dfb0092a9767b9f20d0b4c5cdf619` (checkout `/scratch/code/shibboleth/seedhammer`). You did not write the plan.

## The one question
Walk both itineraries against the shipped code, row by row, and answer for each row: does the needle appear, in that order, with that Expected -- and what ELSE might an operator do at that step, and what happens then? Pin every cell the plan marks `?`. The single highest-value check: drive the keyed arm to the consent and read whether the device's seed-seated slot @2 lands at master B's account 0' and the Policy-ID reads `4dd749a8372af515a61d7104faf944ef` with the four addresses in plan §2; if not, say what the device printed and which of (the device's seed-account rule, the plan's reading of spec §4f, the host oracle's origin) is wrong.

## How
Two ways, use at least the first: (a) the Go harness -- `gui/composer_join_test.go` `TestComposerWalkFromAKeyedPayloadReachesTheEngraveScreen` shows the whole pattern (`synctest`, `runUI`, `pumpUntil`, `click`, `uiContains`, paging); write THROWAWAY tests in a COPY of the fork (`cp -r /scratch/code/shibboleth/seedhammer /scratch/code/shibboleth/.s4-lens/seedhammer`; never dirty the checkout; do not `git checkout` in the copy), building the payload from `gui/composer_fixtures_test.go`'s helpers (`composerRecord`, `composerPayloadWith`) with the plan §2 records -- NOTE the fixture's mnemonic is master A; the plan's seed is master B ("legal winner thank year wave sausage worth useful legal winner thank yellow"), and the plan's Policy-ID assumes B; (b) the emulator in a browser -- `sh cmd/emu/build.sh` in the copy, serve `cmd/emu` with `python3 -m http.server`, drive with Python Playwright (installed; `design/journeys/capture_tr_pathological.py` shows the shape), reading `window.shScreen()`; the composer payload blob does NOT exist in the emulator yet (that is plan Task 1), so the keyed arm runs only on the Go harness today.

Environment: Go `/scratch/code/shibboleth/.toolchain/go/bin/go` (1.26.7), `CGO_ENABLED=0 GOPROXY=off GOTOOLCHAIN=local`, `-mod=readonly`, `TMPDIR=/scratch/code/shibboleth/.tmp`. Read-only on both repos: no commits, nothing left in the checkouts, `git status --porcelain` empty in both at the end. Do NOT spawn sub-agents; read no `.jsonl` file. Tools by path: `/scratch/code/shibboleth/descriptor-mnemonic/target/release/md`, `/scratch/code/shibboleth/mnemonic-engrave/target/debug/me`, `~/.cargo/bin/ms`, `~/.cargo/bin/mk`.

## Already settled -- do not re-derive
Plan §2's values (md compose/encode/inspect/address, me sysw pack/show, ms derive, all run 2026-09-03); Task 0's run (three shipped drivers exit 0 on 60bee002); the emulator JS surface names; the payload-load sequence in `cmd/emu/walk_s4_gate.js`; spec `design/SPEC_wallet_policy_composer.md` §7 and §8 are the normative screens and copy; W-1 (the preset picker's first row) is shipped in 60bee002.

## Lenses
1. Row fidelity: for every row of both tables, VERIFIED (the needle, order and Expected hold; quote the frame) or CORRECTED (the shipped string/order/outcome, quoted, and what the row should say).
2. The `?` cells: pin each with the shipped text.
3. What else: at each step, the plausible other action (Back, the other row, a wrong digit count, declining a confirm, choosing the seed for two slots, "Leave unseated", the passphrase question if one is asked) and what the device then does; classify refusal / warning / default / not-our-concern / documentation-only; a divergence earns a plan change ONLY when the wrong outcome is worse than telling the operator nothing.
4. The oracle: the Policy-ID / origin question above; and whether the keyless arm's engraved string can be read from `shToolpath.strings()` after ONE hold (the engrave loop), or whether the composer's engrave step differs from `walk_s3_nested.js`'s handler list.
5. The device tasks (4 and 5): anything an operator at the machine would hit that the plan's itinerary and abort criteria do not name.

Severity: Critical = a step the operator cannot complete, a wrong outcome worse than silence that no row catches, or an oracle mismatch (the Policy-ID); Important = a needle/order/Expected that would make the driver fail or pass for the wrong reason, or a missing refusal/warning row; Minor/Nit recorded. Fixes as HYPOTHESES. Do not pad; say which lens found nothing.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/composer-S4-plan-R0-r0-journey.md` (create; must not exist): per row the verdict with the quoted frame; the pinned `?` cells; the what-else table; findings `C-n`/`I-n`/`M-n`/`N-n` with reproduction (the throwaway test or the emulator steps) and hypothesis; closing counts. Return a two-line summary plus the path.
