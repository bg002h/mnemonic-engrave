You are the INDEPENDENT fidelity + design reviewer (opus tier) for round 0 of the R0 gate on `design/SPEC_hashlock_H6_preimage_plates.md` (engrave master `<SPEC_SHA>`): hashlock preimage plates on the SeedHammer II. Brainstorm: `design/BRAINSTORM_hashlock_H6_preimage_plates.md`; parent specs `SPEC_hashlock_H2_device.md`, `SPEC_hashlock_H5_device_polish.md`, `SPEC_wallet_policy_composer.md`; host `crates/me-cli/src/sysw/`; ms `design/SPEC_ms_hashlock.md` + corpus.

ONE QUESTION: implemented literally, does the spec produce plates that carry exactly what they claim (a preimage or a phrase, never a seed; the QR the phrase and method in the decided text; the locator the stub screen's own value), through host and device paths that refuse everything H0 refused, with every fork/host claim true and every number a measurement?

SETTLED (do not reopen): the operator's nine brainstorm decisions and the Group A rulings recorded in `design/CONTINUITY_composer_2026-09-01.md` (entries "H6 BRAINSTORM" and "H6 BRAINSTORM -- Group A rulings") and applied in the spec: forms (ms1 string, or phrase+method; QR only with the phrase form, below the text, constant time KEPT with the encoder raised); form-specific bands; mk1 stub locator; entry from the composer at HOLD and from the wallet-policy payload (preimage record or new phrase record, Rust first); hybrid architecture (composer-native + a Hashlock plates flow under the Wallet Policy door); classes secret, sealed by default with a device-cannot-unlock warning, no-seal flag for the flow; `--pack-preimage`; the H6 admission path requires the `hash` id; free text warns-and-cuts; retention accepted. Review the SPEC against those decisions, the fork at `fb0dd04`, engrave master and ms `504ff46` -- not the decisions themselves. Secret-handling never gates. Read-only on every repo (execution only in your own detached fork worktree `git -C /scratch/code/shibboleth/seedhammer worktree add --detach /scratch/code/shibboleth/.tmp/h6-lens-<key> fb0dd04`, Go `/scratch/code/shibboleth/.toolchain/go/bin/go`, removed when done); commit nothing; no sub-agents; never read any `.jsonl`. Construct counterexamples with commands and quoted output; an assessment without one is Minor at most.

Verify in particular: the raised constant-time QR encoder (module ceiling, alignment patterns, the constant-time argument -- is it sound as specified); the geometry at the smallest rung with the QR below (line counts, the 100-char phrase, the method line); the two record kinds' wire shapes against the sibling records; sealing polarity and the warnings' exact conditions; the `hash`-id check on both admission paths and its refusal copy; retention keyed by digest and the flow-exit scrub; the cut order and the abort arm; the census/completeness claim; the lazy derive; the provenance-aware copy; the free-text warning's fit; the H0 guards untouched (grep for every place kind 0x03 is tested and confirm nothing outside the H6 paths changed).

Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H6-spec-R0-r0-fidelity.md` (create; must not exist): findings `### C-n / I-n / M-n / N-n -- title` with the spec section, the counterexample or measurement, a SUGGESTION; closing counts. Return a two-line summary plus the path.
