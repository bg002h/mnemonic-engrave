You are the INDEPENDENT fidelity + design reviewer (opus tier) for round 0 of the R0 gate on `design/IMPLEMENTATION_PLAN_hashlock_H6_preimage_plates.md` (engrave master `e6d84d9c`), the plan for spec `design/SPEC_hashlock_H6_preimage_plates.md` (R0 GREEN `a2a031fa`).

ONE QUESTION: if implementers follow this plan literally, task by task in the stated order and file groups, do the three repos end up doing exactly what the spec says -- every normative sentence of §1-§12 -- with the Rust-first order respected, the constant-time argument intact (encoder raise, budget, scale-2 arm), every H0 guard untouched outside the H6 paths, and every claim about the repos true?

Already settled: the spec is R0 GREEN (`a2a031fa`); the plan author ran a build gate at every task boundary and the blocks-vs-tree checker passes (its report `design/agent-reports/hashlock-H6-plan-author-report.md`); do not report compile findings. The operator's decisions and rulings (continuity entries "H6 BRAINSTORM", "Group A rulings", "C-1 RULING", "C-1 scope corrected") are settled; review the PLAN against the SPEC and the repos at the plan's stated baselines. Secret-handling never gates. Read-only on every repo; the plan author's gated trees are read-only -- copy them (`cp -a`) for execution into your own `/scratch/code/shibboleth/.tmp/h6-<key>-*` and remove them when done; Go `/scratch/code/shibboleth/.toolchain/go/bin/go`; Cargo env `PATH=$HOME/.cargo/bin:$PATH TMPDIR=/scratch/code/shibboleth/.tmp CARGO_TARGET_DIR=/scratch/code/shibboleth/.tmp/<your own dir>`. Commit nothing; no sub-agents; never read any `.jsonl`. Construct counterexamples with commands and quoted output; an assessment without one is Minor at most.

Verify: each spec normative item against the plan'"'"'s code (quote both); task ORDER and repo order (does each task compile and pass alone on top of the previous ones; spot-check two boundaries); the disjoint-file-group claim (grep the `git add` lists for any file in two groups); the constant-time deliverables (budget protocol and values, alignment table, scale-2 arm) against `engrave/`; the plate layout'"'"'s geometry numbers; the copy strings byte-for-byte against the spec; the Go/Rust lockstep for the two records and the encoder; the walk'"'"'s assertions and its mutation recipes.

Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H6-plan-R0-r0-fidelity.md` (create; must not exist): findings `### C-n / I-n / M-n / N-n -- title` with plan section, counterexample/trace, SUGGESTION; closing counts. Return a two-line summary plus the path.
