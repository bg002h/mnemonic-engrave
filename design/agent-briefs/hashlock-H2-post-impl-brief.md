You are the INDEPENDENT adversarial execution reviewer (opus tier) for the hashlock stage H2 implementation -- the SeedHammer II derives a hashlock digest from a phrase typed on the device, byte-for-byte the host's `ms hashlock` rule and derivation. Fork branch `hashlock-h2`, worktree `/scratch/code/shibboleth/.tmp/seedhammer-hashlock-h2`, tip `17b3979`, base main `c4a64fc`; engrave records branch `hashlock-h2` at `2fc2051`. The plan is `design/IMPLEMENTATION_PLAN_hashlock_H2_device.md` (R0 GREEN at `1cb05b8`), the spec `design/SPEC_hashlock_H2_device.md` (GREEN `55ee7a4`), the implementer's report `design/agent-reports/hashlock-H2-implementation-report.md` on the engrave branch (its deviations are claims to verify). Host reference: mnemonic-secret `cd0a60f` (`crates/ms-cli/src/hashlock_phrase.rs`, `argv_guard.rs:148-164`, `format.rs`), corpus `crates/ms-codec/tests/vectors/hashlock-v0.8.json`.

ONE QUESTION: over the whole diff `git -C <fork worktree> diff c4a64fc..17b3979`, can you construct typed bytes, a method, a Back sequence or a composer state for which the device's digest differs from the host's for the same intent, the operator's work is lost where the spec says intact, a hash is assigned before HOLD, or a test reports PASS on a defect it names -- and does every claim in the implementation report hold at the tip?

Read-only on both branches; commit nothing; no sub-agents; never read any `.jsonl`. Build and RUN in your OWN detached copy: `rm -rf /scratch/code/shibboleth/.tmp/h2-review && git -C /scratch/code/shibboleth/seedhammer worktree add --detach /scratch/code/shibboleth/.tmp/h2-review 17b3979`; Go is `/scratch/code/shibboleth/.toolchain/go/bin/go` first on PATH; whole-`gui` counts through `/scratch/code/shibboleth/mnemonic-engrave/scripts/gui-shard-test.sh` only. Mutate only there; revert after each; remove the worktree when done (`git worktree remove --force`).

## Already settled -- do not re-derive
- The controller re-ran the gates at `17b3979` (numbers in the dispatch message) and RAN the emulator walk (`cmd/emu/walk_hashlock_phrase.js`) on a fresh port: its assertions and frames are recorded in the continuity. No compile/vet/fmt findings.
- The plan closed R0 under five lenses + a refute pass; review the CODE against the SPEC. Rulings L5, L7, L12, L15, L16, L22, L24 stand. Secret-handling never gates (report leaks as Minor).
- The host side (`me` 0.8.1, H1b) is out of scope except as the reference for the phrase rule and the digest.

## Construct counterexamples -- an assessment without one is Minor at most
1. **Lockstep, exhaustively.** Every corpus `derivation` row through the device's `hashlock` package (both methods) AND through the SCREEN path (type it on the harness keyboard, pick the method, read the confirm modal's digest) -- a digest that matches in the package but not on the screen is Critical. Every `refusals` row typed on the harness: refused with the spec's copy, and the phrase intact afterwards.
2. **Normalisation by any other name.** Trace the bytes from `PassphraseKeyboard` to `ValidatePhrase` to `DeriveHardened`/`sha256`: any `TrimSpace`, `ToLower`, `strings.Fields`, `Join`, `NormalisePassphrase`, `unicode` call, or byte copy that could alter or truncate; the cap check's position; the counter's count (bytes or runes?).
3. **The Back contract (§4.6)**, on the device: Back at the phrase screen, the method pick, during `Deriving` (mid-countdown), at the confirm -- assert the path exists, the phrase and prior hash state per the spec table; on a NEW path vs an EXISTING path (`composerAddPath` semantics); the W-7 seat-discard guard interplay.
4. **HOLD and assignment.** Prove `Hash` is nil until the confirm's hold completes; mutate the assignment before the hold and show which test fails; `hashByPhrase` set/reset semantics (edit to a payload row; `No hash lock`; path removed) and the §4.7 every-path copy gated on it.
5. **The relation line** against a 2-digest payload: `matches hash 1` / `matches hash 2` / no match -- index base and the digest it compares (payload digests, not `st.list`).
6. **Deriving**: `seal.NewDeriver` iteration accounting (exactly 100_000), `Wipe` on every exit path including Back mid-derivation, the progress callback's false return, the copy during the countdown, no UI starvation (`-scheduler tasks`).
7. **Tests that cannot fail.** For each test the diff adds, mutate the guard it names and quote the failure; a survivor on a spec-normative guarantee is Critical. Include the lockstep test (does it recompute expectations with the code under test? compare full digests?), `holdConfirm`'s explicit release, the modal-fit rows (does the production body builder produce exactly the tested rows?), the pre-existing `composer_gates_test.go` change.
8. **Records.** The fork CHANGELOG lines; FOLLOWUPS entries (numbers continue from F-476); the corpus sha pin equals ms `cd0a60f`'s file; firmware size in the report equals a fresh measurement (`nix develop -c tinygo build -size short ...` recipe in the plan; nix on PATH via `/nix/var/nix/profiles/default/bin`).
9. **The report.** Every count must match what you measure at the tips; a false count is Critical.
10. **F-481, grade it.** The implementer measured that the `Hashlock phrase` screen draws NO readout (masked or revealed) because `kbd.MaxHeight` leaves under one line, so `show` is tappable, flips to `hide`, and reveals nothing (`design/FOLLOWUPS.md` F-481, filed under H3). Spec §4.2 says the reveal key is "inherited as-is: secret-handling, non-gating" -- but a drawn control that does nothing, and a typed secret the operator can never inspect before deriving, are not the secret-handling class; decide whether F-481 is a claims/UX defect that gates (Important) or stays a follow-up, with the measurement and a one-line remedy either way.
11. **Task 7 (F-474).** The unlock KDF's `ErrRecordNotPermitted` arm now names the record index and class; verify the seal error fields, the copy, the modal fit and that the RED ("Payload unreadable.") was real; check the F-475 re-schedule to H3 is recorded.

## Severity
Critical: a digest divergence path; a lost path/phrase or a hash assigned before HOLD; a test that cannot fail on a normative guarantee; a false claim in the report. Important: a missing screen/state/copy, an unsound assumption, a Back edge off the spec table, a corpus pin mismatch. Minor: secret-handling, wording, records.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H2-post-impl.md` (in the MAIN checkout at that absolute path; create; must not exist): findings `### C-n / I-n / M-n / N-n -- title` with the command, verbatim output and the spec line violated; the lockstep table (row, package digest, screen digest, host digest); the Back-edge table (edge, spec says, device does); the mutation table; the deviations with a verdict each; closing counts and GREEN / NOT GREEN. Return a two-line summary plus the path.
