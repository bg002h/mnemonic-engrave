# Agent briefs

The dispatch briefs behind the reports in `design/agent-reports/`, kept in the
repo because the session scratchpad is wiped when the Claude Code process
exits (measured 2026-09-02: every brief, hand-wire script and gate log of a
day's work vanished at once). A report answers a question; the brief is the
question, and a reviewer of the report needs both.
