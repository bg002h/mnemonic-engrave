You are the FOLD author (opus tier) for round 0 of the R0 gate on `design/IMPLEMENTATION_PLAN_hashlock_H2_device.md` in mnemonic-engrave (`/scratch/code/shibboleth/mnemonic-engrave`, plan at `02abee6`, working tree). Five lens reports and a refute pass are persisted in `design/agent-reports/`: `hashlock-H2-plan-R0-r0-{fidelity,tests,journey,adversarial,coverage,refute}.md`. **The refute report's deduplicated CONFIRMED + PARTIAL list is your contract**: fold every item on it; for a REFUTED item change nothing; for a Minor/Nit fold it if it is a one-line truth fix, otherwise file it in the plan's follow-up list with an owning phase. Read the refute report first, then each finding it confirms in its source report.

ONE JOB: fold the confirmed findings into the plan so that an implementer following the plan literally produces a device that (a) never blocks its own derivation on the idle wakeup, (b) has a test for every normative guarantee that can actually fail on it, and (c) says what the spec says on every screen -- and keep the plan's code equal to a GREEN wired tree, proven by the checker and by running the tests.

The mechanism: the plan's code blocks are the SOURCE and `/scratch/code/shibboleth/.tmp/h2-gate` (fork `c4a64fc` + the plan, wired by the gate agent) is the TREE. For every code change you make in the plan, make the identical change in the tree; for every test you add, run it RED (with the guard mutated or absent, quote the failure) then GREEN; re-run every declared `MUTATION:` you touch; when done run `/scratch/code/shibboleth/.toolchain/go/bin/go test ./hashlock/... ./codex32/... ./seal/... ./sysw/...` and the whole `gui` package through `/scratch/code/shibboleth/mnemonic-engrave/scripts/gui-shard-test.sh` (from the tree; 24 shards), then `./scripts/h2-plan-blocks-vs-tree.sh` from the engrave checkout -- every block PASS, else you are not done. Go is `/scratch/code/shibboleth/.toolchain/go/bin/go` first on PATH.

Design guidance for the Criticals (verify each against the fork before folding; reviewers prescribe fixes, they do not author them):
- **Derivation wakeup (adversarial C-1):** read `gui/unlock_kdf.go:295-336` and how `unlockDerive` keeps the loop awake across `ctx.Frame` (`ctx.WakeupAt`/`KeepAwake` or whatever the fork actually uses; also how `run_flow.go:402-406`'s screensaver interacts); fold the SAME mechanism into the plan's derive flow, and add a test that FAILS without it -- the reviewers say the touch harness is structurally blind (`testPlatform.AppendEvents` ignores the deadline), so either find a harness seam that observes `ctx.Wakeup`, or add an assertion on the flow's wakeup state per frame, or make the emulator walk (Task 5 Step 1) assert the countdown ADVANCES within N seconds and say plainly in the plan that this Critical is gated by the walk, which the controller runs before the post-impl review. A gate that has never executed is a hypothesis: whatever you choose must be runnable by the implementer.
- **Decoder / kind-row lockstep (adversarial C-2, tests C-1, fidelity I-6):** the corpus's `kind[0]` row carries `preimage_hex`, `digest`, `entr32_pair_ms1`; the plan's tests compare too little. Make the decoder test compare the full 32 bytes to `preimage_hex`, the digest test compare to the corpus `digest`, and the pair row assert the entr-32 sibling decodes as a seed; quote the mutation each now catches.
- **The reconciliation line behind `composerEveryPathHashed` (fidelity I-2 / journey I-3 / adversarial I-1):** the gate's fix #4 moved the line per spec §4.5 drop-order step 2, and three lenses show the destination is unreachable for the ordinary mixed keys+hash wallet. Resolve it INSIDE the spec's drop order (§4.5 names the steps; if the spec's own step lands the line in an unreachable place, the plan must say so and put the line where the spec's intent -- "before you fund, reconcile" -- is met for every policy that has a phrase-set hash), and re-measure the modal fit (`assertModalBodyFits`; the headroom numbers) for whatever body you end up with. Do NOT edit the spec; if the spec must change, write the exact replacement sentence in the plan's `## R0 round 0 folded here` paragraph as an H3 record item.
- **`Type 64 hex` Back change with no test; relation-line no-match branch; `hashByPhrase` never reset; the `Deriving` zero-state lead unreachable; fit rows measured on the wrong renderer; `IsMS1Shaped` `TrimSpace`/`minMS1Len` boundary and `DeriveHardened` abandon-path survivors:** each gets a test that fails on the named mutation (quote it) or the dead code removed with the plan text corrected.

Rules: edit ONLY the plan, the tree, and (if the checker needs a new header convention) `scripts/h2-plan-blocks-vs-tree.sh`; never edit the spec, any report, or the fork checkout; COMMIT NOTHING; no sub-agents; never read any `.jsonl`; re-grep every NEW file:line citation you add against the fork at `c4a64fc` and the tree; no phrase or preimage bytes in any log you keep. Add `## R0 round 0 folded here` at the end of the plan (before Self-review, after the build-gate paragraph): one line per confirmed finding (report id -> what changed), the declined items with reasons, the H3 record items, and the tail of the test/checker outputs. Update STATUS to `DRAFT -- R0 round 0 folded; r1 fold verification pending`.

Report (your final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H2-plan-R0-r0-fold-report.md` (create; must not exist): per confirmed finding the plan change and the RED/GREEN evidence; the mutation re-runs; the whole-package and shard outputs (tails); the checker output; declined items with reasons; anything you could not fold and why. Return a two-line summary plus the path.
