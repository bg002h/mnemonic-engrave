You are the INDEPENDENT tests + citations reviewer (sonnet tier) for round 0 of the R0 gate on `design/SPEC_hashlock_H5_device_polish.md` (engrave master `f6dd437`), over fork main `b9a9a30`.

ONE QUESTION: is every file:line, number and mechanism the spec cites true at `b9a9a30`, and can every test and mutation in §6 be executed as stated and fail on what it names?

Read-only on every repo (engrave `/scratch/code/shibboleth/mnemonic-engrave`; fork `/scratch/code/shibboleth/seedhammer` main `b9a9a30` -- for execution use your OWN detached worktree `git -C /scratch/code/shibboleth/seedhammer worktree add --detach /scratch/code/shibboleth/.tmp/h5-lens-<key> b9a9a30`, Go `/scratch/code/shibboleth/.toolchain/go/bin/go` first on PATH, remove it when done); commit nothing; no sub-agents; never read any `.jsonl`; secret-handling never gates. Construct counterexamples with commands and quoted output; an assessment without one is Minor at most. Severity: Critical = a normative sentence that, implemented literally, loses operator work, assigns a hash before HOLD, shows a digest that differs from the stored one, or makes a gate unable to fail; Important = a false claim about the fork, a fit or geometry number that does not hold, a missing case, an unsound design step; Minor/Nit = wording.

Do, in your own worktree: (1) re-grep every citation in §8 and in the section bodies (paste the line for each; a wrong number is Important if a reader would act on it, Nit if off by one). (2) Run `assertModalBodyFits` (or the test that drives it) on the longest legal confirm body and report the exact headroom; then on the PROPOSED reconcile body of §1.1 with `method: hardened` rendered through the error-screen path -- does it fit with >= 80 headroom? Quote numbers. (3) Compute the §3 band width at sh2 size and lay out the current two-sentence lead at that width with `ctx.Styles.lead` (write a throwaway test): how many lines, how many px, and the resulting readout budget. (4) Confirm `shTargets` in `cmd/emu` exposes row labels (how) and that a js-only hook returning composer path hashes is reachable from where the emulator's flow state lives (name the type and file). (5) Confirm `gui/unlock_preimage_test.go` exists and asserts the refusal frame text; (6) for each §6 mutation, state precisely which existing or proposed test fails and how you know (run it where a test exists today). (7) The walk: confirm lines 74-76, 232, 286-329 say what the spec says.

Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H5-spec-R0-r0-tests.md` (create; must not exist): a citation table (cite / true-false / evidence), the measured numbers, a per-mutation table, findings `### I-n / M-n / N-n -- title`, closing counts. Return a two-line summary plus the path.
