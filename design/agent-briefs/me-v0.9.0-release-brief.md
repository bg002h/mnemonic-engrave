You are the PUSH + RELEASE agent (sonnet tier) for mnemonic-engrave at `/scratch/code/shibboleth/mnemonic-engrave` (GitHub `bg002h/mnemonic-engrave`). You release **me 0.9.0**, which carries H6's host half (`--pack-preimage`, the `phrase:` record, the preimage and phrase classes, ms-codec 0.9.0) and F-503. The controller has already written the release commit; your job is steps 2-5 of the sequence recorded in `design/agent-reports/decision-me-0.8.1-release-and-flash-rule.md` §"The exact sequence" and §"Abort rules (Q1)" — READ THAT FILE FIRST and follow it exactly, substituting `v0.9.0` for `v0.8.1` and the SHA below. Step 6 (records) is the controller's.

Preconditions (stop and report on any miss): `git -C <repo> rev-parse master` = `9e4ccad2`; `git -C <repo> show HEAD:crates/me-cli/Cargo.toml | grep '^version'` is `version = "0.9.0"`; `git -C <repo> status --short` empty of tracked changes; `git -C <repo> ls-remote origin refs/heads/ci/staging` empty; `git -C <repo> tag -l v0.9.0` empty.

Step 2 — **master push, FROZEN window.** `scripts/push-via-staging.sh master` in the FOREGROUND with a long timeout (several minutes); quote its tail verbatim. Any "Bypassed rule violations" line is FAILURE: do not tag; report. Then `git fetch origin` and confirm `origin/master` = `9e4ccad2`. Say in your final summary when this milestone completed — the controller is frozen off master until then.
Step 3 — **tag.** Write the tag message to a file. First line: `me v0.9.0 -- hashlock preimage and phrase plates on the host; ms-codec 0.9`. Then two or three lines drawn from the CHANGELOG's `[0.9.0]` section (do not invent items). `git tag -a v0.9.0 9e4ccad2 -F <file>`; `git cat-file -p v0.9.0 | head -2` must show object `9e4ccad2`; `git push origin v0.9.0`.
Step 4 — **watch the tag-event run.** `gh run list --repo bg002h/mnemonic-engrave --commit 9e4ccad2 --json databaseId,event,status,conclusion`; `gh run watch <tag-run-id> --repo bg002h/mnemonic-engrave --exit-status`; then per-job conclusions including `assemble + sign + release`. If that job fails after the tag is pushed: leave the tag alone (never delete or move a pushed tag) and report. A non-required `build me (macos-aarch64)` artifact-upload timeout has happened before and is informational, not a release failure — say so if you see it.
Step 5 — **verify the assets and the binary.** `gh release view v0.9.0 --repo bg002h/mnemonic-engrave --json url,assets` — expect 7 assets (4 tar.gz, windows zip, SHA256SUMS, SHA256SUMS.minisig). In a scratch dir under `/scratch/code/shibboleth/.tmp/`: download `SHA256SUMS*` and the linux-amd64 tarball; `minisign -Vm SHA256SUMS -P RWQPmgBXsuw5yi8W0SfDr8KF+IqY/Z5U2p724emSODS1UPfJBP3agbKW`; `sha256sum -c --ignore-missing SHA256SUMS`; untar; `./*/me --version` (expect `me 0.9.0`). Then THREE behaviour checks on the released binary, each quoted:
  1. `printf '%s\n' ms10hashsqw46h2at4w46h2at4w46h2at4w46h2at4w46h2at4w46h2at4w46kzv2ncy60u7z9c | ./*/me sysw pack --out ./p.bin` → exit 4, stderr naming a hashlock PREIMAGE plate, no `p.bin` (the plate is refused without the flag).
  2. the same string with `--pack-preimage --no-passphrase --out ./q.bin` → exit 0, and the run WARNS that the payload carries a preimage.
  3. `printf '%s\n' ms10hashsqvqqqqqqqqqqqqqqqqqqqqqqqqqqmv3lqlgkn6s5c | ./*/me sysw pack --pack-preimage --no-passphrase --out ./r.bin` → exit 4, and the first stderr line says the X is 16 bytes, not 32 (F-503), NOT that the id is not `hash`.
Any miss = report NOT RELEASED even though the tag exists.

Rules: full SHAs and `--repo` on every `gh` call; `git -C` / absolute paths everywhere; no commits on master; do not edit FOLLOWUPS or continuity; never read any `.jsonl`.

Report (your final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/push-me-v0.9.0-release.md` (create; must not exist; do NOT commit it — the controller commits it) with every command, the staging script's tail, run ids, per-job conclusions, the release url, the asset list and the three verification outputs. Return three lines: master-push outcome, release outcome (RELEASED / NOT RELEASED + why), the path.

## Dispatch addendum (controller, 2026-09-06)
The release commit `9e4ccad2` is NOT the master tip: this brief's own commit sits on top of it. So the preconditions read: `git -C <repo> rev-parse master` = **`<TIP_SHA>`** (given in the dispatch), with `9e4ccad2` its parent and `git -C <repo> show 9e4ccad2:crates/me-cli/Cargo.toml | grep '^version'` = `version = "0.9.0"`. Step 2 pushes the TIP through the staging ritual as usual (the required context gates the tip SHA). **Step 3 tags `9e4ccad2`, not the tip** — `git tag -a v0.9.0 9e4ccad2 -F <file>`, and `git cat-file -p v0.9.0 | head -2` must show object `9e4ccad2…`. Step 4 then looks for the tag-event run on `9e4ccad2`'s full SHA. Everything else is unchanged.

Measured by the controller at `9e4ccad2` before dispatch, do not re-derive: `cargo nextest run --locked --no-fail-fast` → **643 tests run: 643 passed, 2 skipped**, exit 0 (fully green; the `history_purge` trio passes now that `/usr/bin/zsh` exists, F-500 closed); `cargo fmt --all -- --check` exit 0; the built binary reports `me 0.9.0`.
