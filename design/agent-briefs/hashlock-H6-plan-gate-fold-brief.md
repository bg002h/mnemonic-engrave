You are the FOLD author (opus tier) for the build-gate round of `design/IMPLEMENTATION_PLAN_hashlock_H6_preimage_plates.md` (engrave master `<PLAN_SHA>`, working tree -- the gate agent for Tasks 8b-12 has already edited it in place: headers, fixed blocks, measured numbers) and its spec `design/SPEC_hashlock_H6_preimage_plates.md` (R0 GREEN `a67a3924`) in mnemonic-engrave (`/scratch/code/shibboleth/mnemonic-engrave`). Inputs, read in full first: the plan author's report `design/agent-reports/hashlock-H6-plan-author-report.md` (§4: nine findings against the spec), the gate agent's report `design/agent-reports/hashlock-H6-plan-gate-8b-12.md` (its fix table, measurements and shard-18 diagnosis), and the three gated scratch trees named in the plan's `## Build gate` paragraph (`.tmp/h6-ms`, `.tmp/h6-me`, `.tmp/h6-gate`).

ONE JOB: make the spec and the plan agree with each other and with the gated trees, so that (a) every one of the nine author findings and every gate fix is folded into the SPEC as a normative change (with its measurement) or declined with a stated reason, (b) the plan's prose and blocks match the folded spec, (c) `./scripts/h6-plan-blocks-vs-tree.sh` passes on every block, and (d) every changed test still fails under its named mutation (run them in your own copies of the trees). The spec was R0 GREEN; a fold re-opens it -- add `## Plan-round fold` to the spec listing each change with its measurement and set its STATUS to `R0 GREEN; plan-round fold pending verification`; add `## Build gate folded here` to the plan and set its STATUS to `DRAFT -- fully gated; R0 round 0 pending`.

Decisions already taken by the controller on the nine findings (apply): (1) the phrase rule moves into ms-codec as the plan's Task 1 says, and a PUBLISHED ms-codec minor release becomes the stage's first deliverable -- write it into the spec's §3 and §12 with the release ritual reference (`mnemonic-secret/design/RELEASE_PROCESS.md`, the H1 precedent) and the `me` bump that follows; (2)(3) replace the two unfalsifiable mutations with the plan's (the 79-character threshold; the budget pin); (4) the two extra conjuncts of `preimage_plate_admissible` become spec text; (5) bearer classes: the argv secret guard now refuses a preimage on argv -- the spec's acceptance uses `--in`, and the guard copy names a hashlock preimage, not a transaction; (6) the two shipped tests the raise falsifies are listed in the spec's §14 with their new expectations; (7) the join-guard trip: adopt the plan's mechanism and state it; (8) fix the body-row order to ONE order, the one the gated layout draws, in every section; (9) the seam corpus row `preimage-plate-0x03` changes class Unknown -> Preimage: re-pin the corpus sha in both repos as part of the leg and say so.

Rules: every number you write is your own measurement of the text as written (fit via `assertModalBodyFits`, QR via `qr.Encode`, line counts via `backup.CharsPerLine`); Go `/scratch/code/shibboleth/.toolchain/go/bin/go`; Cargo env under `/scratch/code/shibboleth/.tmp`; edit only the spec, the plan and (if a header convention needs it) the checker script; never the reports or the repo checkouts; COMMIT NOTHING; no sub-agents; never read any `.jsonl`; re-grep every NEW citation; grep the superseded phrasing after every edit (folds fail by incomplete propagation).

Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H6-plan-gate-fold-report.md` (create; must not exist): per finding/fix the change in spec and plan, the measurement, declines with reasons; the checker output; the mutation re-runs. Return a two-line summary plus the path.
