You are implementer F for hashlock stage H6: **Task 12 then Task 13** (the walk's H6 arm in `cmd/emu/walk_hashlock_phrase.js`; then records: the four falsified statements in engrave `design/SPEC_hashlock_H2_device.md` and `SPEC_wallet_policy_composer.md`, `design/FOLLOWUPS.md` closures with the placeholder `bdb66963`, and the toolkit manual's `--pack-preimage` and plate forms). Repos: fork branch `h6-f` from the controller-merged `hashlock-h6` (tip given; carries A-E): `git -C /scratch/code/shibboleth/seedhammer worktree add -b h6-f /scratch/code/shibboleth/.tmp/seedhammer-h6-f hashlock-h6`; engrave worktree `git -C /scratch/code/shibboleth/mnemonic-engrave worktree add -b h6-records /scratch/code/shibboleth/me-worktrees/h6-records master`; toolkit worktree `git -C /scratch/code/shibboleth/mnemonic-toolkit worktree add -b h6-manual /scratch/code/shibboleth/tk-worktrees/h6-manual master` (`make lint` in docs/manual must print `[lint] OK`). Task 12: build `cmd/emu` and run the walk ONCE on a fresh port (playwright via ToolSearch, as the H2/H5 walks did); the controller re-runs it and the mutation runs at the merged tip. Do not edit the H6 spec or plan.
RULES (every H6 implementer): the plan is `design/IMPLEMENTATION_PLAN_hashlock_H6_preimage_plates.md` at engrave master `3890ad64` (STATUS R0 GREEN) implementing `design/SPEC_hashlock_H6_preimage_plates.md` (`95418b2c`); execute ONLY your tasks, exactly as written, test-first (RED quoted, GREEN, every `MUTATION:` re-run once and reverted). The plan author and gate agent left GATED scratch trees with every task applied (`/scratch/code/shibboleth/.tmp/h6-ms`, `/scratch/code/shibboleth/.tmp/h6-me`, `/scratch/code/shibboleth/.tmp/h6-gate`) -- you may READ them to resolve a block whose context is unclear; never copy files wholesale; never modify them. Touch ONLY the files your tasks list (other groups work in parallel on disjoint files; a file outside your list is a merge conflict for the controller -- if a task needs one, STOP and report). Go is `/scratch/code/shibboleth/.toolchain/go/bin/go`, first on PATH; whole-gui counts only via `/scratch/code/shibboleth/mnemonic-engrave/scripts/gui-shard-test.sh ./gui/ 24`; Cargo env `PATH=$HOME/.cargo/bin:$PATH TMPDIR=/scratch/code/shibboleth/.tmp CARGO_TARGET_DIR=/scratch/code/shibboleth/.tmp/<your own dir>`, `cargo nextest run --locked`, `cargo fmt --all -- --check` before every commit. Commit per task with the plan's commit messages, `git commit -F <file>` (fish shell; `-s` on the fork), trailers `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01Fs3bg7TRfuSaFcCEkskwXA`; stage paths explicitly. Nothing pushed; no `main`/`master` commits; no sub-agents; never read any `.jsonl`; no phrase or preimage bytes in any log you keep. If a step is wrong, do the MINIMAL correct thing and record the deviation. Every count in your report comes from a run at your tip, captured to a file under `/scratch/code/shibboleth/.tmp/` and quoted from there. FINAL ACTION: write your report (path below; create; must not exist) with per-task commit SHAs, RED evidence, PASS counts, every mutation re-run with its failing line, every deviation; return a two-line summary plus the path and your branch tip.
Report: /scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H6-implementation-F-report.md
ADDENDUM (controller, at dispatch): fork integration branch `hashlock-h6` tip is `bdb66963` (A's corpus via C; C, D, E merged; gui 1287/24 shards; checker 97/0). Engrave master tip `bd5276b5`; implementer B's me work is on branch `h6-b` (tip 8cf2a7f9, NOT yet on master -- read it read-only at `/scratch/code/shibboleth/me-worktrees/h6-b` when a record cites it). Three extra records items are YOURS in Task 13: (1) implementer D left the fork's sysw provenance pin PROVISIONAL (the file in the fork carrying the word PROVISIONAL -- `grep -rl PROVISIONAL` in your fork worktree); re-record its `commit`/`file_commit` to B's actual engrave commits for the corpus it vendors: the me corpus file is `crates/me-cli/testdata/record_class_vectors.json` (sha256 `3575ccb0e12d12646c45dde583380199170cff815ea5e8d86d4d37d4a1c4abaf`, unchanged), whose last-touching commit on `h6-b` is `4d00fbbf2b4012a533d060161bef72e3c7b547c1` (`git -C /scratch/code/shibboleth/me-worktrees/h6-b log -1 --format=%H -- crates/me-cli/testdata/record_class_vectors.json`), and remove the PROVISIONAL note; verify the sha256 still matches before and after. (2) The plan's gofmt baseline says THREE files and measures FIVE at `fb0dd04` (`mt/mt.go`, `mt/mt_test.go` are also pre-existing) -- record the measured five wherever Task 13 cites the baseline. (3) F-493 and F-494 already exist in engrave `design/FOLLOWUPS.md` (filed by the controller); do not renumber them; your new entries take the next free numbers.
