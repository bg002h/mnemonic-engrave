You are the SAME single implementer, resumed, for one predictable consequence you named yourself (W-3 fix report, item 4): once W-3 is on fork `main`, the composer's stub screens page one more time, and `cmd/emu/shots_composer.js` asserts `stub1.pages.length !== 2` (keyed arm) and `stub.pages.length !== 2` (keyless arm). W-3 is now merged into fork `main` as `1ae0ffcb3cd61ddc176eb2f1b9b365558185d982`.

## Where
`/scratch/code/shibboleth/wt-composer-s4-emu` (branch `composer-s4-emu`, tip `a6eb44e`): first `git merge --no-ff main` (message naming W-3; no conflict expected -- W-3 touched `gui/composer_paged.go`, `gui/composer_measure_test.go` and a new geometry test; this branch touched `cmd/emu/` only -- if there is one, stop and report). Engrave worktree `/scratch/code/shibboleth/wt-engrave-s4-emu` (tip `651fa0e`) is unchanged unless a capture expectation there moves. Same environment; commit signed-off with the trailer lines; never push, never flash; no sub-agents; read no `.jsonl` file; direct exit codes.

## The fold
Run `capture_composer.py --arm both` against the merged worktree FIRST and paste the failure (the page-count assertion, with the measured count). Then set the two assertions to the MEASURED page counts (do not loosen them to `>= 2`: the count is a pin, and a screen that pages differently is a finding). Anything else the merge moved -- a shot name, a page index in a caption, `stubPages` in the result JSON -- follows the same rule: pin the measured value, never relax. Re-run: `--arm both` exit 0; `--arm keyed --prove-it-can-fail` exit 0 (PASSED) and its INCONCLUSIVE arm non-zero; the unchunked-string mutation non-zero; the three shipped drivers exit 0; `gofmt -l cmd/`; `go test -count=1 ./cmd/emu/`; `GOOS=js GOARCH=wasm go vet ./cmd/emu/`. LOOK at the new stub-screen shots (`c06-stub-p*.png`, `c10-stub2-p*.png`, `k02-stub-p*.png`) and confirm the Template-ID's 32 hex digits are visible clear of the buttons on the merged build; paste the paths. If `transcript_composer.txt`'s rev line moves, regenerate it last (as for N-3).

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/composer-S4-fold-r1-report.md` (create; must not exist): the merge commit, the failing-then-passing capture output, the new tips (`git log --oneline main..HEAD`, `master..HEAD`), every gate, the shot paths, anything you decided or could not do. Return a two-line summary plus the path.
