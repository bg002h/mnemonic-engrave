You are the INDEPENDENT fold-verification reviewer (targeted) for the composer S3 implementation on fork branch `composer-s3` (worktree `/scratch/code/shibboleth/wt-composer-s3`). One question: did the controller's fold commit(s) `7edc863`, `83e932a`, `27afa9f` on the fork (diff `a63fd1e..27afa9f`), plus `db53513` (spec folds M-2/M-3) and `b1a1985` (F-461) on mnemonic-engrave master fix every Critical and Important in `design/agent-reports/composer-S3-exec-review-r0.md` (mnemonic-engrave master) exactly as filed, can each new or changed test fail, and did nothing else move?

Read-only; every mutation reverted; `git status --porcelain` empty at the end. Go: `/scratch/code/shibboleth/.toolchain/go/bin/go` on PATH (the tree's tests need go1.26), `CGO_ENABLED=0 GOPROXY=off GOTOOLCHAIN=local`, `-mod=readonly`, `TMPDIR=/scratch/code/shibboleth/.tmp`; sharded gui runner `/scratch/code/shibboleth/mnemonic-engrave/scripts/gui-shard-test.sh ./gui/ 24`. Do NOT spawn sub-agents; read no `.jsonl` file.

## Verify
0. **The second Critical the fold found** (fold report `design/agent-reports/composer-S3-fold-r0-report.md`, commit 83e932a): `composerSelfCheck` read `md.Branch`'s K/N outside their documented domain, so a multi behind a timelock compared 2-of-2 against 0-of-0 and 4 of the 12 offered (wrapper, preset) pairs were refused on an honest build. Verify: the new CONTROL test walks every offered pair and the self-check accepts each; on the pre-fold tip a63fd1e the four pairs (tiered-recovery, decaying-multisig, both wrappers) FAIL the control (reproduce by checking out a63fd1e in a copy, or by reverting the K/N read); the fix reads K/N only where `md/policy_shape.go` documents them valid. Any offered pair still refused = Critical.
1. Per Critical/Important in the report: the fold's hunk(s) for it; reproduce the report's reproduction on the pre-fold tip (it must fail/misbehave) and on the fold (it must not). Apply the fold's own named mutation to each new guard -> the named test FAILS with pasted output; revert.
2. Minors/Nits: folded, filed (name the F-number in `design/FOLLOWUPS.md`), or declined with a reason -- each accounted for.
3. Nothing else moved: every hunk belongs to a finding; gofmt clean on touched files; `go vet ./gui/` clean (two pre-existing ArtifactDir findings excepted); `go test -timeout 20m ./...` all ok; sharded gui count; `-run '^TestComposer'` ok; `scripts/test-32bit.sh`; oraclelive build; js vet. The firmware size step NOW RUNS: Nix is reinstalled; put `/nix/var/nix/profiles/default/bin` on PATH and run `nix develop -c tinygo build -size short -o /dev/null -target pico-plus2 -stack-size 16kb -gc precise -opt 2 -scheduler tasks ./cmd/controller` in the worktree; the controller's baseline on fork main 321acb56 is 1,506,884 B flash / 62,592 B RAM and the implementer reports 1,579,924 / 62,800 on 27afa9f -- report your own numbers; a zero delta is Critical.

Severity: a finding folded wrongly, a guard that cannot fail, or a hunk outside the findings = Important (Critical if a guarantee breaks). Do not pad.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/composer-S3-exec-review-r1-fold-verification.md` (create; must not exist): per item VERIFIED / NOT VERIFIED with output; closing counts. Return a two-line summary plus the path.
