You are the INDEPENDENT fold-verification reviewer (sonnet tier, targeted) for round 1 of the R0 gate on `design/IMPLEMENTATION_PLAN_hashlock_H0_reader_guards.md` in mnemonic-engrave (`/scratch/code/shibboleth/mnemonic-engrave`). Round 0 = two lenses, persisted verbatim: fidelity (opus) at `1b254c9` — `design/agent-reports/hashlock-H0-plan-R0-r0-fidelity.md`, 2C/5I/2M/0N — and tests (sonnet) at `a7aebdc` — `design/agent-reports/hashlock-H0-plan-R0-r0-tests.md`, 0C/1I/3M. The fold is ONE commit, `fdfb040`, over the gate-green draft `b0af794`; its message maps every finding to a change and quotes the re-run gate (both repos, hand-wired by the controller).

ONE QUESTION: did the fold address every Critical and Important from both reports — FIXED / PARTIAL / NOT FIXED / DECLINED-with-reason, one line each — without introducing a contradiction or a false claim of its own?

Read-only on every repo; commit nothing; no sub-agents; read no `.jsonl`. You MAY run the plan's code in your OWN scratch copies (never the controller's at `.tmp/h0-fork` or `me-worktrees/h0-gate2`): fork copy `rm -rf /scratch/code/shibboleth/.tmp/h0-fork-verify && mkdir -p /scratch/code/shibboleth/.tmp/h0-fork-verify && (cd /scratch/code/shibboleth/seedhammer && git ls-files -z | tar --null -T - -cf - | tar -xf - -C /scratch/code/shibboleth/.tmp/h0-fork-verify)` with Go `/scratch/code/shibboleth/.toolchain/go/bin/go` first on PATH; engrave worktree `git -C /scratch/code/shibboleth/mnemonic-engrave worktree add --detach /scratch/code/shibboleth/me-worktrees/h0-verify fdfb040` with `PATH=$HOME/.cargo/bin:$PATH CARGO_TARGET_DIR=/scratch/code/shibboleth/mnemonic-engrave/target TMPDIR=/scratch/code/shibboleth/.tmp`, package `-p mnemonic-engrave`; remove the worktree when done. When you revert a mutation by restoring a backup file, `touch` it — cargo keys on mtime.

## Already settled — do not re-derive
- The two reports are the specification for this round; read them first, then `git show fdfb040`.
- The re-run gate (in the fold message) proved the plan's blocks build and their tests pass in both repos and that ten named mutations each fail the named test. Do not report compile/test findings on gated blocks; DO re-run any mutation whose claimed output you doubt.
- The spec is `/scratch/code/shibboleth/mnemonic-secret/design/SPEC_ms_hashlock.md` §1, §9, §12 item 7 only.

## Verify
1. **The finding table.** For each C and I in both reports (2 + 5 + 1 = 8), the plan section that now carries the fix, quoted, and a verdict. A finding the fold message says it addressed but whose text you cannot find is NOT FIXED. Then the Minors (2 + 3), one line each.
2. **The two Criticals, executed.** Apply the plan's Task 2 to your fork copy (every step, the corpus from Task 1 Step 1 applied to `sysw/testdata/`, sha `f1f2fa6bbbf27e3697ee496636de49be2f25787deff7b3bc4a2c5e16854e391c` — recompute it). (a) C-1: run `TestEngraveCodex32RefusesAPreimagePlate` and `TestScanDoesNotHandAPreimagePlateToEngrave`; then remove the `engraveCodex32` guard and confirm the first fails on the `Confirm Codex32 Secret` frame; then trace whether ANY door other than the two named still reaches `engraveCodex32`/`EngraveSeedString` with a preimage (`grep -n "engraveObjectFlow(\|engraveCodex32(\|codex32.New(" gui/*.go`). (b) C-2: run the six-population `TestIsPreimageReadsThePrefixByteOnly`; confirm `bip93-plain-payload-0x03` and `bip93-share-payload-0x03` classify `ClassCodex32Secret` in BOTH `sysw.Classify` and `seal.Classify` after the guard (write a throwaway test if needed, delete it after); confirm the three `NewSeed` strings decode to the payloads the corpus `source` fields claim (`Seed()[0]`, length, threshold/index via `Split`).
3. **I-3, executed.** Apply Task 1 Step 7 to your engrave worktree; run `sysw_pack_names_a_preimage_plate_and_never_echoes_it` and `a_preimage_plate_is_named_not_misdiagnosed`; swap the two arms and confirm the unit test fails with `Bip93OutsideTheProfile(75)`; confirm the `me sysw pack` stderr for a 90-character entr string is UNCHANGED (still the profile message) — the new arm must not have widened.
4. **New contradictions.** The fold rewrote Architecture, Global Constraints (predicate, "No new class", sha, anchors rule), the File Structure table, the site table, Task 1 (rows, Steps 5-9), Task 2 (Steps 1-3, 8, 9), Task 3 Step 2, Task 4, the self-review. Read as a hostile implementer: does any sentence contradict another, or contradict the code blocks? In particular: the corpus shape counts (2/6/4) — recompute from the rows; Task 2 Step 1's RED claim (two rows) versus the corpus; whether `errScanUnknownFormat` for a scanned plate contradicts spec §12 item 7 or the plan's own "named refusal" wording.
5. **Anchors.** Every `Modify` cites anchor text; check each exists exactly once in the fork at `839fa5aa` / engrave at `e06e29d`.

## Severity
A C/I marked FIXED in the message but not fixed in the text = Critical. A new contradiction between two normative sentences, or between the plan and its code blocks, or a mutation claim you cannot reproduce = Important. Wording = Minor/Nit. A clean round closes R0 for this plan (lens-closure: fidelity, tests/mutation, fold-verification).

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H0-plan-R0-r1-fold-verification.md` (create; must not exist): the finding table (8 C/I rows, then M rows), the executed checks with output, the anchor check, closing counts and a plain GREEN / NOT GREEN. Return a two-line summary plus the path.
