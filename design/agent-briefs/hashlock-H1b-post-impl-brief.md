You are the INDEPENDENT adversarial execution reviewer (opus tier) for the hashlock stage H1b implementation — `me` bumps to ms-codec 0.8 and keeps refusing a kind-`0x03` PREIMAGE plate by name on both host verbs. Branch `hashlock-h1b`, worktree `/scratch/code/shibboleth/me-worktrees/hashlock-h1b`, tip `<TIP>`, base master `<BASE>`. The plan is `design/IMPLEMENTATION_PLAN_hashlock_H1b_me_bump.md` (R0 GREEN at `eece8a3`), the spec `mnemonic-secret/design/SPEC_ms_hashlock.md` §1 rule 2, §3, §9, the implementer's report `design/agent-reports/hashlock-H1b-implementation-report.md` on the branch (its deviations are claims to verify).

ONE QUESTION: over the whole diff `git diff <BASE>..<TIP>`, can you construct an ms1 string for which `me` at ms-codec 0.8 places a preimage-kind payload as a seed, names it wrongly, or refuses a legitimate seed/share/BIP-93 secret it accepted at 0.7 — and does every test the diff adds actually fail on the defect it names?

Read-only on the branch and master; commit nothing; no sub-agents; never read any `.jsonl`. Build and RUN in your own detached worktree with its own target dir: `git -C /scratch/code/shibboleth/mnemonic-engrave worktree add --detach /scratch/code/shibboleth/me-worktrees/h1b-review <TIP>`; `PATH=$HOME/.cargo/bin:$PATH TMPDIR=/scratch/code/shibboleth/.tmp CARGO_TARGET_DIR=/scratch/code/shibboleth/.tmp/h1b-review-target`; package `-p mnemonic-engrave`. Mutate only there; revert (`touch`) after each; remove the worktree and target dir when done.

## Already settled — do not re-derive
- The controller re-ran the gates at `<TIP>` (numbers in the dispatch message). No compile/lint/fmt findings; the local nightly's `is_multiple_of` lint in `composer_records.rs:114` is pre-existing; the three `history_purge` failures are box-local.
- The plan's R0 closed under fidelity, tests/mutation and fold-verification; review the CODE against the SPEC. Rulings L24 (mismatch refused) and L26 stand. Secret-handling never gates.
- The device side (fork `c4a64fc`, H0) is out of scope except as the reference for what the host must NAME the same way (`codex32.IsPreimage`: unshared, 33-byte payload, first byte 0x03).

## Construct counterexamples — an assessment without one is Minor at most
1. **The kind space, exhaustively, through both verbs.** Build (with `ms encode`/`ms hashlock` from the released ms 0.18.0 at `/scratch/code/shibboleth/mnemonic-secret/target/debug/ms`, or by hand via the codec's encoder in a throwaway Rust test) every family: entr 16/20/24/28/32; mnem; the well-formed preimage plate; `0x03` under id `entr` (mismatch), under id `test` (unknown), with X of 16 and 33 bytes (`PreimageLengthMismatch`), as a 2-of-3 share; a plain BIP-93 16- and 33-byte secret whose first byte is 0x03 (the seam corpus has both); an UPPERCASE plate; a plate with a corrupted checksum. For each: `me sysw pack` and `me seal --seal-secret` exit code + first stderr line, and `sysw::classify`'s class. A preimage-kind payload placed (exit 0) is Critical; a legitimate 0.7-accepted seed now refused is Critical; a `0x03` single named "outside the profile" is Important.
2. **The wildcard.** Confirm `validate_record`'s `Ok(_)` arm refuses with a named message; then construct the hazard it guards: is there any way to reach it today (a `Payload` variant `me` does not enumerate)? If not, say so and confirm the arms enumerate exactly `Preimage | Entr | Mnem` at ms-codec 0.8.0.
3. **The predicate's negative space.** `preimage_plate` on: a share whose data begins 0x03; the 48-character plain BIP-93 0x03 row; the 33-byte 0x31 row; a 33-byte 0x03 unshared with id `test` (must be a plate); the entr-id mismatch (must NOT be a plate: `id_kind_mismatch`); a 0x03/17-byte plate (`PreimageLengthMismatch` → named). Quote each.
4. **Tests that cannot fail.** For each test the diff adds or edits, mutate the guard it names and quote the failure; a survivor on a spec-normative guarantee is Critical. Include: the witness's second assertion; the three new sysw assertions; `an_id_kind_mismatch_is_named_not_misdiagnosed`; the seam test's `preimage-plate-0x03` row.
5. **Records.** Version `0.8.1`; one `[Unreleased]` CHANGELOG section carrying H0's, the `+`-sign entry's and H1b's items with the H0 sentence in the past tense; F-473 closed with the SHA; F-454 advanced; the M-6 seam-prose correction filed under H2 and NOT edited; `Cargo.lock` moved only `ms-codec` (`git diff <BASE>..<TIP> -- Cargo.lock` — any other crate changed is Important).
6. **The report.** Every count in the implementation report must match what you measure at `<TIP>`; a false count is Critical.

## Severity
Critical: a placed preimage; a refused legitimate seed; a test that cannot fail on a normative guarantee; a false claim in the report. Important: a misnamed `0x03` string, a missing case, an unsound assumption, a lockfile change beyond ms-codec. Minor: secret-handling, wording, records.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H1b-post-impl.md` (in the MAIN checkout at that absolute path; create; must not exist): findings `### C-n / I-n / M-n / N-n — title` with the command, verbatim output and the spec line violated; the kind-space table (string family, `sysw pack` exit/first line, `seal` exit/first line, class); the mutation table; the deviations with a verdict each; closing counts and GREEN / NOT GREEN. Return a two-line summary plus the path.
