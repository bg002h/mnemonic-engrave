You are the INDEPENDENT adversarial execution reviewer for the composer's Stage 4 implementation: the emulator journey driver and its payload, spanning TWO repositories. You did not write it. Find defects the implementer and its own runs missed, by EXECUTING, mutating and constructing counterexamples.

## The one question
Do the two diffs -- fork branch `composer-s4-emu` (worktree `/scratch/code/shibboleth/wt-composer-s4-emu`, base fork `main` `60bee002`, tip `<S4_FORK_TIP>`) and mnemonic-engrave branch `composer-s4-emu` (worktree `/scratch/code/shibboleth/wt-engrave-s4-emu`, base `master` `<S4_ENGRAVE_BASE>`, tip `<S4_ENGRAVE_TIP>`) -- do exactly what `design/IMPLEMENTATION_PLAN_composer_S4_acceptance.md` at mnemonic-engrave `<S4_GREEN_SHA>` Tasks 1, 2 and 3 require (§2 fixture, §3 itineraries, §4 gates) -- no more, no less -- and is the capture a COMPARISON that can fail, rather than a photo shoot that cannot? The implementer's report: `design/agent-reports/composer-S4-implementation-report.md` -- re-derive, do not trust.

Read-only: no commits; every mutation reverted; `git status --porcelain` empty in both worktrees at the end (work in `cp -r` copies for mutations; never `git checkout` in a copied worktree -- it shares the gitdir). Go: `/scratch/code/shibboleth/.toolchain/go/bin/go` (1.26.7), `CGO_ENABLED=0 GOPROXY=off GOTOOLCHAIN=local`, default `-mod=readonly`, `TMPDIR=/scratch/code/shibboleth/.tmp`. Tools by path: `/scratch/code/shibboleth/descriptor-mnemonic/target/release/md`, `/scratch/code/shibboleth/mnemonic-engrave/target/debug/me`, `~/.cargo/bin/mk`, `~/.cargo/bin/ms`. Python Playwright with chromium is installed; the capture scripts build `emu.wasm` themselves (`cmd/emu/build.sh`) and serve `cmd/emu` -- point them at YOUR copy of the fork worktree (the plan's `EMU` override), and use ports other than 8793/8734 if a run is still live. Do NOT spawn sub-agents; read no `.jsonl` file.

## Already settled -- do not re-derive
Plan §2's oracle values (md/me/ms/mk runs; the keyed arm confirmed on the Go harness by the r0 journey lens; the keyless string is the CHUNKED 56-character form); Task 0 (the three shipped drivers exit 0 on 60bee002); every itinerary row's needle and order on the Go harness (r0 lens, quoted frames). The plan's R0 closed 0C/0I (journey lens + fold verification). The controller re-ran on the worktrees after the implementer finished: `<CONTROLLER_GATES>`.

## Lenses (say which found nothing)
1. **The comparison can fail -- prove it, three ways.** (a) The plan's named mutation: substitute the UNCHUNKED keyless string (`md15zfdsssj6tvyywtfdssj5hqqxqujzyxaduyd9dp5v3xc`) for the expected one in the host artifact and run the keyless arm: the capture MUST exit non-zero naming the byte mismatch (`md verify` accepts both forms, so a driver that verifies instead of comparing passes here -- Critical). (b) Corrupt one character of one expected address and one hex of the Policy-ID: each must fail with the "does not match" message. (c) Drop one chunk from `keyed.md1.txt` and separately make the census entry-count assertion tautological in a copy: name the assertion that catches each, or the absence. `--prove-it-can-fail` itself: read what it corrupts and confirm it is not a fixed string that a refactor could leave unreachable.
2. **Counterexamples through the driver**: a stale `emu.wasm` (the browser cache; `walk_s4_gate.js` has the guard), a wrapped paged screen (does the driver stop on recurrence or over-count shots?), a `shToolpath.strings()` entry that is NOT newline-joined as the plan assumes (measure `notifyPlateText`), the `SCAN` row at `Where from?` (the emulator reports NFC: does the driver select `FROM PAYLOAD` by NAME or by index?), the F1 `Payload Warnings` page count, the `Bundle engraved` terminal modal and the door after it. Any tap-by-index where the row set differs between harness and emulator is Important.
3. **The payload**: `cmd/buildpayloadcomposer` refuses on a wrong xpub (mutate the pinned `ms` value: does it refuse?); the blob's digest test recomputes rather than compares strings; the confinement test still discovers the new embed (delete the `//go:build js` line in a copy: does `embed_confinement_test.go` fail?); `shSysw("composer")` before the door.
4. **Mutation-test the host half**: `transcript_composer.sh` without `--force-chunked` on the keyless mint, and with `@2` at `48'/0'/2'/2'` instead of B's account 0' -- each must change an artifact the capture then rejects; `diff <(go run ./cmd/buildpayloadcomposer) out/composer/records.txt` as the plan's gate.
5. **What did the diff make false elsewhere**: the three shipped drivers still exit 0 (run them); `cmd/emu/needle_test.go` (any new single-site needle pinned?); `design/journeys/README.md` and the S3 plan's Task C2 note; `design/FOLLOWUPS.md` items the diff claims to touch.
6. **Whole-repo gates as CI runs them**: fork `CGO_ENABLED=0 go test -timeout 20m ./...` (or the sharded runner with an exhaustive partition), `scripts/test-32bit.sh`, the oraclelive build, `GOOS=js GOARCH=wasm go vet ./cmd/emu/`, `gofmt -l`; the firmware size recipe (`nix develop -c tinygo build -size short -o /dev/null -target pico-plus2 -stack-size 16kb -gc precise -opt 2 -scheduler tasks ./cmd/controller`, with `/nix/var/nix/profiles/default/bin` on PATH) must still read 1,579,940 B flash / 62,800 B RAM -- the diff touches nothing under `cmd/controller`, so a change is a finding. Engrave: `cargo fmt --check`, clippy and nextest are unaffected unless a Rust file changed (state which).

Severity: Critical = a comparison that cannot fail, a wrong oracle, an accepted mismatch, a state the walk cannot leave; Important = a tap-by-index that diverges, a missing arm of the negative control, a gate that did not run, a §2 value transcribed wrong; Minor/Nit recorded. Fixes as HYPOTHESES. Do not pad.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/composer-S4-exec-review-r0.md` (create; must not exist): per lens the commands + trimmed output and findings `C-n`/`I-n`/`M-n`/`N-n` with file:line, reproduction, hypothesis; closing counts. Return a two-line summary plus the path.
