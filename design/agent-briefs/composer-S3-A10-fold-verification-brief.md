You are the INDEPENDENT fold-verification reviewer (targeted, post-GREEN) for Task A10 of `/scratch/code/shibboleth/mnemonic-engrave/design/IMPLEMENTATION_PLAN_composer_S3_fork_gui.md` (master). One question: does the A10 fold (`git show 0051be733de0d4a8b0060a73e810bf00dba8ca0c`; `git diff 9081222..0051be733de0d4a8b0060a73e810bf00dba8ca0c -- design/IMPLEMENTATION_PLAN_composer_S3_fork_gui.md`) apply the four notes in `design/agent-briefs/composer-S3-A10-fold-notes.md` correctly against the facts, and did nothing outside Task A10 move? The fold author's report: `design/agent-reports/composer-S3-plan-A10-fold-report.md` (commit c677c6d).

## Facts to check the fold against (verify each by command, quote the output)
1. Vector names and wrappers: `ls /scratch/code/shibboleth/wt-composer-s0b/crates/md-codec/tests/vectors/ | grep preset` (30 files, six names `keyed_compose_preset_<archetype>`); wrapper per vector from each `.template` file's first characters (five `wsh(`, `kofn_recovery` = `tr(`); parameters from `crates/md-codec/tests/compose_support.rs:308-330` in that worktree. Every parameter, digest and name A10 now quotes must match those files byte-for-byte.
2. Fork numbers: `/scratch/code/shibboleth/seedhammer/scripts/vendor-compose-vectors.sh` line 16 glob covers `keyed_compose_*`; `md/compose_vectors_pin_test.go` has 26 names in `composeVectorNames` and asserts 126 files; A10 must say 32 and 156 with the file arithmetic (28x5 + 4x4), and 277 -> 307 for the fork's vectors directory.
3. A10 keeps §4d's OFFER (six under wsh and tr; plain k-of-n alone under sh/sh(wsh)) and narrows only the VECTOR PINNING to the six pinned (wrapper, preset) pairs, with the other six pairs checked structurally and named unpinned; the Expected line states a concrete count and naming. A tr vector's chunks must never be compared against a wsh preset.
4. A10 is BLOCKED until S0b ships on descriptor-mnemonic main, names the S0b plan path, says who records the merge commit and when, and its Step 1 builds `md` by path from that checkout rather than trusting a bare `md` on PATH.
5. No ```go fence was added to A10; every hunk of the diff lies between `### Task A10` and `### Task A11`; plan checks (`scripts/plan-cite-check.sh` with `CITE_FORK_ROOT=/scratch/code/shibboleth/seedhammer`, `plan-glyph-check.sh`, `plan-table-check.sh`, `plan-stepref-check.sh`) clean -- run them from `/scratch/code/shibboleth/mnemonic-engrave`. The controller measured: glyph 306 / 0, tables 129 / 0, stepref 0, cite 249/249, 18 hunks all inside A10 -- reproduce, do not trust.

Severity: a fact misquoted, a note misapplied, or a hunk outside A10 = Important; cosmetic = Minor/Nit. Do NOT spawn sub-agents, edit nothing, commit nothing, read no `.jsonl` file.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/composer-S3-plan-A10-fold-verification.md` (create; must not exist): per item VERIFIED / NOT VERIFIED with the command output; closing counts. Return a two-line summary plus the path.
