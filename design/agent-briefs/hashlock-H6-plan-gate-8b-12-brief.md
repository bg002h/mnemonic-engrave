You are the BUILD-GATE agent (opus tier) for the un-wired half of `design/IMPLEMENTATION_PLAN_hashlock_H6_preimage_plates.md` (engrave master `4b9a4dd8`) in mnemonic-engrave (`/scratch/code/shibboleth/mnemonic-engrave`). The plan author wired and gated Tasks 1-8a in three scratch trees (paths in the plan's `## Build gate` paragraph and in `design/agent-reports/hashlock-H6-plan-author-report.md` §5) and left Tasks 8b, 9, 10, 11, 12 -- the fork's `gui` surface: two new screens, the Hashlock plates flow, the changed `composerCensusLines` signature, five copy bodies, the walk arm -- SPECIFIED with no `file=` headers and never built. A gate that has never run is a hypothesis; your job is to run it.

DO, in the author's FORK scratch tree (continue from its state; it already holds Tasks 4-8a): apply Task 8b, then 9, 10, 11, 12 IN ORDER, one at a time, from the plan's text -- for each: create/modify the files the task names, run its RED (quote), implement, run GREEN, run every `MUTATION:` the task writes (quote the failure, revert), then `go test ./gui/ -run <that task's tests>`; after Task 9 and after Task 12 run the whole gui via `/scratch/code/shibboleth/mnemonic-engrave/scripts/gui-shard-test.sh ./gui/ 24`. Measure every body the tasks add with `assertModalBodyFits` (the spec's numbers were carried, not measured -- yours are the first) and every new screen's geometry at `sh2DisplaySize`. Go is `/scratch/code/shibboleth/.toolchain/go/bin/go`. `GOOS=js GOARCH=wasm go vet ./cmd/emu/` and `./cmd/emu/build.sh` at the end; firmware size at the end with nix on PATH (`export PATH=/nix/var/nix/profiles/default/bin:$PATH`; recipe in the plan) versus the author's 1,600,944 B.

ALSO: the author's whole-gui run failed on shard 18, twice, at the Task-8a tree -- find the failing test, decide whether it is a real regression from Tasks 4-8a or a pre-existing/flaky test (F-490 is a known load flake in `TestEngraveScreenReleasesResumeStateOnReturn`), and fix or report it as a finding with the evidence.

THEN EDIT THE PLAN: give every code block in Tasks 8b-12 its `file=<path> mode=whole|fragment` header and make the block text equal what you wired (the plan is the source; where the plan's block was wrong, fix the block and LIST THE FIX AS A FINDING: block, what failed verbatim, what you changed, why); update the plan's `## Build gate` paragraph so it no longer claims the half is un-gated, with your boundary tails; replace every carried-over number in Tasks 8b-12 with your measurement. Run `./scripts/h6-plan-blocks-vs-tree.sh` from the engrave checkout until every block passes (extend its symlink farm if a new path needs it). Leave the scratch trees in place.

RULES: edit only the plan, the checker script and the fork scratch tree; never the spec, any report or the repo checkouts; COMMIT NOTHING; no sub-agents; never read any `.jsonl`; no phrase or preimage bytes in logs you keep; every count you write comes from a run captured to a file under `/scratch/code/shibboleth/.tmp/` and quoted from there.

Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H6-plan-gate-8b-12.md` (create; must not exist): per task the RED/GREEN/mutation tails; the measured bodies and geometry; the shard-18 diagnosis; the fix table (every plan block you changed); the checker output; the verdict GATE GREEN / GREEN WITH FIXES (n) / RED with what remains. Return a two-line summary plus the path.
