You are the INDEPENDENT adversarial execution reviewer (opus tier) for the WHOLE hashlock stage H6 implementation — engraving hashlock PREIMAGE plates and PHRASE plates on the SeedHammer II from the composer (at HOLD) and from an unsealed wallet-policy payload carrying a `phrase:` record or a kind-0x03 preimage plate string. Spec `design/SPEC_hashlock_H6_preimage_plates.md` (`95418b2c`); plan `design/IMPLEMENTATION_PLAN_hashlock_H6_preimage_plates.md` (R0 GREEN `3890ad64`, blocks re-folded at `e095ea90`). Three trees under review:
- fork `/scratch/code/shibboleth/seedhammer`, integration branch `hashlock-h6`, tip `e089a539`, base main `fb0dd04` (implementers C, D, E, F: engrave/, codex32/, hashlock/, backup/, sysw/, gui/, cmd/emu/walk_hashlock_phrase.js);
- `me` `/scratch/code/shibboleth/mnemonic-engrave`, branch `h6-b`, tip `8cf2a7f9`, base master `f82b8c11` (implementer B: ms-codec 0.9.0 bump, `--pack-preimage`, the `phrase:` record, the preimage/phrase classes in `sysw::classify`, sealing decision);
- ms-codec 0.9.0 as PUBLISHED (ms master `1a4f4aa8`), already reviewed GREEN pre-publish (`design/agent-reports/hashlock-H6-A-pre-publish-review.md`) — reference only, not under review.
Implementer reports (claims to verify): `design/agent-reports/hashlock-H6-implementation-{B,C,D,E,F}-report.md`.

ONE QUESTION: over the whole diff of each tree, can you construct an input — a typed phrase, a `phrase:` record, an ms1 string, a payload — for which the device cuts a plate whose text or QR does NOT let a reader recover the same 32-byte preimage the host derives (wrong method line, wrong parameters, normalised or truncated phrase, a preimage plate string that decodes to different bytes, a QR whose text differs from the corpus's `qr_text` for the same phrase and method), or for which a hashlock PREIMAGE/PHRASE reaches a plate WITHOUT the NOT A SEED band, or reaches a SEED layout, or for which the host packs a phrase/preimage that the device then refuses or misclassifies — and does every test the diffs add actually fail on the defect it names?

Read-only on every branch; commit nothing; no sub-agents; never read any `.jsonl`. Build and RUN in your own detached worktrees with their own target dirs (fork: `git -C /scratch/code/shibboleth/seedhammer worktree add --detach /scratch/code/shibboleth/.tmp/seedhammer-h6-review e089a539`; me: `git -C /scratch/code/shibboleth/mnemonic-engrave worktree add --detach /scratch/code/shibboleth/me-worktrees/h6-review 8cf2a7f9`, `PATH=$HOME/.cargo/bin:$PATH TMPDIR=/scratch/code/shibboleth/.tmp CARGO_TARGET_DIR=/scratch/code/shibboleth/.tmp/h6-review-target`). Go is `/scratch/code/shibboleth/.toolchain/go/bin/go` first on PATH; whole-gui counts only via `/scratch/code/shibboleth/mnemonic-engrave/scripts/gui-shard-test.sh ./gui/ 24`. Mutate only in your worktrees; revert after each; remove them when done.

## Already settled — do not re-derive
- The controller re-ran every gate at the tips (numbers in the dispatch message): package tests, gui shards, gofmt, nextest/fmt/clippy on me, the plan-block checker, the emulator walk three times with the mutation discipline, firmware size. No compile/lint/fmt findings; do not report `go vet` (red at baseline, F-494).
- The spec's rulings and the plan's R0 rounds are settled; review the CODE against the SPEC. Secret-handling never gates (operator ruling 2026-08-27) — log it as Minor with a reproduction.
- ms-codec 0.9.0's `validate_phrase`/`looks_like_ms1`/`qr_text` are reviewed and published; the fork's `hashlock.ValidatePhrase`/`MethodLine`/`QRText` and me's use of the crate are what you review against them.

## Construct counterexamples — an assessment without one is Minor at most
1. **Host ↔ device digest identity, end to end.** For each corpus `qr_text` row and for edge phrases (100 chars; 100 with `:` and double spaces; leading/trailing spaces; a 64-hex string; an ms1-shaped string): `me sysw pack --pack-preimage` (both methods) → the payload's `phrase:` record → the device's classify/admission → the plate text and QR text. Compare the device's QR text byte-for-byte with ms-codec's `qr_text` for the same (phrase, method) and the plate's method line with `MethodLine`; compare the digest the device derives from the plate's phrase with `ms hashlock`'s digest. Any divergence is Critical.
2. **The preimage plate string path.** A kind-0x03 ms1 string packed on the host (`--pack-preimage` from a preimage, and a hand-made one via `ms hashlock`) → device → plate. `codex32.EncodeMS1Preimage` round-trips through ms-codec's decoder (build a tiny Rust or Go check) for random 32-byte preimages; uppercase input; a string with a corrupted checksum; a 0x03 string under id `entr` (mismatch) — must not reach a NOT-A-SEED plate (spec: the H6 admission path requires the `hash` id). A misclassified string that reaches the seed layout is Critical.
3. **The layouts.** `backup.HashlockPreimage`/phrase layouts: every plate carries the band (HASHLOCK PREIMAGE / HASHLOCK PHRASE) and NOT A SEED; the locator (mk1 stub) when seated; the QR (v9 constant-time encoder, scale-2 arm) only in phrase form and only below the text; the worst case (100-char hardened phrase, 194 bytes) fits — assert geometry from the goldens, not from text extraction. The constant-time argument: is `engrave.ConstantQR`'s command count independent of the payload at v9 (measure over the corpus + random payloads); is `engrave.QR` (content-dependent) reachable from any hashlock path (grep the call sites).
4. **The flows and every Back edge.** From the composer HOLD confirm and from the Wallet Policy door's "Hashlock plates" flow: trace each screen, the review step at Done, the free-text WARNING (must warn and cut, never refuse), the sealed-payload refusal, and each Back edge against the spec's Back table; the harness inputs must exist on the hardware (SH2 has no camera and the button set the walk uses must be real). Run the walk yourself once on a fresh port and mutate one guard the walk claims to prove (revert).
5. **The admission oracle.** `gui/sysw_admit_oracle_test.go` must now register E's sites and both class names; delete the `progWalletPolicy` admission row for the two classes and quote the failing line (D's M8).
6. **Tests that cannot fail.** For each test the diffs add or edit, mutate the guard it names and quote the failure; a survivor on a spec-normative guarantee is Critical. Include the implementers' own survivors and added tests (C's strengthened word-wrap row; D's added class test; E's oracle registration; B's classify rows).
7. **Records.** Version bumps; CHANGELOG entries; the fork's provenance pins (sysw's must name B's actual me commit — D's was PROVISIONAL); FOLLOWUPS closures (F-4xx) name real SHAs; the toolkit manual entry carries `--pack-preimage`, the `phrase:` wire form and both plate forms; the spec's falsified statements (Task 13) are updated. Every count in every implementer report must match what you measure; a false count is Critical.

## Severity
Critical: a host/device preimage divergence; a hashlock plate without the band or on a seed layout; a preimage-kind string placed as a seed; a refusal that does not refuse or a warning that refuses; a test that cannot fail on a normative guarantee; a false report count. Important: a missing screen/state/Back edge; a QR text or method line differing from the corpus; a content-dependent QR reachable from a hashlock path; a missing case; an unsound assumption. Minor: secret-handling, wording, records.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H6-post-impl.md` (in the MAIN checkout at that absolute path; create; must not exist): findings `### C-n / I-n / M-n / N-n — title` with the command, verbatim output and the spec line violated; the host↔device identity table (input, host digest, device digest, QR text equal?); the string-path table; the mutation table; the deviations with a verdict each; closing counts and GREEN / NOT GREEN. Return a two-line summary plus the path.

## Dispatch addendum (controller)
- Records live on two more read-only branches: engrave `h6-records` tip `32a46860` (worktree `/scratch/code/shibboleth/me-worktrees/h6-records`: Task 13's spec statements, FOLLOWUPS F-495..F-502 and closures, `design/ACCEPTANCE_hashlock_H6_preimage_plates.md`) and toolkit `h6-manual` tip `4fc30009` (worktree `/scratch/code/shibboleth/tk-worktrees/h6-manual`: the manual entry). The me CODE is on `h6-b` (8cf2a7f9) as stated above; F-501 (a falsified comment in ms `crates/ms-cli/src/cmd/hashlock.rs:352`) is being folded by the controller in ms and is not yours.
- Item 4's walk is the controller's three-run discipline, delegated to you: run `cmd/emu/walk_hashlock_phrase.js` unmutated on a fresh port (PASS), then TWO mutations of guards the walk claims to prove (implementer F's report §Task 12 names its two: pick at least one DIFFERENT guard of your own), each REJECTED with its message, then revert and run again (PASS). Each run on its own fresh port (the browser caches `emu.wasm`); the H2/H5 walk reports in `design/agent-reports/` show the playwright pattern (`window.__done`/`window.__walk`). Quote every run's result line.
- Controller-measured at the tips (do not re-derive): fork e089a539 — gofmt clean on every changed file, every non-gui package ok, gui 1287 tests / 24 shards ok, `go build ./cmd/emu` ok, firmware 1,643,580 B flash / 63,272 B RAM at bdb66963 (Task 12/13 add no firmware code); me 8cf2a7f9 — workspace nextest 642 run / 639 pass / 3 fail = the box-local `history_purge` trio (no `/usr/bin/zsh`, F-500), fmt clean, clippy red only on the pre-existing `is_multiple_of` lint under the box's rustc 1.98; plan checker 97 blocks / 0 FAIL against (fork e089a539, ms h6-a 1a4f4aa8, me h6-b 8cf2a7f9).
