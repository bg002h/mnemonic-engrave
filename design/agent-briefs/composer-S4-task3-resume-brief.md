You are the SAME single implementer, resumed a second time, to finish Task 3 of the composer Stage 4 plan (`design/IMPLEMENTATION_PLAN_composer_S4_acceptance.md`, now at mnemonic-engrave `fdce82f` -- re-read Task 3 in full: the W-2 fold changed its preamble). W-2 is fixed, verified (0C/0I) and MERGED into fork `main` as `3cc71d9bbe0f211afe2a8e3facdf57f4a3a66d1b` (merge of `composer-s4b` `2dff0ee`).

## Where
- Fork: in `/scratch/code/shibboleth/wt-composer-s4-emu` (branch `composer-s4-emu`, tip `05d903b` on `60bee002`), first `git merge --no-ff main` (fork `main` is now `3cc71d9bbe0f211afe2a8e3facdf57f4a3a66d1b`; `git -C /scratch/code/shibboleth/seedhammer rev-parse main` to confirm) with a one-line message naming W-2; resolve nothing by hand -- there should be no conflict (Task 1 touched `cmd/` only; W-2 touched `gui/` only); if there is one, stop and report.
- Engrave: `/scratch/code/shibboleth/wt-engrave-s4-emu` (branch `composer-s4-emu`, tip `5040bb2`), unchanged.
- Same environment and tool paths as before; `TMPDIR=/scratch/code/shibboleth/.tmp`; build `emu.wasm` from the fork worktree; serve on a port other than 8793/8734 if one is busy; `EMU`/`FORK` overrides as you added them.

## What to write
`cmd/emu/shots_composer.js` and `design/journeys/capture_composer.py` exactly per plan Task 3: two arms (`--arm keyed|keyless|both`), each reloading the page; `--prove-it-can-fail`; the comparison rules (split each `shToolpath.strings()` entry on `"\n"`, flatten, compare byte for byte against the host file; assert entry count == the census screen's plate count); the paged screens read until the first page recurs; the engrave loop with the `Bundle engraved` handler terminating on the door; **rows selected by TAPPING at the frame's own geometry** (measure the row bands the way `walk_build_policy.js` measured `rowY`, or read them from the layout; never inject `Up`/`Down`); the keyless arm calling `shSysw("none")` after the boot offer. Every assert in the itinerary tables is an assert in the driver; every "shot" is a screenshot the capture keeps.

## Gates (paste real output)
`python3 capture_composer.py --arm both` exit 0; `python3 capture_composer.py --arm keyed --prove-it-can-fail` exit 0 printing the caught message; the plan's named mutation -- substitute the UNCHUNKED `md15zfdsssj6tvyywtfdssj5hqqxqujzyxaduyd9dp5v3xc` for the expected keyless string in a COPY of `out/composer/keyless-tr.md1.txt` and run the keyless arm: it MUST exit non-zero naming the byte mismatch (paste it; restore the file); the three shipped drivers exit 0 against the fork worktree; `GOOS=js GOARCH=wasm go vet ./cmd/emu/`; `gofmt -l cmd/`; `CGO_ENABLED=0 go test -count=1 ./cmd/emu/` (if `needle_test.go` pins a count your driver changes, that is a finding to report, not a test to edit). Commit per repo, signed-off, with the trailer lines; never push, never flash.

## Report (your final action)
APPEND a "Task 3 -- DONE" section to `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/composer-S4-implementation-report.md` (the file exists; append, do not rewrite): the merge commit in the fork worktree, `git log --oneline main..HEAD` and `master..HEAD`, every value the emulator printed beside the plan's (Template-ID, both stubs, Policy-ID, four addresses, the census lines, the engraved strings), the negative control's and the named mutation's output, the shipped drivers' exit codes, any deviation from a plan Expected line verbatim, anything you decided or could not do. Return a two-line summary plus the path. No sub-agents; read no `.jsonl` file.
