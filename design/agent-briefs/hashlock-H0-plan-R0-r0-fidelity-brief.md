You are the INDEPENDENT fidelity + design reviewer (opus tier) for round 0 of the R0 gate on `design/IMPLEMENTATION_PLAN_hashlock_H0_reader_guards.md` in mnemonic-engrave (`/scratch/code/shibboleth/mnemonic-engrave`, plan committed at `b0af794`). The spec it implements is `SPEC_ms_hashlock.md` §9 (H0) in `/scratch/code/shibboleth/mnemonic-secret/design/` (read §1, §9 and §14; the rest of that spec is H1's and is NOT your scope). The fork under change is `/scratch/code/shibboleth/seedhammer` at main `839fa5aa`.

ONE QUESTION: if an implementer follows this plan literally, will a kind-0x03 hashlock PREIMAGE plate string be INERT on every path by which the flashed SeedHammer II or `me` today would treat it as a seed — and does the plan claim nothing false about the code it cites?

Read-only; commit nothing; no sub-agents; read no `.jsonl`. You MAY grep and read both repos and the fork freely. Do NOT re-audit the H1 plan or the spec; do not propose widening H0 beyond "0x03 is inert" (the wider device/host convergence question is explicitly a follow-up in the plan).

## Already settled — do not re-derive
- The build gate ran (controller hand-wire of every block into scratch copies of both repos; output verbatim in `git show b0af794`): Rust 2/2 tests pass, fmt clean, the pin test fails under the recorded mutation; Go `codex32`/`sysw`/`seal` pass, `go test -run TestUnlock ./gui/` passes with the new refusal test, and the three Go mutations each fail the named tests (the gui one ends on the "Engrave Plate" frame). Do not report compile/test findings on those blocks.
- The vector string is a real 0x03 plate from H1's gated binary; ms-codec 0.7 refuses it (`reserved-prefix byte was 0x03`). The corpus sha256 after the row is `4ac542ea8e0e36d92127b744bce0a83072f787870756bf7b86b9c947bb1370a5`, measured.
- Secret-handling defects never gate (operator ruling 2026-08-27).

## Verify (construct counterexamples; an assessment without one is a Minor at most)
1. **Coverage of the reader table.** The spec's §9 table names `isStrictMs1` / `seal.Classify` and `me`'s `validate_record`; the plan guards those plus `unlockEngraveCodex32`. Enumerate EVERY other site in the fork that turns a codex32 string into a seed or an engrave without passing through one of the guarded functions: grep `codex32.New(`, `ClassCodex32Secret`, `DecodeMS1(`, `EngraveSeedString(` across `gui/`, `seal/`, `sysw/`, `backup/`, `cmd/`; for each hit, trace whether a 0x03 string can reach it (`gui/transaction.go:280`, `gui/composer_door.go:45`, `gui/sysw_admit.go`, `gui/ms1_decode.go`, `gui/scan.go` are the ones to start with). A path that still shows, uses or cuts a preimage plate as a seed is Critical.
2. **The "no new class" consequence.** With the guard, `seal.AdmitSection` refuses an ENCRYPTED section that carries a preimage record whole (`ErrRecordNotPermitted`), as for any unknown record. Argue from the spec and from what the device shows the operator whether refusing the section is the right H0 behaviour versus skipping the record; a wrong choice that loses access to the OTHER secrets in a real payload is Important. Note that `me` 0.7 cannot pack such a payload today.
3. **Rust-primary compliance.** The plan calls this a convergence fix (Go adopting Rust's current refusal). Check the claim against `crates/me-cli/src/seal/record.rs` and `sysw/mod.rs` at `e06e29d`: does `me` at ms-codec 0.7 refuse the 75-character `hash`-id string for the KIND (prefix) and not merely for some earlier gate (tag table, length) that the 0.8 bump would also change? If the pin test could stay green after the bump for a reason other than a refusing arm, that is Important — the test's whole purpose is to be the tripwire.
4. **Fragment fidelity.** Every `Modify` block cites `path:line`; check each against the real files (fork at `839fa5aa`, engrave at `e06e29d`) — the anchor text must exist exactly once. A block that cannot be applied where the plan says is Important.
5. **Task 3/4 and the flash.** The plan defers on-device acceptance to H2 because `me` cannot produce the payload. Is there a cheaper acceptance available today (e.g. a Go test helper writing a blob, the simulator's Load Payload) that the plan should require before the flash? If yes and it is cheap, Important; if not, say so.

## Severity
Critical: a path that still treats the plate as a seed, or a plan step that produces wrong behaviour. Important: a real defect, missing case, unsound assumption, unappliable fragment. Minor/Nit: wording, ordering, records.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H0-plan-R0-r0-fidelity.md` (create; must not exist): findings as `### C-n / I-n / M-n / N-n — title`, each with the plan section, the counterexample or the exact grep/trace that shows it, and a prescribed fix marked as a SUGGESTION (the controller decides the remedy); the site enumeration from item 1 as a table (site, reachable by 0x03?, guarded by?); closing counts. Return a two-line summary plus the path.
