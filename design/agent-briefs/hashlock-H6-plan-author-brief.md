You are the PLAN AUTHOR (opus tier) for hashlock stage H6 (preimage plates) in mnemonic-engrave (`/scratch/code/shibboleth/mnemonic-engrave`). Write `design/IMPLEMENTATION_PLAN_hashlock_H6_preimage_plates.md` from the spec `design/SPEC_hashlock_H6_preimage_plates.md` at engrave master `<SPEC_GREEN_SHA>` (STATUS R0 GREEN). Precedent: `design/IMPLEMENTATION_PLAN_hashlock_H5_device_polish.md` (header, Global Constraints, File Structure with `file=`/`mode=` block headers, task shape, MUTATION comments, `## Build gate` paragraph, the per-task-boundary gate) and its checker `scripts/h5-plan-blocks-vs-tree.sh` (generalise or wrap for H6 as `scripts/h6-plan-blocks-vs-tree.sh`).

THE JOB: a plan an implementer can follow literally, task by task, test-first, whose code blocks you have ALREADY WIRED AND RUN in scratch trees -- built and tested at EVERY TASK BOUNDARY. H6 spans THREE repos and the order is fixed by the Rust-first rule: (1) mnemonic-secret: the QR text format and the phrase record's method spelling as corpus rows (`crates/ms-codec/tests/vectors/hashlock-v0.8.json` -> a new corpus version or file per the spec) and, if the spec asks, `ms hashlock` reading the QR text from `--in`; (2) mnemonic-engrave `me`: the two record kinds in `crates/me-cli/src/sysw/` (Rust first, vectors), `--pack-preimage`, sealing polarity and the warnings, the host-side `hash`-id narrowing of `preimage_plate`; (3) the fork: the constant-time QR encoder raise to v9 with goldens and the constant-time argument (`engrave/`), the dedicated plate layout (`backup/`, on the `backup.Passphrase` pattern), device admission (`sysw/` classes, the `hash`-id check, `progWalletPolicy` only), the composer state and screens (retention keyed by digest, `Which hash?` rows with lazy derive, the review at the engrave Done census, cut order + abort arm, provenance-aware copy, `PREIMAGE REQUIRED` marking), the Hashlock plates flow under the Wallet Policy door, the free-text/passphrase warning, the walk; (4) records (toolkit manual, FOLLOWUPS, H2/composer spec folds). Split tasks so that DISJOINT file groups exist for parallel implementers (the controller will assign A/B/C/D by file lists as for H5); say which tasks share files.

Gate you run yourself, per task in order: ms scratch (`cargo nextest run --locked` with an own `CARGO_TARGET_DIR` under `/scratch/code/shibboleth/.tmp/`, `TMPDIR=/scratch/code/shibboleth/.tmp`, `PATH=$HOME/.cargo/bin:$PATH`), engrave scratch (`cargo nextest run --locked -p mnemonic-engrave --no-fail-fast`; the box-local `history_purge` trio may fail -- name it), fork scratch `/scratch/code/shibboleth/.tmp/h6-gate` (copy of fork main `fb0dd04` via `git ls-files | tar`; Go `/scratch/code/shibboleth/.toolchain/go/bin/go`; package tests per task; whole `gui` via `/scratch/code/shibboleth/mnemonic-engrave/scripts/gui-shard-test.sh ./gui/ 24` after each fork task boundary; `GOOS=js GOARCH=wasm go vet ./cmd/emu/`; `./cmd/emu/build.sh`; firmware size with nix on PATH before/after, hook-style deltas stated per change). QR goldens: render every QR the spec names and compare module counts to the spec's numbers. Every `MUTATION:` you write, run once and quote. Checker PASS on every block. The emulator walk is out of your scope to run.

Rules: code from the actual sources (open every file you cite); every `file:line` re-grepped at the named revisions; no placeholders; Interfaces blocks; explicit `git add` lists; MUTATION comments on every test; baseline revisions in the header. Edit only the new plan, the checker script and your scratch trees; commit nothing; no sub-agents; never read any `.jsonl`; no phrase or preimage bytes in logs. Leave the scratch trees in place.

Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H6-plan-author-report.md` (create; must not exist): per task the RED/GREEN/mutation tails, boundary test tails, size and QR numbers, checker output, anything in the spec you could not implement as written (why, with the measurement). Return a two-line summary plus the report path.
