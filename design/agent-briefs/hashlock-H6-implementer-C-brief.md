You are implementer C for hashlock stage H6: **Tasks 4, 5, 5b, then 6** (the fork: the constant-time QR encoder raise to v9 and the scale-2 arm with goldens; `EncodeMS1Preimage` and `IsPreimagePlate`; package `hashlock`'s corpus re-vendor from implementer A's branch plus `MethodLine`/`QRText`; the dedicated plate layout with its goldens). Repo `/scratch/code/shibboleth/seedhammer` (main `fb0dd04`); worktree: `git -C /scratch/code/shibboleth/seedhammer worktree add -b h6-c /scratch/code/shibboleth/.tmp/seedhammer-h6-c main`. Task 5b's corpus rows come from the ms branch `h6-a` at `/scratch/code/shibboleth/ms-worktrees/h6-a` (read-only; the plan names the file and the sha to pin). Your files: engrave/engrave.go, engrave/h6_qr_test.go (new), engrave/engrave_test.go, engrave/testdata/h6-qr-v{6,7,8,9}-scale2.bin (new), codex32/msencode.go, codex32/mspayload.go, codex32/msencode_preimage_test.go (new), hashlock/testdata/hashlock-v0.8.json + .provenance.json (re-vendor), hashlock/hashlock_test.go, hashlock/hashlock.go, hashlock/methodline_h6_test.go (new), backup/hashlock.go (new), backup/hashlock_test.go (new), backup/passphrase_test.go, backup/testdata/hashlock-*.bin (new). The budget fuzz values are the plan's (already run at 43M payloads); do not re-run the campaign, run the plan's pin test. Firmware size at the end with nix on PATH (`export PATH=/nix/var/nix/profiles/default/bin:$PATH`; recipe in the plan) and record the delta. Whole gui shard set once at the end (your packages are below gui; run it to prove nothing above broke).
RULES (every H6 implementer): the plan is `design/IMPLEMENTATION_PLAN_hashlock_H6_preimage_plates.md` at engrave master `3890ad64` (STATUS R0 GREEN) implementing `design/SPEC_hashlock_H6_preimage_plates.md` (`95418b2c`); execute ONLY your tasks, exactly as written, test-first (RED quoted, GREEN, every `MUTATION:` re-run once and reverted). The plan author and gate agent left GATED scratch trees with every task applied (`/scratch/code/shibboleth/.tmp/h6-ms`, `/scratch/code/shibboleth/.tmp/h6-me`, `/scratch/code/shibboleth/.tmp/h6-gate`) -- you may READ them to resolve a block whose context is unclear; never copy files wholesale; never modify them. Touch ONLY the files your tasks list (other groups work in parallel on disjoint files; a file outside your list is a merge conflict for the controller -- if a task needs one, STOP and report). Go is `/scratch/code/shibboleth/.toolchain/go/bin/go`, first on PATH; whole-gui counts only via `/scratch/code/shibboleth/mnemonic-engrave/scripts/gui-shard-test.sh ./gui/ 24`; Cargo env `PATH=$HOME/.cargo/bin:$PATH TMPDIR=/scratch/code/shibboleth/.tmp CARGO_TARGET_DIR=/scratch/code/shibboleth/.tmp/<your own dir>`, `cargo nextest run --locked`, `cargo fmt --all -- --check` before every commit. Commit per task with the plan's commit messages, `git commit -F <file>` (fish shell; `-s` on the fork), trailers `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01Fs3bg7TRfuSaFcCEkskwXA`; stage paths explicitly. Nothing pushed; no `main`/`master` commits; no sub-agents; never read any `.jsonl`; no phrase or preimage bytes in any log you keep. If a step is wrong, do the MINIMAL correct thing and record the deviation. Every count in your report comes from a run at your tip, captured to a file under `/scratch/code/shibboleth/.tmp/` and quoted from there. FINAL ACTION: write your report (path below; create; must not exist) with per-task commit SHAs, RED evidence, PASS counts, every mutation re-run with its failing line, every deviation; return a two-line summary plus the path and your branch tip.
Report: /scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H6-implementation-C-report.md
ORDERING ADDENDUM (controller, at dispatch): implementer A is running CONCURRENTLY on ms branch `h6-a`. Do Tasks 4 and 5 first. Before Task 5b, check that `/scratch/code/shibboleth/ms-worktrees/h6-a` exists and that its `git log` carries A's corpus commit (the `qr_text` rows in the corpus file the plan names). If it does not, do NOT invent the sha and do NOT take the rows from the gated tree: stop after Task 5's commit, return a one-line status naming your branch tip and "awaiting h6-a corpus", and do NOT write the report yet -- the controller will message you the ms commit to pin, and you then continue with 5b, then 6, then write the report as your final action.
