You are the INDEPENDENT fold-verification reviewer (targeted) for one finding from the S4 journey walk on the SeedHammer II: W-4 in `/scratch/code/shibboleth/mnemonic-engrave/design/S4_journey_walk_2026-09-02.md` (the composer's digit pad draws its prompt and its range/echo line over each other). The fix is fork branch `composer-s4d`, tip `bb5077547b422c47084850b119d772c4d9fcc28b` on `6fb90cb` (worktree `/scratch/code/shibboleth/wt-composer-s4d`); the implementer's report is `design/agent-reports/composer-S4-W4-fix-report.md` -- re-derive, do not trust. One question: are the digit pad's prompt and echo line now two separate, fully visible lines on every pad, can the regression test fail, and did nothing else move?

Read-only: copy the worktree with `cp -r` to `/scratch/code/shibboleth/.s4d-verify/` for every mutation (never dirty the worktree; do not run `git checkout` in a copied worktree -- it shares the gitdir). Go: `/scratch/code/shibboleth/.toolchain/go/bin` on PATH, `CGO_ENABLED=0 GOPROXY=off GOTOOLCHAIN=local`, `-mod=readonly`, `TMPDIR=/scratch/code/shibboleth/.tmp`. Do NOT spawn sub-agents; commit nothing; read no `.jsonl` file.

## Verify
1. `git diff 6fb90cb..bb5077547b422c47084850b119d772c4d9fcc28b --stat` and the hunks: the change is confined to `gui/composer_digitpad.go` (the box, prompt and echo laid out as ONE vertically centred group in the band above the keyboard; no per-line clamp) and the new geometry test; `gui/composer_digitpad_geometry_test.go` (the S4 driver's `DIGIT_KEY_PITCH` pin) is UNCHANGED and passes; no other production file changed. A hunk that changes the validator, the cap, Back, or the keyboard geometry is Important.
2. The geometry test renders all four pads (blocks, days, date, height) empty and filled and asserts no two text rectangles intersect, every text rectangle lies between the title and the keyboard, and both lines are present; it FAILS on `6fb90cb` (throwaway worktree at that revision) naming the overlapping pair, passes on the fix, and fails again in a copy where the per-line clamp is restored. Prove the mutated line RAN.
3. Drive the pads through the real flow on the harness (`composerLockEdit` via the shape flow, or the pads directly with the real validators) with the longest echo each validator can produce (an out-of-range value: the §8u ceiling line; an impossible date) and confirm the group still fits: a wrapped two-line echo that overlaps or is clipped is Important.
4. Gates as the brief names them, incl. the sharded gui runner (count = 1189 + the new tests) and the firmware size against `1,580,580 B flash / 62,800 B RAM` at `6fb90cb` -- report the delta.
5. The emulator proof: build `emu.wasm` from the copy, drive the operator's route (SKIP; Wallet Policy -> Build a new policy -> Taproot -> decaying-multisig -> Path 1 -> Time lock -> After a wait -> Blocks) and the date pad, screenshot each empty and after typing, and LOOK at the PNGs: two legible lines under the box, none under the keyboard or the buttons. Paste the paths and `shScreen()`. Then `design/journeys/capture_composer.py --arm both` against the copy: exit 0.

Severity: an overlapping or clipped line on any pad, a test that cannot fail, or a hunk outside W-4 = Important; cosmetic = Minor/Nit. Do not pad.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/composer-S4-W4-verification.md` (create; must not exist): per item VERIFIED / NOT VERIFIED with output; the size delta; closing counts. Return a two-line summary plus the path.
