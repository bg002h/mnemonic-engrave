You are the INDEPENDENT fold-verification reviewer (sonnet tier, targeted) for round 1 of the H0 post-implementation review. Round 0: `design/agent-reports/hashlock-H0-post-impl.md` (opus, engrave master `87771f6`): 1C/2I/4M/1N on engrave `hashlock-h0` at `265dc8e` and fork `hashlock-h0` at `14afdff`. The fold, by the same implementer: engrave `hashlock-h0` now at `95cd48a` (worktree `/scratch/code/shibboleth/me-worktrees/hashlock-h0`), fork `hashlock-h0` at `83fbc17` (worktree `/scratch/code/shibboleth/.tmp/seedhammer-hashlock-h0`); the "Post-review fold" section of `design/agent-reports/hashlock-H0-implementation-report.md` on the engrave branch maps each finding to its commit and proof.

ONE QUESTION: did the fold fix the Critical and both Importants — FIXED / PARTIAL / NOT FIXED / DECLINED-with-reason, one line each — with a test that can FAIL for each, without a regression or a false claim of its own?

Read-only on every repo and both branches; commit nothing; no sub-agents; never read any `.jsonl`. Build and run in your OWN copies: engrave `git -C /scratch/code/shibboleth/mnemonic-engrave worktree add --detach /scratch/code/shibboleth/me-worktrees/h0-verify3 95cd48a` with `PATH=$HOME/.cargo/bin:$PATH TMPDIR=/scratch/code/shibboleth/.tmp CARGO_TARGET_DIR=/scratch/code/shibboleth/.tmp/h0-verify3-target`, package `-p mnemonic-engrave`; fork: `rm -rf /scratch/code/shibboleth/.tmp/h0-verify3-fork && mkdir -p /scratch/code/shibboleth/.tmp/h0-verify3-fork && (cd /scratch/code/shibboleth/.tmp/seedhammer-hashlock-h0 && git ls-files -z | tar --null -T - -cf - | tar -xf - -C /scratch/code/shibboleth/.tmp/h0-verify3-fork)` with `/scratch/code/shibboleth/.toolchain/go/bin/go` first on PATH. Mutate only in those copies, revert (and `touch`) after each mutation, remove them when done.

## Already settled — do not re-derive
- Round 0 confirmed all 13 original mutations kill their tests, firmware 1,583,100 / 62,800, corpus byte-identical, no false claim in the implementation report. Do not re-run those unless the fold touched them.
- The three `history_purge` failures are box-local. Secret-handling never gates.
- The controller's decisions (in the fold's commit messages): C-1 → the guard at the top of the `for` body plus a Recover-arm test; I-1 → behaviour unchanged, wording fixed, a `bip93-plain-33-byte-payload-0x03` corpus row (host false / device false) and the record-corpus capture at 38; I-2 → `RecordError::PreimagePlate` in `validate_record` so `me seal` names the kind; M-1/M-3/M-4 wording; N-1 the pin test discriminates the variant. Do not argue the decisions; verify them.

## Verify (execute; quote output)
1. **C-1.** Read the new `engraveCodex32`; run the new Recover-arm test; then move the guard back above the loop and run it again — it must FAIL on a `Confirm Codex32 Secret` (or `Engrave Plate`) frame. Also run `TestEngraveCodex32RefusesAPreimagePlate` and `TestScanDoesNotHandAPreimagePlateToEngrave` still green. Then think like the round-0 reviewer: is there any OTHER reassignment or re-entry that hands a `codex32.String` to the loop or to `backupSeedStringFlow` without passing the guard (grep `scan =`, `engraveCodex32(`, `backupSeedStringFlow(`)?
2. **I-1.** The corpus: 13 rows, recompute the sha256 and check BOTH literals; shapes both/device-only/neither recomputed; the new row's device verdict via `sysw.Classify` AND `seal.Classify` (both must be not-secret) and the `0x31` row still a secret in both. Flip the new row's `device_admits` to true in a copy and confirm the FORK seam test fails; flip its `host_admits` to true and confirm the RUST seam test fails (the Rust test asserts the host verdict and `host <= device`, so only the host flip bites there — the implementer measured this and it is correct). Read the new doc comment: does any sentence still claim 33-byte plain seeds are untouched? Capture: 38 records, `record_corpus` tests 6/6.
3. **I-2.** `printf '%s\n' "<the plate>" | me seal --seal-secret --out <tmp>/a.uf2` → non-zero exit, stderr names "hashlock PREIMAGE plate", contains neither `reserved-prefix byte` nor the plate; the same with an entr-32 string FIRST and the plate second (the multi-record case). `me sysw pack` unchanged. The pin test asserts `Err(RecordError::PreimagePlate)`; mutate `validate_record` to skip the new arm and confirm the pin test and the seal test both fail.
4. **Regressions.** Whole engrave crate `--no-fail-fast` (quote the Summary; only `history_purge` may fail); fork `go vet` + `go test ./codex32/ ./sysw/ ./seal/` + `go test -run 'TestEngraveCodex32|TestScan|TestUnlockEngrave|TestConfirmCodex32|TestClassify|TestAdmit|TestRecover' ./gui/`; `gofmt -l` on the touched Go files empty; firmware re-measured (`nix develop -c tinygo build -size short -o /dev/null -target pico-plus2 -stack-size 16kb -gc precise -opt 2 -scheduler tasks ./cmd/controller`, with `/nix/var/nix/profiles/default/bin` on PATH) within a few hundred bytes of 1,583,100 and RAM 62,800.
5. **The report.** Every count in the "Post-review fold" section must match what you measure at the tips; a false count is Critical.

## Severity
A C/I marked FIXED but not fixed = Critical. A test the fold added that cannot fail on its defect = Critical. A regression, a count or hash that does not reproduce, or a surviving reassignment path = Important. Wording = Minor.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H0-post-impl-r1-fold-verification.md` (in the MAIN checkout at that absolute path; create; must not exist): the finding table (3 C/I rows, then the 5 M/N), the executed checks with output, the regression check, closing counts and GREEN / NOT GREEN. Return a two-line summary plus the path.
