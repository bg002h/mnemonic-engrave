You are the ONE implementer for hashlock stage H2 (the device leg: a hashlock phrase typed on the SeedHammer II derives the same digest the host's `ms hashlock` derives). Execute the GREEN plan `/scratch/code/shibboleth/mnemonic-engrave/design/IMPLEMENTATION_PLAN_hashlock_H2_device.md` (engrave master `1cb05b8`, STATUS R0 GREEN) task by task, test-first, exactly as written. The spec is `/scratch/code/shibboleth/mnemonic-engrave/design/SPEC_hashlock_H2_device.md` (GREEN `55ee7a4`). Use the superpowers `executing-plans` discipline: one task at a time, RED then GREEN, commit per task.

Two repos, two worktrees, nothing pushed, no `master`/`main` commits:
- seedhammer fork (Tasks 1-5): `git -C /scratch/code/shibboleth/seedhammer worktree add -b hashlock-h2 /scratch/code/shibboleth/.tmp/seedhammer-hashlock-h2 main` (the fork's worktrees live OUTSIDE the repo; `main` is `c4a64fc`). Go is `/scratch/code/shibboleth/.toolchain/go/bin/go`, first on PATH. Fork commits are `git commit -s` (DCO), author Brian Goss, plus the trailers below. Whole-package `gui` runs go through `/scratch/code/shibboleth/mnemonic-engrave/scripts/gui-shard-test.sh` (24 shards; it asserts its partition is exhaustive) -- never a bare `go test ./gui/` for a count; `-run` filters for a single test. The corpus to vendor is ms `crates/ms-codec/tests/vectors/hashlock-v0.8.json` at `/scratch/code/shibboleth/mnemonic-secret` `cd0a60f` -- copy the bytes, pin the sha256 as the plan says.
- mnemonic-engrave (Task 6 records): `git -C /scratch/code/shibboleth/mnemonic-engrave worktree add -b hashlock-h2 /scratch/code/shibboleth/me-worktrees/hashlock-h2 master`. No Cargo work in this stage.
- Task 5 Step 2 (firmware size) needs nix on PATH: `export PATH=/nix/var/nix/profiles/default/bin:$PATH` first; the recipe is in the plan (from the fork worktree: `nix develop -c tinygo build -size short -o /dev/null -target pico-plus2 -stack-size 16kb -gc precise -opt 2 -scheduler tasks ./cmd/controller`). Task 5 Step 1: WRITE the walk script and run it once yourself on an emulator built from your branch on a FRESH port (see `cmd/emu/walk_h0_preimage.js` and how the H0 report ran it); record its output. The controller re-runs it independently. The merge, push, flash and H4 are NOT yours.
- Task 6: the FOLLOWUPS entries in the engrave worktree (numbers continue the file's sequence -- F-476 is the last filed) and the fork CHANGELOG lines; the post-implementation review is the controller's; do not run it.

Rules:
- TDD per step: the plan's RED steps quote the failure text they expect -- reproduce it, then implement, then PASS; every `MUTATION:` comment in the plan's tests is a claim you re-run once (mutate, see the named failure, revert -- `touch` a file you restore from a backup). Commit per task with the plan's commit messages. Every commit message ends with:
  `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and
  `Claude-Session: https://claude.ai/code/session_01Fs3bg7TRfuSaFcCEkskwXA`
  Stage paths explicitly (no `git add -A`). The shell is fish: write messages to a file and `git commit -F`.
- The gate agent's report `design/agent-reports/hashlock-H2-plan-build-gate.md` records fixes the plan's blocks needed when it was first wired; the plan at `1cb05b8` has those folded. If a step is still wrong (an anchor moved, an Expected line differs, a test's text is off), do the MINIMAL correct thing and record the deviation -- what the plan said, what you did, why -- in your report. Never silently diverge. If genuinely blocked, stop and report.
- The phrase bytes are secret material: never print a phrase or a preimage to a log you keep; the plan's copy is the only place they appear.
- Every count you write in the report comes from a run at the tip you report, captured once to a file under `/scratch/code/shibboleth/.tmp/` and quoted from there.
- Never read any `.jsonl` file.

FINAL ACTION (mandatory): write `/scratch/code/shibboleth/me-worktrees/hashlock-h2/design/agent-reports/hashlock-H2-implementation-report.md` (create; commit it on the engrave branch) containing: per task -- the commit SHA(s) in each repo, the RED evidence, the PASS counts; every mutation re-run with its failing line; every deviation with its reason; the walk's output; the firmware size numbers (flash / RAM) against the plan's baseline; the verbatim tails of the final gate runs (the sharded `gui` package, `./hashlock/...`, `./codex32/...`, `go vet`). Return a two-line summary plus the report path and both branch tips.
