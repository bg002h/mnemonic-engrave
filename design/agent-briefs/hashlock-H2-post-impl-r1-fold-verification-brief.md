You are the INDEPENDENT fold-verification reviewer (sonnet tier, narrowly scoped) for the hashlock H2 post-implementation fold. The opus review `design/agent-reports/hashlock-H2-post-impl.md` (engrave master `cbecf57`) returned NOT GREEN 0C/2I/3M/2N at fork `17b3979`; the controller folded it in ONE fork commit `26fd1dd` (branch `hashlock-h2`, worktree `/scratch/code/shibboleth/.tmp/seedhammer-hashlock-h2`, READ ONLY) and one engrave records commit on branch `hashlock-h2` (worktree `/scratch/code/shibboleth/me-worktrees/hashlock-h2`, READ ONLY; its FOLLOWUPS and the implementation report's `## Post-implementation fold` addendum map each item).

ONE QUESTION: does `26fd1dd` fix I-1 and I-2 as stated, handle M-1/M-2/M-3/N-1/N-2 as the addendum says, and introduce no new defect -- with every claim in the fork commit message and the addendum true when YOU execute it?

Work in your OWN detached copy: `git -C /scratch/code/shibboleth/seedhammer worktree add --detach /scratch/code/shibboleth/.tmp/h2-fold-verify 26fd1dd` (remove it when done); Go is `/scratch/code/shibboleth/.toolchain/go/bin/go` first on PATH; whole-gui counts only via `/scratch/code/shibboleth/mnemonic-engrave/scripts/gui-shard-test.sh ./gui/ 24`. Commit nothing; no sub-agents; never read any `.jsonl`; never modify the two branch worktrees.

## Execute (quote outputs)
1. `git diff 17b3979..26fd1dd --stat` and read every changed line.
2. **I-1 mutations**: in `gui/composer_hashlock.go` change `s[len(s)-8:]` to `s[:8]` in `hashlockFirst8Last8` -> `TestHashlockPhraseRouteSetsTheCorpusDigest` must FAIL naming the drawn token; revert; change `len(phrase)` to `len(phrase)+1` at the `composerCopyHashlockConfirm` call -> must FAIL on `chars:`; revert.
3. **I-2 / F-481**: restore `content, _ = content.CutBottom(8)` after `_, content := screen.CutTop(leadingSize)` in `hashlockPhraseFlow` -> `TestHashlockPhraseScreenDrawsTheMaskedReadout` must FAIL with 0 asterisks; revert. Then the reviewer's caveat: text extraction cannot see clipping -- confirm GEOMETRY, not only text: on the unmutated tree, drive the phrase screen on the harness (model on that test) and assert from the layout/frame that the readout line's bottom is above the keyboard grid's top and below the counter/lead bands (read `gui/passphrase_keyboard.go` Layout for where the readout is placed and how MaxHeight/grid/readoutGap compose; compute the pixel budget as the review did: MaxHeight - grid.Y - readoutGap versus one line's height, and report the numbers before/after the fix). If the harness exposes drawn-text positions (see how `modal_fits_test.go`/`normalizeDrawn` obtain frame text and whether positions are available), use them; otherwise report the computed budget and say plainly that a raster check was not possible.
4. **N-1 mutation**: delete the `ErrHex64` arm in `composerCopyHashlockRefusal` -> `TestHashlockRefusalCopyCoversEverySentinel` must FAIL; revert. **M-2**: read the nil-`Key()` branches; confirm `seal.Deriver.Key()` returns nil after `Wipe` (`seal/pbkdf2.go`) and that the lockstep corpus test still passes (so the guard changed no live value). **N-2**: confirm `IsMS1Shaped` still refuses every corpus `refusals` row that is ms1-shaped and accepts nothing new (run `./hashlock/...`); note the host's `to_ascii_lowercase` equivalence.
5. Whole gates at `26fd1dd`: four packages; the 24 gui shards (expect 1224 tests); gofmt -l (only the three pre-existing transaction*.go); vet.
6. Records: `git -C /scratch/code/shibboleth/me-worktrees/hashlock-h2 show HEAD --stat`; F-481 header CLOSED by `26fd1dd`; F-482 table's citations true at `17b3979`; F-483 present; the addendum's numbers match yours.

## Severity
An Important not fixed, a new defect, a false claim in the commit message or addendum = Important. Wording = Minor/Nit. A clean round closes the post-impl loop and the branch merges.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H2-post-impl-r1-fold-verification.md` (create; must not exist): the executed checks with outputs, the geometry numbers, a verdict per item, closing counts and a plain GREEN / NOT GREEN. Return a two-line summary plus the path.
