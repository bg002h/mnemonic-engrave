You are the REFUTE-PASS reviewer (sonnet tier) for round 0 of the R0 gate on `design/IMPLEMENTATION_PLAN_hashlock_H2_device.md` in mnemonic-engrave (`/scratch/code/shibboleth/mnemonic-engrave`, plan at `<PLAN_SHA>`). Five lens reports have landed in `design/agent-reports/`: `hashlock-H2-plan-R0-r0-fidelity.md`, `-tests.md`, `-journey.md`, `-adversarial.md`, `-coverage.md`.

ONE QUESTION: for every Critical and Important finding in the five reports, is it TRUE of the plan at `<PLAN_SHA>` and the fork at `c4a64fc` -- reproduce it or refute it, with evidence -- and which findings are duplicates of each other?

Rules: read-only on every repo; commit nothing; no sub-agents; read no `.jsonl`. You MAY build a throwaway in your own scratch (`/scratch/code/shibboleth/.tmp/h2-refute`, created by copying `git ls-files` of the fork as the tests brief did) when a finding needs execution to settle; never touch `.tmp/h2-gate` or `.tmp/h2-tests`. Do NOT add new findings of your own beyond noting a finding whose remedy would introduce a defect. Do NOT re-grade on taste: a finding is CONFIRMED (evidence reproduces the defect as stated), PARTIAL (true but the severity or scope claimed is wrong -- say which), or REFUTED (evidence shows the plan/fork does not have the defect; quote the line that settles it). Reviewers prescribe fixes; treat every SUGGESTION as a hypothesis, not a remedy -- note when a suggestion would itself be wrong.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H2-plan-R0-r0-refute.md` (create; must not exist): one row per C/I finding (report, id, title, verdict, evidence, duplicate-of), then the deduplicated list of CONFIRMED + PARTIAL findings the fold must address, with the one-line remedy each reviewer proposed and your note where the remedy is unsound; closing counts (confirmed C / I, refuted, duplicates). Return a two-line summary plus the path.
