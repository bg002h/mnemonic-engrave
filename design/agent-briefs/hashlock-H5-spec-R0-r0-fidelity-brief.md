You are the INDEPENDENT fidelity + design reviewer (opus tier) for round 0 of the R0 gate on `design/SPEC_hashlock_H5_device_polish.md` (engrave master `f6dd437`): five small device changes (F-487 reconcile screen repeats the digest; F-480 per-digest hash provenance; F-484 lead inside the page band; F-485 walk proves hold order and stored-vs-displayed; F-488 refusal names the next step) over fork main `b9a9a30`, parent spec `design/SPEC_hashlock_H2_device.md` (GREEN, H3-folded), rulings in `design/FOLLOWUPS.md` F-487/F-480/F-484/F-485/F-488.

ONE QUESTION: implemented literally, does each section fix exactly the follow-up it names without contradicting the H2 spec or the fork, and is every claim the spec makes about the fork true?

Read-only on every repo (engrave `/scratch/code/shibboleth/mnemonic-engrave`; fork `/scratch/code/shibboleth/seedhammer` main `b9a9a30` -- for execution use your OWN detached worktree `git -C /scratch/code/shibboleth/seedhammer worktree add --detach /scratch/code/shibboleth/.tmp/h5-lens-<key> b9a9a30`, Go `/scratch/code/shibboleth/.toolchain/go/bin/go` first on PATH, remove it when done); commit nothing; no sub-agents; never read any `.jsonl`; secret-handling never gates. Construct counterexamples with commands and quoted output; an assessment without one is Minor at most. Severity: Critical = a normative sentence that, implemented literally, loses operator work, assigns a hash before HOLD, shows a digest that differs from the stored one, or makes a gate unable to fail; Important = a false claim about the fork, a fit or geometry number that does not hold, a missing case, an unsound design step; Minor/Nit = wording.

Verify, per section: §1 the fit arithmetic (`modalBodyMargin = 80` at `gui/modal_fits_test.go:51`; the confirm body's current headroom -- run `assertModalBodyFits` on the longest legal body yourself; whether the fit gate covers `showError` bodies and which renderer measures them); the exact `hashlockMethod.String()` values; whether repeating the digest on an error-style screen has any z-order or width issue at sh2 size. §2 construct a composition where per-digest provenance answers differently from the current flag and decide which is right (e.g. a phrase digest later chosen from a payload row on another path; two paths one digest; the digest re-typed as hex); is deleting `composerHashByPhraseSync` safe at every call site. §3 compute the band width at sh2 size (`assets.NavBtnPrimary` width) and the lead's line count at that width with `ctx.Styles.lead` -- does the two-line rule hold, or does §3.3's fallback copy fire; does the readout budget survive. §4 is the emulator hook implementable in `cmd/emu` without touching production code paths (how does the js build reach the composer state; how does `shTargets` expose labels); can the pre-hold read see the state while the confirm modal is up. §5 the longer sentence's fit at the longest noun and a two-digit index.

Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H5-spec-R0-r0-fidelity.md` (create; must not exist): findings `### C-n / I-n / M-n / N-n -- title` with the spec section, the counterexample or measurement, a SUGGESTION; closing counts. Return a two-line summary plus the path.
