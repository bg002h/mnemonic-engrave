You are the FOLD author (opus tier) for round 0 of the R0 gate on `design/IMPLEMENTATION_PLAN_hashlock_H5_device_polish.md` (engrave master `0c2b13e`, working tree) and its spec `design/SPEC_hashlock_H5_device_polish.md` (`e03d8e7`), in mnemonic-engrave (`/scratch/code/shibboleth/mnemonic-engrave`). Three lens reports are persisted in `design/agent-reports/`: `hashlock-H5-plan-R0-r0-fidelity.md` (0C/3I/4M/4N), `-tests.md` (0C/0I/1M), `-journey.md` (0C/2I/5M/2N). Read all three in full first.

THE MECHANISM (as for the H2 plan): the plan's code blocks are the SOURCE and `/scratch/code/shibboleth/.tmp/h5-gate` (fork `b9a9a30` + every task, wired by the plan author) is the TREE. Every code change you make in the plan you make identically in the tree; every test you change or add you run RED (mutated/absent) then GREEN and quote both; every `MUTATION:` you touch you re-run; at the end run `go test ./hashlock/... ./codex32/... ./seal/... ./sysw/... ./cmd/emu/...`, the whole gui via `/scratch/code/shibboleth/mnemonic-engrave/scripts/gui-shard-test.sh ./gui/ 24`, `GOOS=js GOARCH=wasm go vet ./cmd/emu/`, `./cmd/emu/build.sh`, and `./scripts/h5-plan-blocks-vs-tree.sh` from the engrave checkout -- every block PASS or you are not done. Go `/scratch/code/shibboleth/.toolchain/go/bin/go` first on PATH; nix for the size recipe if a change could move firmware size (the doc-comment fix and the walk do not; re-measure if you touch a non-test Go file compiled into the firmware).

FOLD, with the measured repairs the reports supply (reviewers prescribe, you verify):
1. fidelity I-1 (= journey I-7 unmet): the walk's stored-versus-displayed assertion must compare the STORED digest's `first8..last8` against the token PARSED FROM THE CONFIRM-MODAL TEXT the walk captured (never a file constant), and run before the corpus check; relabel Step 12's table so run (c) is shown failing THAT assertion; journey M-1: Step 12 restores `gui/composer_hashlock.go` (`git checkout -- ` + `git diff --quiet`) before each mutation and after run (c), and re-runs `./cmd/emu/build.sh` after the final restore.
2. fidelity I-2: the `ok`-guard in `cmd/emu/needle_test.go` checks EVERY `.ok =` assignment (`FindAllStringSubmatch`), with a test row for a walk whose last assignment is `out.ok = out.plates === 3` (must FAIL the guard); journey N-1: the log line says only what is checked.
3. fidelity I-3: restore the blank line between the inserted comment blocks and the doc comments of `composerFlow` (`gui/composer_flow.go`) and `composerPageLines` (`gui/composer_paged.go`), prove with `go doc ./gui composerFlow` and `go doc ./gui composerPageLines` printing the original comments; add a test or a checker line that would catch a stolen doc comment on these two symbols if cheap (`go doc` output non-empty), else record why not.
4. journey I-1 (operator agreed): the confirm modal's second sentence becomes *"The phrase and method are not on this device. Without both, this path can never be spent."*; the first stays *"Write down this phrase, the method and this digest now."* byte-identical; measured 343/107 -- re-measure; update the copy-table rows, the fit row, the tests' needles, the H2 §4.5 fold text in Task 6, and the H5 spec §1.2.
5. journey I-2 (operator agreed): §8h's PLAIN form ends *"Back up every preimage separately."* (measured 133/397 -- re-measure) with a test row and mutation beside the phrase-form test; H5 spec §2.5 gains the plain form; Task 6's H2 §4.7 fold text covers both forms.
6. journey M-2: the reconcile body's sentence opens *"Before you cut plates, run ms hashlock ..."* (measured 181/339 -- re-measure); keep the substring `run ms hashlock with this phrase` verbatim; spec §1.1 and every test needle follow.
7. journey M-3: the refusal sentence becomes *"Remove that record -- and any others like it -- (records count from 0) on the host and seal the payload again."* only if it fits with >= 80 headroom (re-measure; the report says 397 headroom before); otherwise record documentation-only.
8. journey M-4: record the decision in a comment beside the "every ... and every" copy (overcount is the safe direction); M-5: Task 6's manual edit gains the Back-table row for the reconcile screen and the qualified lead sentence; N-2: one clause in the manual about `phrase: 28 characters` on the host's stderr card.
9. fidelity M-1..M-4 and N-1..N-4 (read them; fold the cheap truth fixes; fidelity M-4 = a one-line erratum in H5 spec §5: no copy-table row exists for the unlock body -- the fit row is the gate); tests M-1: complete the two incomplete RED transcriptions from your own run.
10. Every changed number in plan or spec is YOUR measurement of the changed text (a number quoted from a report is only as good as the body it measured -- the H5 spec's r1 lesson).

RECORDS: add `## R0 round 0 folded here` to the plan (finding -> change, declines with reasons) and update its STATUS to `DRAFT -- R0 round 0 folded; r1 fold verification pending`; add `## Plan-round fold` to the spec listing the copy changes with their measurements, STATUS unchanged (R0 GREEN) but noting the fold. Edit only the plan, the spec, the tree and (if needed) `scripts/h5-plan-blocks-vs-tree.sh`; never the reports or the fork checkout; COMMIT NOTHING; no sub-agents; never read any `.jsonl`; re-grep every NEW file:line you add.

Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H5-plan-R0-r0-fold-report.md` (create; must not exist): per item the change, RED/GREEN evidence, measurements; the gate tails; the checker output; declines with reasons. Return a two-line summary plus the path.
