You are the INDEPENDENT journey-walk reviewer (opus tier) for round 0 of the R0 gate on `design/IMPLEMENTATION_PLAN_hashlock_H2_device.md` in mnemonic-engrave (`/scratch/code/shibboleth/mnemonic-engrave`, plan at `<PLAN_SHA>`). Spec: `design/SPEC_hashlock_H2_device.md` (GREEN `55ee7a4`). Fork: `/scratch/code/shibboleth/seedhammer` main `c4a64fc` (read-only). Host reference: mnemonic-secret `cd0a60f` (`ms hashlock`, `crates/ms-cli/src/hashlock_phrase.rs`, `argv_guard.rs:148-164`, `format.rs`), corpus `crates/ms-codec/tests/vectors/hashlock-v0.8.json`.

ONE QUESTION: walk the operator's journeys through the device the PLAN builds (not the spec) step by step and find the moments where the plan is SILENT -- a screen, a state, a Back edge, a copy line or a hardware limit the operator meets and the plan's code does not answer.

Method (the constellation's standing "walk the journey" rule): at every step ask (1) what does the operator have in hand, exactly; (2) what does the device (as the plan codes it) do; (3) what ELSE might they reasonably do, and what happens then. Classify every divergence: refusal / warning / default / not our concern / documentation only. A divergence earns a finding ONLY if the wrong outcome is worse than telling the operator nothing.

Journeys to walk (trace the plan's Task 3/4 code for each, with the fork's `passphrase_keyboard.go`, `composer_shape.go`, `composer_copy.go`, `composer_hash.go`, `modal_fits_test.go` open):
1. **The happy path with a real wallet**: `ms hashlock` on the host produced `sha256` of `correct horse battery staple`; the operator builds the same policy on the SH2, reaches `Which hash?`, taps `Type a hashlock phrase`, types it on the passphrase keyboard, picks `SHA-256`, holds to confirm, finishes the policy, engraves. Which keyboard page holds the space bar and the letters; can EVERY character the host accepts (`0x20..=0x7E`) be typed (which of `-`, `,`, `"`, backslash, `~` can the keyboard produce; what happens for one it cannot -- is that a host/device divergence the plan must document?); the counter `28/100`; the digest shown vs the host's `b867db87..edbc96cb` (its display form on the host: `ms hashlock` prints what, exactly?); the "matches hash N" line; the drop order at the longest phrase.
2. **The hardened method**: the same phrase, `PBKDF2 100k`; what the operator sees during ~30 s (countdown copy, can they Back out -- and if they do, what state is the phrase in); the second `Deriving` on a second path; a phrase re-entered after Back at the confirm.
3. **Mistakes**: a phrase with a trailing space (the host TRIMS? read the spec §2 and `hashlock_phrase.rs`); a 101-character phrase (what does the keyboard do at the cap -- refuse the 101st key, or accept and refuse at OK?); a phrase that is 64 hex characters; a phrase that is ms1-shaped; an EMPTY phrase then OK; typing the phrase with different case than the host (refused? accepted with a different digest -- is the operator told the digest differs from the host's?); picking the wrong method.
4. **The two-hash payload**: a payload with two hashlock digests, the operator types a phrase for path 1 that matches hash 2 -- the relation line; then edits path 1's hash back to `No hash lock` -- `hashByPhrase` and the §4.7 every-path copy; then `Back` at `Which hash?` on a NEW path (deleted, per `composerAddPath`) vs on an EXISTING path.
5. **Power and memory**: derivation with `-stack-size 16kb` and `-gc precise` -- does `seal.NewDeriver` allocate per step; the phrase bytes' lifetime (`Wipe`) on every exit; the confirm modal's `assertModalBodyFits` rows for the longest body.
6. **The reader side**: an engraved preimage plate from the host, presented to the device's typed and NFC doors (H0's guards) -- what the operator sees, and whether H2 changes any of it.

Read-only; commit nothing; no sub-agents; read no `.jsonl`; no scratch copies (you may READ `/scratch/code/shibboleth/.tmp/h2-gate` for the wired tree; never modify it).

## Already settled -- do not re-derive
The gate proved the tree compiles and the plan's tests pass (`design/agent-reports/hashlock-H2-plan-build-gate.md`). The spec's rulings (L5, L7, L12, L15, L16, L22, L24) stand; walk the PLAN. Secret-handling never gates.

## Severity
Critical: a moment where the plan's device derives a digest the host would not for the same intent, or loses the operator's work (a path deleted, a phrase dropped) where the spec says intact. Important: a silent step whose wrong outcome is worse than nothing (an unreachable character, an unanswered Back, an unexplained divergence). Minor/Nit: documentation-only divergences.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H2-plan-R0-r0-journey.md` (create; must not exist): per journey a step table (step / in hand / device does / what else / classification), then findings `### C-n / I-n / M-n / N-n -- title` with the plan section and a SUGGESTION; closing counts. Return a two-line summary plus the path.
