You are the ONE implementer for follow-up F-503 (the preimage KIND under the id `hash` is inert at every length). Your COMPLETE instruction set is `design/IMPLEMENTATION_PLAN_hashlock_F503_hash_kind_inert.md` in mnemonic-engrave (`/scratch/code/shibboleth/mnemonic-engrave`, master at the SHA the controller gives you; STATUS GATED, R0 skipped by the operator). Execute Tasks 1, 2, 3 IN ORDER (Rust first, then the fork as convergence, then records), exactly as written, test-first: quote every RED, run every `MUTATION` once and revert it, and take the plan's boundary numbers as the pass criteria. The plan's code blocks are verbatim from two READ-ONLY gate worktrees (`/scratch/code/shibboleth/me-worktrees/f503-gate`, `/scratch/code/shibboleth/.tmp/seedhammer-f503-gate`) -- consult them to resolve context, never copy files wholesale from them except the seam corpus (Task 2 Step 1 copies Task 1's file; it must be byte-identical, sha256 `f53a17dc9d1ea5a4c0ff913786e82a5f19de004f4719536bb6d957bccdef1ee8`).

Branches and worktrees, all NEW: me `git -C /scratch/code/shibboleth/mnemonic-engrave worktree add -b f503 /scratch/code/shibboleth/me-worktrees/f503 master`; fork `git -C /scratch/code/shibboleth/seedhammer worktree add -b f503 /scratch/code/shibboleth/.tmp/seedhammer-f503 main`; ms `git -C /scratch/code/shibboleth/mnemonic-secret worktree add -b f503-records /scratch/code/shibboleth/ms-worktrees/f503-records master`. Toolchains and env as the plan's Global constraints. Touch ONLY the files the plan's File structure lists. Commit per task with the plan's messages, `git commit -F <file>` (`-s` on the fork), trailers `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01Fs3bg7TRfuSaFcCEkskwXA`; stage paths explicitly; nothing pushed; no master/main commits; no sub-agents; never read any `.jsonl`. One hazard the plan names: `IsPreimage` and `IsPreimageKind` end in textually identical lines -- edit by function and `git diff` before every restore.

Every count in your report comes from a run at your tip, captured under `/scratch/code/shibboleth/.tmp/` and quoted. FINAL ACTION: write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/f503-implementation-report.md` (create; must not exist) with per-task SHAs and branch tips, RED quotes, boundary numbers, every mutation with its failing line, every deviation; return a two-line summary plus the path and the three tips.
