You are the INDEPENDENT fold-verification reviewer (targeted) for the composer Stage 4 implementation's review fold. The review is `design/agent-reports/composer-S4-exec-review-r0.md` (opus, 0C/1I/4M/4N); the controller's decisions are `design/agent-briefs/composer-S4-fold-r0-brief.md`; the implementer's fold report is `design/agent-reports/composer-S4-fold-r0-report.md` -- re-derive, do not trust. The folds are fork branch `composer-s4-emu` (worktree `/scratch/code/shibboleth/wt-composer-s4-emu`, reviewed tip `86cec95`, folded tip `a6eb44e794c3ee7bd6484d0125fc51a256401706`) and mnemonic-engrave branch `composer-s4-emu` (worktree `/scratch/code/shibboleth/wt-engrave-s4-emu`, reviewed tip `c6adac2`, folded tip `651fa0ea019ec25fe1a878e89623903cf1bb4e6c`). One question: did each of I-1, M-1, M-2, M-3, M-4, N-1, N-3, N-4 land as decided, can the new guards fail, and did the fold introduce nothing?

Read-only: `cp -r` copies for every mutation (never dirty the worktrees; no `git checkout` in a copied worktree); Go `/scratch/code/shibboleth/.toolchain/go/bin` on PATH, `CGO_ENABLED=0 GOPROXY=off GOTOOLCHAIN=local`, `-mod=readonly`, `TMPDIR=/scratch/code/shibboleth/.tmp`; Playwright is installed; run the captures against YOUR copy of the fork worktree (`EMU`/`--emu`, ports other than 8793/8734/8797/8738/8803/8744 if any is busy). Do NOT spawn sub-agents; commit nothing; read no `.jsonl` file; record every exit code directly, never through a pipe.

## Verify
1. `git diff 86cec95..a6eb44e794c3ee7bd6484d0125fc51a256401706 --stat` and `git diff c6adac2..651fa0ea019ec25fe1a878e89623903cf1bb4e6c --stat`: only the files the fold report names; no oracle value, no itinerary row, no `shots_composer.js` comparison logic changed beyond M-1/M-2/N-1.
2. I-1 both ways, yourself: corrupt `payload.digest.txt` (`dbe9`->`dbe0`) in a copy of `out/composer/` -> `--prove-it-can-fail` exits non-zero with INCONCLUSIVE in seconds; the honest run exits 0 with PASSED after the full itinerary and its text names the corrupted address. Then a THIRD arm the report did not run: corrupt the address in `out/composer/keyed.receive.txt` yourself (not via the flag) and run `--arm keyed` plainly: it must fail naming that address.
3. M-3: empty `keyed.receive.txt` in a copy -> `read_keyed()` (or the capture) exits non-zero before any walk. M-2: on the keyed arm the `QR ONLY` absence is asserted (mutate the assertion to see it can fail, or drive the screen text); on the keyless arm the taken row is `TEXT + QR` by label. M-1: the passphrase `Skip` post-condition is `(any slots)`. M-4: ports `8803/8744` in the code AND the usage line. N-1: the guard precedes the call.
4. N-4: `go test -count=1 -run 'Needle' ./cmd/emu/` passes on the tip and FAILS in a copy with a second `"Build a new policy"` literal in another `gui/*.go` file.
5. N-3: `transcript_composer.txt`'s rev line equals `a6eb44e794c3ee7bd6484d0125fc51a256401706` (short) and the ids/addresses/strings equal the reviewed transcript's.
6. Gates: fork `gofmt -l cmd/`, `go test -count=1 ./cmd/emu/`, `GOOS=js GOARCH=wasm go vet ./cmd/emu/`; engrave `--arm both` exit 0, the unchunked-string mutation non-zero (56 vs 47), the three shipped drivers exit 0.

Severity: a finding not folded as decided, a guard that cannot fail, or a hunk outside the decisions = Important; wording = Minor/Nit. Do not pad.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/composer-S4-fold-r0-verification.md` (create; must not exist): per finding VERIFIED / NOT VERIFIED with output; the extra I-1 arm; closing counts. Return a two-line summary plus the path.
