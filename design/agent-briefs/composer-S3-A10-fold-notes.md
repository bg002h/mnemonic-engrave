# S3 task A10 fold (apply after S3's r1 verification lands; from S0b's fidelity lens I-1/I-2/I-4)

Source: `design/agent-reports/composer-S0b-plan-R0-r0-fidelity.md` and the controller's decisions in `fold-brief-s0b-r0.md`.

1. **Defaults (I-1).** A10's Go preset table takes its default shapes VERBATIM from the S0b vectors (already A10's rule: "Do not invent a shape here"); add the sentence that S0b decided these are the device's offered defaults too (the smallest legal shape of each archetype is the honest starter; a wider tier is one edit away), so A10 no longer reads as inheriting an accident.
2. **Wrapper coverage (I-2).** A10 currently offers all six presets under BOTH `wsh` and `tr` and its vector test iterates `composerPresets(md.ComposeWsh)` expecting six wsh vectors. S0b exports ONE vector per archetype (five under wsh, `kofn-recovery` under tr). Narrow `TestComposerPresetsReproduceTheirVendoredVectors` to "each preset that HAS a vector at this wrapper, with the wrapper named in the assertion", and keep the rule "the VECTOR wins" only for pairs that have a vector; a preset with no vector at a wrapper is checked structurally (lowering through `md.Compose` succeeds; `ValidatePathList` accepts) and named as unpinned. Do not let a `tr` vector's chunks be compared against a `wsh` preset.
3. **Fork pin-test ownership (I-4).** A10 owns: re-running `scripts/vendor-compose-vectors.sh` after S0b ships (126 → 156 files), extending `composeVectorNames` in `md/compose_vectors_pin_test.go` by the six `keyed_compose_preset_*` names (26 → 32) and the `126` count to `156`; A10 currently asserts no such edit is needed — correct that. The vendored file names are `keyed_compose_preset_<archetype>` (snake_case archetype), not `preset_<name>`.
4. **Precondition.** A10 stays BLOCKED until S0b ships on descriptor-mnemonic main; state the S0b plan path and that the S0b tag/commit is recorded in A10 when it lands.
