You are the INDEPENDENT tests/mutation reviewer (sonnet tier) for round 0 of the R0 gate on `design/IMPLEMENTATION_PLAN_hashlock_H6_preimage_plates.md` (engrave master `e6d84d9c`), spec `a2a031fa`.

ONE QUESTION: can every test the plan adds actually FAIL on the defect it names, does every RED and every declared MUTATION reproduce when YOU run it from the plan'"'"'s text in your own copies of the gated trees, and which mutations of the new guards survive (the `hash`-id checks on both sides; the never-`entr` encoder test; the admission gate for `--pack-preimage`; the budget check; the scale-2 arm; the census exclusion; the lazy derive; the free-text warning)?

Already settled: the spec is R0 GREEN (`a2a031fa`); the plan author ran a build gate at every task boundary and the blocks-vs-tree checker passes (its report `design/agent-reports/hashlock-H6-plan-author-report.md`); do not report compile findings. The operator's decisions and rulings (continuity entries "H6 BRAINSTORM", "Group A rulings", "C-1 RULING", "C-1 scope corrected") are settled; review the PLAN against the SPEC and the repos at the plan's stated baselines. Secret-handling never gates. Read-only on every repo; the plan author's gated trees are read-only -- copy them (`cp -a`) for execution into your own `/scratch/code/shibboleth/.tmp/h6-<key>-*` and remove them when done; Go `/scratch/code/shibboleth/.toolchain/go/bin/go`; Cargo env `PATH=$HOME/.cargo/bin:$PATH TMPDIR=/scratch/code/shibboleth/.tmp CARGO_TARGET_DIR=/scratch/code/shibboleth/.tmp/<your own dir>`. Commit nothing; no sub-agents; never read any `.jsonl`. Construct counterexamples with commands and quoted output; an assessment without one is Minor at most.

Also: false-PASS hunting (does any test recompute its expectation with the code under test; do the QR goldens compare module counts or images; does the constant-time test compare command sequences across payloads or only lengths); run the whole gui shard set and `cargo nextest` once at the final trees and quote the tails.

Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H6-plan-R0-r0-tests.md` (create; must not exist): RED quotes; a mutation table (mutation, caught-by / SURVIVED, quoted assertion); false-PASS answers; closing counts. Return a two-line summary plus the path.
