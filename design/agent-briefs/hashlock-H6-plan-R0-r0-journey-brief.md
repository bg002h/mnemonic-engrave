You are the INDEPENDENT journey-walk reviewer (opus tier) for round 0 of the R0 gate on `design/IMPLEMENTATION_PLAN_hashlock_H6_preimage_plates.md` (engrave master `e6d84d9c`), spec `a2a031fa`.

ONE QUESTION: walk the operator through the device and host the PLAN builds (drive the gated fork tree on the touch harness where you can; run the host binaries from the gated engrave/ms trees) through every journey the spec defines -- device-derived phrase to a cut plate (both forms, QR on/off); host-derived via `ms hashlock` and `me sysw pack --pack-preimage` with and without the no-seal flag; a payload phrase record with its lazy derive; the mistakes (wrong method, unmatched preimage, two hashlocks, declined plate, abort windows, the collision string, an ms1 string in free text, a phrase record at the Password program) -- asking at each step what they have in hand, what the device or host does, and what ELSE they might do. A divergence is a finding only if the wrong outcome is worse than telling the operator nothing. Also walk the CONTROLLER'"'"'s emulator runs and the QR scan gate as the plan describes them.

Already settled: the spec is R0 GREEN (`a2a031fa`); the plan author ran a build gate at every task boundary and the blocks-vs-tree checker passes (its report `design/agent-reports/hashlock-H6-plan-author-report.md`); do not report compile findings. The operator's decisions and rulings (continuity entries "H6 BRAINSTORM", "Group A rulings", "C-1 RULING", "C-1 scope corrected") are settled; review the PLAN against the SPEC and the repos at the plan's stated baselines. Secret-handling never gates. Read-only on every repo; the plan author's gated trees are read-only -- copy them (`cp -a`) for execution into your own `/scratch/code/shibboleth/.tmp/h6-<key>-*` and remove them when done; Go `/scratch/code/shibboleth/.toolchain/go/bin/go`; Cargo env `PATH=$HOME/.cargo/bin:$PATH TMPDIR=/scratch/code/shibboleth/.tmp CARGO_TARGET_DIR=/scratch/code/shibboleth/.tmp/<your own dir>`. Commit nothing; no sub-agents; never read any `.jsonl`. Construct counterexamples with commands and quoted output; an assessment without one is Minor at most.

Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H6-plan-R0-r0-journey.md` (create; must not exist): per journey a step table; findings `### C-n / I-n / M-n / N-n -- title` with SUGGESTION; closing counts. Return a two-line summary plus the path.
