You are the FOLD author (opus tier) for the build-gate round on `design/IMPLEMENTATION_PLAN_hashlock_H2_device.md` in mnemonic-engrave (`/scratch/code/shibboleth/mnemonic-engrave`, working tree; the plan is at `38509a9` plus nothing since). The gate agent hand-wired every code block of Tasks 1-4 into a scratch copy of the fork at `/scratch/code/shibboleth/.tmp/h2-gate` (fork main `c4a64fc` + the plan), needed 12 fixes and logged 2 prose corrections, and its tree is GREEN (the whole `gui` package, 1213 tests; `codex32`/`sysw`/`seal`/`hashlock`; firmware 1,595,236 / 62,856). Its report is `design/agent-reports/hashlock-H2-plan-build-gate.md` -- read it first, in full.

ONE JOB: make the plan describe the GATED tree, byte for byte, so an implementer following the plan literally produces exactly what the gate proved green -- and prove it with a script.

Do:
1. For every file the plan CREATES (the `hashlock` package files, `gui/composer_hashlock_test.go`, the vendored corpus reference, `cmd/emu/walk_hashlock_phrase.js` if the plan carries it as code), replace the plan's code block with the scratch tree's file content verbatim.
2. For every file the plan MODIFIES via fragments (`gui/composer_copy.go`, `gui/composer_copy_test.go`, `gui/modal_fits_test.go`, `gui/composer_hash.go`, `gui/composer_shape.go`, `codex32/*.go`, others the report names), update each fragment so it is a verbatim substring of the scratch tree's file at the anchor the plan names; where the gate added something the plan had no step for (fix #1's table row and count, fix #2's ten rows and the 42->51 literal, fix #12's `composer_gates_test.go` pump target), ADD the step with the exact text and the reason, in the task the change belongs to (fix #12 belongs to Task 3, as a consequence of the no-payload lead swap; say so).
3. Fixes #3 and #4 change the confirm modal's copy per spec §4.5's drop order: update Task 4's copy step, the confirm body, and any prose that still quotes the unshortened text or places the reconciliation line in the confirm modal; add the measured headroom numbers (64 before, 186 after) as the reason. Do NOT edit the spec.
4. Fix #11: replace the plan's `holdConfirm` description with the gate's implementation and a MUTATION-style comment recording the `EventRouter` single-global-pointer mechanism (`gui/event.go`: `pointer.pressed`/`pressedTag`) so a future reader knows why the explicit release exists. Fixes #5-#10: fold each exactly as the report describes (display size, the `Wrapper: md.ComposeWsh` state, the `What can spend on this path?` choice, the `32-byte value` needle, the `Type a hashlock phrase` needle, the removed stray Button3).
5. The two prose corrections in the Task 1 Step 5 mutation table (separator-strip scope and the cap literal) -- fold the corrected claims; quote the report's measurement.
6. Task 5 Step 2's expected firmware numbers: 1,595,236 / 62,856 measured by the gate at the wired tree (baseline 1,583,132 / 62,800); the plan's acceptance line should state the measured delta and the budget rule it applies.
7. Add a paragraph `## Build gate folded here` at the end (before Self-review) listing the 12 fixes by number with one line each and the two prose corrections, and citing the report. Update the plan's STATUS line to `DRAFT -- build gate GREEN WITH FIXES folded; R0 round 0 pending`.
8. Write `scripts/h2-plan-blocks-vs-tree.sh`: extracts every fenced ```go / ```js / ```json block that carries a file path header from the plan, and for each: whole-file blocks are `diff`ed against `/scratch/code/shibboleth/.tmp/h2-gate/<path>`; fragment blocks are checked as a verbatim substring (`grep -F -x` line-wise or a python substring test) of that file. It prints PASS/FAIL per block and exits non-zero on any FAIL, and prints what it does NOT cover (blocks without a path header; prose claims). Make the plan's block headers carry the path in a form the script parses (state the convention at the top of the plan's File Structure section). Run it; it must PASS on every block. Paste its output tail into the fold paragraph.

Rules: edit ONLY the plan and the new script; do not touch `.tmp/h2-gate`, the spec, the gate report or any other file; COMMIT NOTHING (the controller commits persist-then-fold; engrave master is frozen for a release push right now); no sub-agents; never read any `.jsonl`; re-grep every NEW file:line citation you add against the scratch tree and the fork at `c4a64fc` (a fold adds citations that were never gated).

Report (your final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H2-plan-gate-fold-report.md` (create; must not exist): per fix, what changed in the plan (section, before/after in one line each); the script's full output; anything in the report you could NOT fold and why. Return a two-line summary plus the path.
