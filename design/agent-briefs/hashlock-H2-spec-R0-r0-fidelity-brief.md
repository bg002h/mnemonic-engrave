You are the INDEPENDENT fidelity + design reviewer (opus tier) for round 0 of the R0 gate on `design/SPEC_hashlock_H2_device.md` in mnemonic-engrave (`/scratch/code/shibboleth/mnemonic-engrave`, spec committed at `bfd042e`). Its sources: `design/BRAINSTORM_hashlock_phrase.md` (§2 rulings L1-L23, §3.4, §3.7, §4.4, §4.6, §7.1 the r2 security review), `design/SPEC_wallet_policy_composer.md` §6c/§8h/§8i, `mnemonic-secret/design/SPEC_ms_hashlock.md` §4.3 and §7 (in `/scratch/code/shibboleth/mnemonic-secret/design/`), and the fork at `/scratch/code/shibboleth/seedhammer` main `c4a64fc` (read-only).

ONE QUESTION: is every mechanism the spec names real at `c4a64fc` and every ruling carried faithfully — and if the fork were built exactly to this spec, would the device's digest equal the host's for the same phrase and method, with no state in which a preimage is stored, shown, engraved, or a path's hash silently changed?

Read-only everywhere; commit nothing; no sub-agents; no scratch copies (a second lens runs concurrently); read no `.jsonl`.

## Already settled — do not re-derive
- The citation table (§10) was re-grepped by the controller at `c4a64fc`/`cd0a60f`: every cited line exists as described. Do not report line numbers; DO report a mechanism that does not do what the spec says it does.
- H0 (merged, fork `c4a64fc`) already made a `0x03` single inert on every reader and door; H1 (`ms hashlock`) is released; ms-codec 0.8.0 is on crates.io. The corpus sha `a46c197a…1d30` is measured.
- Rulings L5, L7, L12, L15, L16, L22, L24 stand. Secret-handling never gates.

## Verify (construct counterexamples; an assessment without one is Minor at most)
1. **The digest lockstep.** Trace how the spec's §3 port would be called from §4.4 with the 14-byte salt; then read `gui/unlock_kdf.go` (`unlockDerive`, the countdown driver) and `seal/pbkdf2.go`: is there any existing helper whose signature invites the `Salt [16]byte` zero-pad (r2 M-5), and does the spec forbid it clearly enough that an implementer following §4.4 cannot reach for it? Is the Go PBKDF2 in `seal/pbkdf2.go` HMAC-SHA256 with the same iteration semantics as Rust's `pbkdf2` crate (dkLen 32 = one block: no block-index subtlety)? Is SHA-256(phrase) of the raw bytes, not of any normalised form?
2. **The phrase rule.** §2 versus SPEC_ms_hashlock §4.3 clause by clause: order of checks, the ms1-shape detection (the host's `looks_like_ms1` trims and case-folds INSIDE the predicate — does the spec's §2 rule 3 match it exactly, including what "trimmed" means for a phrase with leading spaces that is NOT ms1-shaped?), the 64-hex rule either case, the cap. A phrase the host accepts and the device refuses (or vice versa) is Critical: the operator would derive two digests.
3. **The state machine.** Walk `Which hash?` → phrase → method → (modal) → derive → confirm → CONTINUE/Back at every step, against `composerHashEdit`'s current structure (`gui/composer_hash.go:140-172`) and `composerPickScreen`: at which step is `st.list.Paths[idx].Hash` written? Can Back after derivation leave a stale hash? Can the countdown's Back (`unlock_kdf.go`) be reached in a state that assigns? Does §5's label-keyed switch have a row the spec does not enumerate (the composer's `Which hash?` today has payload rows + `Type 64 hex` + `No hash lock`; the paged pick screen's `composerPickScreenMaxRows` — does adding a row overflow a page, and what happens then)?
4. **Copy and geometry.** Every string in §4 must fit the composer's modal-fits gate (`gui/composer_copy_test.go`, the glyph and modal-fits assertions) — measure the longest lines against the width the gate enforces; a line that cannot fit is Important. Is the F-132 line the spec cites (§4.5) a real string in the fork (`composer_copy.go`), and under what condition does §8h fire today?
5. **Scope and records.** Does the spec's §1/§9 match the brainstorm's stage split (H2 fork port; H3 records; H4 device walk) and the composer spec's §14 ("never derives, stores or engraves a preimage this cycle" — H2 DERIVES: is the composer spec's sentence now false, and does this spec say which record to fold)? Is `DecodeMS1Preimage` (§6) justified with no caller, or is it scope creep that adds a decoder nothing tests end to end?

## Severity
Critical: a host/device digest divergence path; a state in which a preimage is stored/shown/engraved or a hash silently changes; a mechanism the spec relies on that does not exist. Important: a missing state, an unfitting string, an unsound assumption, a stale record the spec should fold. Minor/Nit: wording.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H2-spec-R0-r0-fidelity.md` (create; must not exist): findings `### C-n / I-n / M-n / N-n — title` with the spec section, the counterexample or trace, and a SUGGESTION; the state-machine walk as a table (step, what is written, Back does); closing counts. Return a two-line summary plus the path.
