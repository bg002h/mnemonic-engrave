You are the INDEPENDENT tests/mutation reviewer (sonnet tier) for round 0 of the R0 gate on `design/IMPLEMENTATION_PLAN_hashlock_H5_device_polish.md` (engrave master `<PLAN_SHA>`), spec `design/SPEC_hashlock_H5_device_polish.md` (`<SPEC_SHA>`).

ONE QUESTION: can every test the plan adds actually FAIL on the defect it names, does every RED and every declared MUTATION reproduce when YOU run it from the plan's text, and which mutations of the new guards survive?

Read-only on every repo (engrave `/scratch/code/shibboleth/mnemonic-engrave`; fork `/scratch/code/shibboleth/seedhammer` main `b9a9a30`); the plan author left the GATED tree at `/scratch/code/shibboleth/.tmp/h5-gate` (fork b9a9a30 + every task applied; read it, never modify it); for execution use your OWN copy `rm -rf /scratch/code/shibboleth/.tmp/h5-<key> && mkdir -p /scratch/code/shibboleth/.tmp/h5-<key> && cp -a /scratch/code/shibboleth/.tmp/h5-gate/. /scratch/code/shibboleth/.tmp/h5-<key>/` (Go `/scratch/code/shibboleth/.toolchain/go/bin/go` first on PATH; remove it when done); commit nothing; no sub-agents; never read any `.jsonl`; secret-handling never gates. Already settled: the plan author ran the build gate at every task boundary and the blocks-vs-tree checker passes (its report `design/agent-reports/hashlock-H5-plan-author-report.md`); do not report compile findings. Construct counterexamples with commands and quoted output; an assessment without one is Minor at most.

Do, in your own copy: (1) every RED step -- revert the task's implementation blocks and run the named test, quote the failure; (2) every declared `MUTATION:` -- apply, quote, revert; (3) your own mutations, at least: the nil-map helper without the nil check (must panic in the zero-value-state test); `composerAnyPathByPhrase` returning `len(set) > 0`; the reconcile body without `chars`; the write-down line reverted; the banner with "or"; the refusal without "(records count from 0)"; the lead back to `dims.X-2*8`; the hook never registered by `composerFlow` (what does the walk see); (4) false-PASS hunting: does any test recompute its expectation with the code under test; does the geometry test measure ink or only layout arithmetic; do the fit rows drive the renderer production uses; (5) run the whole gui shard set once at the final tree and quote the tail.

Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H5-plan-R0-r0-tests.md` (create; must not exist): RED quotes; a mutation table (mutation, caught-by / SURVIVED, quoted assertion); false-PASS answers; closing counts. Return a two-line summary plus the path.
