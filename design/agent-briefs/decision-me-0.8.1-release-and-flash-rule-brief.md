You are the DECISION agent (fable tier) standing in for the operator, who is asleep and has said "Fable may answer for me". Two operator-shaped questions; answer both with a DECISION, its conditions, and the exact commands. Read-only on every repo; commit nothing; no sub-agents; read no `.jsonl`.

Repos: mnemonic-engrave `/scratch/code/shibboleth/mnemonic-engrave` (master `308a905`, branch `hashlock-h1b` at `278a0e4` under post-implementation review); seedhammer fork `/scratch/code/shibboleth/seedhammer` (main `c4a64fc` = H0 reader guards, merged and pushed, NOT flashed); mnemonic-secret `/scratch/code/shibboleth/mnemonic-secret` (`ms` 0.18.0 released with the `hashlock` verb; ms-codec 0.8.0 on crates.io).

## Q1 -- when does `me` 0.8.1 get tagged and released?
Facts: `design/FOLLOWUPS.md` F-454 (grep `^### F-454`) says cut me 0.8.1 "with the next host change" -- H1b (`me` bumps to ms-codec 0.8 and refuses a kind-0x03 preimage plate BY NAME on `sysw pack` and `seal --seal-secret`) is that host change; the branch's CHANGELOG `[Unreleased]` carries H0's, the `+`-sign entry's and H1b's items (`git -C /scratch/code/shibboleth/mnemonic-engrave show 278a0e4:CHANGELOG.md | head -60`); the release is `git tag v0.8.1` + push -> `.github/workflows/release.yml` `assemble + sign + release` (`if: startsWith(github.ref, 'refs/tags/v')`, line ~329) -- read how v0.8.0 was cut (`git log --oneline --grep 0.8.0`, `design/RELEASE_PROCESS.md` if present, the v0.8.0 tag's commit) and whether any step is manual. The operator's ruling L26 for ms was "Release regardless of the device" (0.18.0 did not wait for a measured flash). The H2 device stage (fork only) and H3 (records) do not touch `me`. What does RELEASED v0.8.0 do with a preimage plate today (ms-codec 0.7 has no kind 0x03 -- refused as invalid, or placed)? Check with `/scratch/code/shibboleth/mnemonic-engrave/target/release/me` if it is 0.8.0, else say unknown.
Decide: (a) release v0.8.1 immediately after H1b's post-impl review closes GREEN and the branch merges to master -- or hold for H2/H3; (b) the exact sequence (merge --no-ff, records, push via `scripts/push-via-staging.sh master`, tag, push tag, watch the release run, verify assets), and whether the CHANGELOG `[Unreleased]` -> `[0.8.1] - 2026-09-05` rename plus the `Cargo.toml` version (already 0.8.1 on the branch) is all the release commit needs.

## Q2 -- the flash rule while the operator sleeps
Facts: the SH2 runs `bg839fa5a` (composer W-6/W-7); fork main `c4a64fc` (H0) is built, merged, pushed and unflashed; H2 (device hashlock phrase) will merge later tonight; every previous flash was `~/bin/sh/sh2-flash -y` at the operator's explicit word (read that script: `cat ~/bin/sh/sh2-flash` -- does it need the device in BOOTSEL by hand, does it reboot a running firmware itself, does it sign with the operator's boot key?); memory notes: PD negotiation is sometimes slow (a dark screen + BOOTSEL mimics a rejected signature -- wait), the boot key in OTP slot 1 accepts only images signed by the operator's key, slot 0 recovery intact. Probe the device presence yourself, read-only: `lsusb` and `picotool info 2>&1 | head` (do NOT flash, do NOT reboot the device).
Decide: may the controller flash `c4a64fc` (and later H2's merge tip) unattended if the device is detected ready -- or does every flash wait for the operator? If unattended flashing is allowed, state the exact preconditions (device state, which tip, what "boot judgement" the controller can make without eyes on the screen -- e.g. `picotool info` reporting the new build id) and the abort rule. Prefer the conservative answer when the evidence is thin; a flash that the operator has to recover from at breakfast is worse than one deferred.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/decision-me-0.8.1-release-and-flash-rule.md` (create; must not exist): for each question the facts you verified (commands + output), the DECISION in one sentence, conditions, the command sequence. Return two lines: the two decisions + the path.
