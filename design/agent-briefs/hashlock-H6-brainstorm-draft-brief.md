You are the DESIGN AUTHOR (opus tier) for the brainstorm of hashlock stage H6 -- engraving hashlock PREIMAGE plates on the SeedHammer II. Write `design/BRAINSTORM_hashlock_H6_preimage_plates.md` in mnemonic-engrave (`/scratch/code/shibboleth/mnemonic-engrave`) with SEVEN sections, then a report whose main content is the list of questions the operator's decisions do NOT settle. The operator will answer only those; everything decided below is FIXED and not to be reopened.

REPOS (read-only; commit nothing): engrave master (`design/SPEC_hashlock_H2_device.md`, `SPEC_hashlock_H5_device_polish.md`, `SPEC_wallet_policy_composer.md`, `FOLLOWUPS.md`; `crates/me-cli/src/sysw/` for the wallet-policy payload's record classes `key:`/`hash:`/`now:`/`tx:` and `me sysw pack`; `crates/me-cli/src/seal/record.rs` for the preimage refusal); mnemonic-secret (`design/SPEC_ms_hashlock.md`, `crates/ms-cli/src/hashlock_phrase.rs`, corpus `crates/ms-codec/tests/vectors/hashlock-v0.8.json`: the plate string format, `HASHLOCK_SALT=b"ms-hashlock-v1"`, `HASHLOCK_ITERATIONS=100_000`, dklen 32); seedhammer fork main `fb0dd04` (`gui/composer_*.go` -- state, HOLD in `composer_hashlock.go`, `bundleEngrave` in `composer_flow.go`, the Wallet Policy door in `composer_door.go`; `backup/` plate layouts `Seed`, `SeedString`; `gui/freetext_flow.go` and `gui/passphrase_flow.go` typed entry; `seal/record.go` classifier; `hashlock/` package incl. `IsMS1Shaped`; `sysw/` payload reader). Verify every mechanism you cite with a grep and cite file:line at the named revisions.

DECIDED BY THE OPERATOR (fixed):
1. Plate forms: EITHER the preimage plate string (the ms1 kind-0x03 string `ms hashlock` prints) OR the phrase and its method as text. A QR is optional and, when chosen, ALWAYS encodes the phrase and method as plain text (never the ms1 string). Default: plate string, no QR; the phrase form only when the device knows the phrase; the QR toggle only with the phrase form.
2. QR text: labelled lines with a version tag; the method line names the algorithm IN FULL, e.g. `hashlock v1` / `method: pbkdf2-hmac-sha256 iterations=100000 salt=ms-hashlock-v1 dklen=32` (or `method: sha256`) / `phrase: <phrase>` as the LAST line, no trailing newline. Exact spelling to be pinned in the spec and corpus; `ms hashlock` learning to parse it is a follow-on.
3. Entry points: (a) the wallet-policy composer at HOLD (device-derived: phrase, method and preimage in hand); (b) the UNSEALED wallet-policy payload (`me sysw pack`), which may carry EITHER the ms1 preimage string OR a NEW phrase-and-method record (Rust first with vectors, then Go). The sealed payload and the typed M*1 STRING door are OUT OF SCOPE; H0's inertness elsewhere is untouched. The host may generate the preimage (`ms hashlock`) -- a first-class path, not an afterthought.
4. Architecture C (hybrid): composer-native for device-derived preimages (kept in the composition state, cut at Done inside the composer's bundle, header with path number, digest, policy csid); a separate "Hashlock plates" flow, reached under the Wallet Policy door, for payload-delivered preimage/phrase records (cut without a composition; header with digest, the payload's `hash:` position when matched, template id when the payload carries one). The composer's `Which hash?` screen also lists payload preimage/phrase records as pickable digests.
5. Plate: a DEDICATED layout for both forms (never the seed layouts), title band `HASHLOCK PREIMAGE` / `NOT A SEED`, then the locator header, then the body (string, or phrase + method line), QR on the right when chosen; sized so a 100-character phrase fits at the smallest rung.
6. Cut at Done (composer) after a review step offering per-plate form and QR; the review lists a payload-delivered preimage no path used as "not on any path, will not be cut".
7. Host: `me sysw pack --pack-preimage` (one flag, both record kinds) in the pattern of `--seal-secret`; without it the H1b refusal stays, its text gaining "re-run with --pack-preimage if that is what you intend"; with it, a transit warning (an unsealed payload is readable by anyone holding the tag; for a key-less path the preimage alone spends) and a warning when the flag is given with no preimage/phrase record or when a preimage's digest matches none of the payload's `hash:` records.
8. Free text and passphrase programs: text stays free -- WARN (a confirm naming what the text looks like: an ms1 string) then cut as typed. Never refuse.
9. Retention of the preimage/phrase/method in RAM for the composition's lifetime is accepted (secret-handling, non-gating by the 2026-08-27 ruling; F-483 territory).

THE SEVEN SECTIONS: (1) the two paths and the material each holds (data model, provenance, lifetime); (2) host records and `me sysw pack` (the two record kinds' wire form -- look at the siblings `key:`/`hash:`/`now:` first; the flag; the warnings; Rust-first vectors); (3) device admission and the composer screens (the payload reader's new classes, `Which hash?` rows, the Hashlock plates flow under the door, what stays inert); (4) retention and the Done review (state, the review screen, the not-on-any-path warning, Back contract); (5) the plate layout and QR text (geometry at the smallest rung, the header fields, the QR payload, fit gate); (6) the free-text and passphrase warning; (7) tests, gates and acceptance (corpus rows for the new record and the QR text; lockstep host/device; walk; fit gates; whole-gui; firmware size). Each section: the decisions applied, the mechanism with citations, and its OPEN QUESTIONS -- things two implementers would do differently that the decisions above do not settle (e.g. the exact wire shape of the phrase record; how a payload phrase's method is spelled; whether the composer's `Which hash?` shows a payload phrase record before deriving it; whether csid is available at Done and where it comes from; the review screen's fit; what happens to a plate the operator declines; how many hashlock plates per bundle; the template id's source). Do not answer the open questions yourself unless one of the fixed decisions answers it -- list them.

Rules: read-only; commit nothing; no sub-agents; never read any `.jsonl`; no placeholders. Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H6-brainstorm-draft-report.md` (create; must not exist): the consolidated list of UNSETTLED QUESTIONS for the operator, each with 2-3 options and your recommendation in one line, grouped by section, most consequential first; plus anything in the fixed decisions you believe is unsound (say why, with the citation) under a separate heading. Return a two-line summary plus the two paths.
