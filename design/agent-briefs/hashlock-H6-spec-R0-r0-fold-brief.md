You are the FOLD author (opus tier) for round 0 of the R0 gate on `design/SPEC_hashlock_H6_preimage_plates.md` (engrave master `a0f832d0`, working tree) in mnemonic-engrave (`/scratch/code/shibboleth/mnemonic-engrave`). Three lens reports are persisted in `design/agent-reports/`: `hashlock-H6-spec-R0-r0-fidelity.md` (4C/7I/7M/3N), `-journey.md` (1C/7I/7M/2N), `-tests.md` (0C/3I/1M/1N). Read all three in full first. The operator's decisions and rulings (continuity entries "H6 BRAINSTORM", "Group A rulings", "OPERATOR: Proceed", and the C-1 ruling below) are FIXED.

C-1 RULING (journey C-1, the missing ms1 preimage encoder): <C1_RULING>

THE JOB: fold every Critical and Important (or decline with a true reason), and the Minors/Nits where they are truth fixes, so that every step the spec describes has been RUN by you where it can be run at spec time. In particular:
- fidelity C-1 = tests I-3: the QR raise's missing `constantTimeQRModules` capacity entries -- derive the fuzzed budget the fork's own method uses (`engrave/engrave.go`, the existing entries' provenance), state the deliverable and its test; make the constant-time test non-tautological (say what it compares).
- fidelity C-2: QR scale 2 panics `ConstantQRCmd.Engrave` (`unsupported module scale`); either add scale-2 support to the constant-time engraver as a normative deliverable with its geometry re-measured, or re-fit the phrase plate at a supported scale and re-measure every geometry number -- state which and why, with your measurements.
- fidelity C-3: `--pack-preimage` in classification -- read `decide_sealing` (`crates/me-cli/src/main.rs:2353-2410`, the call at `:1696`, the admission value at `:1478`, strict `sysw::classify`) and specify exactly where the flag acts, such that (ruling A6) the two classes are secret, the payload seals by default, the no-seal flag yields the cleartext payload, and the Go/Rust lockstep corpus (generated with the strict classifier) stays consistent; state the refusal without the flag and the warnings with it.
- fidelity C-4 = journey I: the per-plate review cannot live on `confirmReviewScreen` (three buttons bound; rows not touch targets); specify the screen that does it (a picker per hashlock plate before the census, or a new screen), its Back contract and fit rows.
- journey I: the Hashlock plates flow's silent ~10 s derive (say what is shown); the orphan check covers `phrase:` records too; the collision copy names the exit; `PREIMAGE REQUIRED` marking's true scope (mk1 cards too, or excluded -- decide and say); the Password-program refusal made visible; copy for the "preimage plate cut, policy aborted" window.
- tests I-1/I-2, fidelity: the 5.0 mm rung advance 3.3333; every non-ASCII character in every copy string removed (the device blanks on `§` and `—`), and every affected headroom re-measured; the two non-reproducing headroom numbers replaced by your measurements.
- Every number in the folded spec is YOUR measurement of the text as written (QR module counts with `qr.Encode` at ECC-L; `backup.CharsPerLine`; `assertModalBodyFits`; the constant-time budget by the fork's method). Work in your own detached fork worktree `git -C /scratch/code/shibboleth/seedhammer worktree add --detach /scratch/code/shibboleth/.tmp/h6-fold fb0dd04` (Go `/scratch/code/shibboleth/.toolchain/go/bin/go`; remove when done) and an engrave scratch if you need cargo (`CARGO_TARGET_DIR` under `/scratch/code/shibboleth/.tmp`).

RECORDS: add `## R0 round 0 folded here` (finding -> change, declines with reasons, the C-1 ruling applied) and set STATUS to `DRAFT -- R0 round 0 folded; r1 fold verification pending`. Edit only the spec; never the reports or any repo code; COMMIT NOTHING; no sub-agents; never read any `.jsonl`; re-grep every NEW citation you add.

Report (final action): write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H6-spec-R0-r0-fold-report.md` (create; must not exist): per finding the change and the measurement; declines with reasons; anything you could not make consistent. Return a two-line summary plus the path.
