You are the INDEPENDENT adversarial execution reviewer (opus tier) for hashlock stage H6 implementer A — Task 1 of `design/IMPLEMENTATION_PLAN_hashlock_H6_preimage_plates.md` (engrave master `3890ad64`, R0 GREEN; spec `design/SPEC_hashlock_H6_preimage_plates.md` at `95418b2c`, §3.1 the move, §3.6 the phrase rule, §7 the QR text). This review sits immediately BEFORE the stage's one irreversible action: `cargo publish` of `ms-codec` 0.9.0. Branch `h6-a` in mnemonic-secret, worktree `/scratch/code/shibboleth/ms-worktrees/h6-a`, tip `ca715165`, base master `504ff46`; the implementer's report is `design/agent-reports/hashlock-H6-implementation-A-report.md` in engrave (its four deviations are claims to verify).

ONE QUESTION: over the whole diff `git -C /scratch/code/shibboleth/ms-worktrees/h6-a diff 504ff46..ca715165`, can you construct a typed phrase, an ms1 string, or a (phrase, method) pair for which ms-codec 0.9.0's `validate_phrase` / `looks_like_ms1` / `qr_text` differ from what ms-cli 0.18.0 did at `504ff46` for the same bytes (the CHANGELOG's "no behaviour change to any existing verb" claim), or for which the seven `qr_text` corpus rows differ from what the three constants and the spec's normative text produce — and does every test the diff adds fail on the defect it names?

Read-only on the branch and master; commit nothing; no sub-agents; never read any `.jsonl`. Build and RUN in your own detached worktree with its own target dir: `git -C /scratch/code/shibboleth/mnemonic-secret worktree add --detach /scratch/code/shibboleth/ms-worktrees/h6-a-review ca715165`; `PATH=$HOME/.cargo/bin:$PATH TMPDIR=/scratch/code/shibboleth/.tmp CARGO_TARGET_DIR=/scratch/code/shibboleth/.tmp/h6-a-review-target`. Mutate only there; revert after each; remove the worktree and target dir when done. The released 0.18.0 binary for A/B comparison: build it from master `504ff46` in a second detached worktree (`/scratch/code/shibboleth/ms-worktrees/h6-a-base`) or use `/scratch/code/shibboleth/mnemonic-secret/target/debug/ms` if it is at 0.18.0 (`ms --version`).

## Already settled — do not re-derive
- The controller re-ran at `ca715165`: `cargo nextest run --locked`, `cargo fmt --check`, `cargo clippy --all-targets -D warnings` under the repo's `rust-toolchain.toml` 1.85.0 pin, `cargo publish --dry-run`, and the corpus sha256 (numbers in the dispatch message). No compile/lint/fmt findings.
- The plan's R0 closed under four lenses; review the CODE against the SPEC and against 0.18.0's behaviour. Secret-handling never gates.

## Construct counterexamples — an assessment without one is Minor at most
1. **Behavioural equivalence of the move.** Diff the DELETED ms-cli code (its `validate_phrase`, `PhraseRefusal`, the ms1 shape test, `HASHLOCK_PHRASE_MAX_CHARS`) against the ADDED ms-codec code clause by clause (trim, separator set, `ms1` prefix, min/max length, cap counted in chars vs bytes, control characters, the 64-hex rule, the order of checks). For every clause that could differ, run BOTH binaries (0.18.0 at master and the branch) on an input that exercises it: e.g. a phrase of exactly 100 chars; 101; 100 multibyte chars; leading/trailing/interior TAB, CR, NUL; `ms1` with each separator; `MS1` uppercase; a 46/47/48-char ms1-looking string; a 64-hex string; a 64-hex string with a `-`; the corpus `refusals` rows. Same exit code and same first stderr line, or it is a Critical.
2. **`qr_text`.** Recompute each of the seven corpus rows by hand from the constants (`HASHLOCK_SALT`, `HASHLOCK_ITERATIONS`, dklen) and the spec §7 template; confirm the byte counts 122/63/194/135/119/102/109; confirm the row's `kind`/method labels match the spec; confirm the Go twin the plan names (Task 5b `QRText`) will be pinned to the SAME strings — any spelling the plan's fork block disagrees with is Important.
3. **The public API surface** against what Tasks 2 (me) and 5b (fork) consume per the plan's Interfaces blocks: exact names, signatures, error enum variants, `pub` visibility, re-exports, `#[non_exhaustive]` decisions, `Zeroizing` on anything secret. A name B will call that does not exist is Critical.
4. **Tests that cannot fail.** For each test the diff adds or edits, mutate the guard it names and quote the failure; the implementer's supplementary mutation (the `dklen=` assertion at `:100` vs the 73-character row) — reproduce both.
5. **Records and lockfile.** Version 0.9.0 on ms-codec only; ms-cli's exact pin `="0.9.0"` (deviation 1) is correct and minimal; `git diff 504ff46..ca715165 -- Cargo.lock` moves only ms-codec/ms-cli; CHANGELOG under `## ms-codec [0.9.0]` records the corpus sha; `MIGRATION.md` and the `FOLLOWUPS.md` cross-repo entry are known-OPEN (the release agent does them) — do not report them.
6. **The report.** Every count in the implementer's report must match what you measure at `ca715165`; a false count is Critical.

## Severity
Critical: a behavioural divergence from 0.18.0 for the same bytes; a corpus row the constants do not produce; a consumed name that does not exist; a test that cannot fail on a normative guarantee; a false report count. Important: a missing case, an unsound assumption, a lockfile move beyond the two crates, a mutation whose effect differs from the report's claim. Minor: wording, secret-handling, records.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-engrave/design/agent-reports/hashlock-H6-A-pre-publish-review.md` (create; must not exist): findings `### C-n / I-n / M-n / N-n — title` with the command, verbatim output and the spec line; the A/B equivalence table (input, 0.18.0 exit + first line, branch exit + first line); the mutation table; the deviations with a verdict each; closing counts and GREEN / NOT GREEN. Return a two-line summary plus the path.
